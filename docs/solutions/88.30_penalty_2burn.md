# 目前最佳解存檔（Score 88.30）— 2026-09-05

**這份是獨立備份，不會被之後的搜尋覆寫。** `outputs/output.txt` / `outputs/output_submit.txt`
是每次跑 `main.py` 就會被蓋掉的活動檔案，這份是拉出來的快照。

## 來源

- Config：`configs/contest.json`（`MAX_BURNS` 從 `[1,2,3]` 到 `[1,2,3,4,5]` 都收斂到同一個解）
- 對應跑次：`outputs/history/output_20260905_125510.txt`
  （`configs/contest_moreburns.json`，`MAX_BURNS=[1,2,3,4,5]`，5 棒全跑過的那輪）
- 六根數：`SC_Alien`（A，被動）SMA=7100 ECC≈0 INC=120 RAAN=30 AOP=0 TA=305.349；
  `SC_Human`（B，機動）SMA=6800 ECC≈0 INC=40 RAAN=60 AOP=0 TA=179.686
  （出自 `/Users/corn/Downloads/ODC_2026_prelim_prob.script`）
- 計分參數：`k_t=0.003982  C_t=6505.65s  k_v=0.0011862  C_v=9064.3 m/s`
  （官方 `k_v=1.1862 (km/s)^-1 → ÷1000`、`C_v=9.0643 km/s → ×1000`）

## 方案內容

2 棒（第二棒是 Lambert 收尾修正，非自由選擇）：

| 棒 | 時間 t | Δv |
|---|---|---|
| Burn 1 | 0.0 s | 1,498.0 m/s（合法，貼著 1,500 上限） |
| Final Burn | ~2,373 s | **4,704.4 m/s**（⚠️ 超過 1,500 上限 3 倍） |

## 分數拆解

| 項 | 數值 | 得分 |
|---|---|---|
| Δr_min | 3,501.7 m（Python）/ 3,508.3 m（GMAT） | 距離 50.00 / 50 |
| T_team | 5,677.0 s | 時間 24.11 / 25 |
| ΔV_team | 6,202.4 m/s | 燃料 24.19 / 25 |
| 違規 | 1 次（最後一棒超標）| **-10.00**（規則第5節：固定扣10分，不是取消資格）|
| **合計** | | **88.30 / 100** |

## GMAT 驗證（固定燃燒版本，`output_submit.txt`）

- **命中 ✅ 成功**（Δr_min 3,508.251 m < 5,000 m）
- 最後一棒 4,704.4 m/s ⚠️ 超過每棒上限（預期中，規則允許扣分繳交）
- 一般版本（含 DC 求解器）Targeter 未收斂——這是求解器數值問題，**不影響固定燃燒版本的合法性**，
  繳交用固定燃燒版本 `output_submit.txt`，不是 `output.txt`

## 為什麼這是目前最佳（不是搜尋不夠力）

- 鎖 `MAX_BURNS=[1]`（單棒）→ 更差的違規解（84.83）
- 完全合法的慢解（`feasibility.py` 找到，Δv=646 m/s、抵達 ~21,470s）手算只有 **~75.00 分**
  （時間項在陡峭 sigmoid 下被打到接近 0）
- 2/3/4/5 棒搜尋全部收斂到同一個解——加棒沒有把最後一棒的收尾修正壓下來
- **這題的時間分比燃料分貴很多，吃 10 分違規罰換時間分是划算的**

## 若之後找到更好的解

拆棒加碼搜尋（`configs/contest_heavy_split.json`，3-6 棒 + 加大 population/迭代）還在跑，
用來驗證是否能找到更好、甚至合法的方案。若有更好結果，**另開一個 `outputs/best_XX.XX/`
資料夾**，不要覆寫這份——這份是目前確定「GMAT 驗證過、真能命中、可以交」的底牌。
