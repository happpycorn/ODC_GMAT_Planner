# 4 棒路線家族 · A1 拆棒六實燒（Score 98.3190，零違規）— 2026-09-23

**contest.json（初賽幾何）目前最高分。** 比初賽冠軍 Team15 的 98.3162 高 0.0028、比我們初賽繳交的
98.32 家族（98.3174～98.3177）高 ~0.0015。GMAT 一般版（DC）與固定燃燒版皆命中、收斂、合規。
繳交腳本：[`98.319_route4_split6_submit.txt`](98.319_route4_split6_submit.txt)（固定燃燒、無求解器）。

## 怎麼來的

- 搜尋：`MAX_BURNS=[4]` + `SPLIT_AWARE_SEARCH=true`（模仿 C2 的 3→4 棒重搜）、MAXITER=200、
  POPSIZE=50、`SEED=7`、REVS 集成關、C2 關。
- 拆前 DE 贏家只有 **88.3009**（1 次違規），**比 88.3219 那條路線還低**——但終端大棒的時刻/幾何不同
  （2386.5 s、4756 m/s，對比 2370.9 s、4691 m/s），拆完反而少 ~7 m/s。**拆前最優 ≠ 拆後最優**
  的實例；C2 只讀拆前分數，所以滿預算管線預設走不到這裡（C3 文件問題 ①(b)）。
- 拆棒：A1（SLSQP 跑到收斂 + 約束餘裕）→ `prune_null_burns` 剔掉 2 發空燒（8 → 6 棒）。
- 重現：拆棒前存檔 → `main.py --from-winner <存檔>`（branch route-family-split，commit 66e3d14 之後）。
  注意 10 顆 4 棒 SEED 只有 SEED 7 走到這條（其餘 98.2607～98.3177 或拆不出）。

## 方案內容（6 實燒，全部 ≤1498 m/s）

| 棒 | 時間 t | Δv |
|---|---|---|
| Burn 0 | 0.0 s | 1498.0 m/s |
| Burn 1 | 100.1 s | 48.8 m/s |
| Burn 2 | 2257.3 s | 1498.0 m/s |
| Burn 3 | 2357.3 s | 1498.0 m/s |
| Burn 4 | 2457.3 s | 1498.0 m/s |
| Burn 5 | 2598.3 s | 141.4 m/s |

- 抵達 T_team = 5,676.61 s、總 Δv = 6,182.1 m/s、Δr = 3,496 m（≤5 km，距離分滿分）、違規 0。
- 機動間隔最短 100.0 s（合規）；1498 m/s 棒離嚴格上限 1500 還有 2 m/s。

## 分數拆解

| 項 | 數值 | 得分 |
|---|---|---|
| Δr_min | 3,496 m（≤5 km 地板）| 50.00 / 50 |
| T_team | 5,676.6 s | 24.11 / 25 |
| ΔV_team | 6,182.1 m/s | 24.21 / 25 |
| 違規 | 0 | 0 |
| **合計** | | **98.3190** |

## 驗證

- GMAT 一般版（DC）：命中 ✅、Targeter 收斂 ✅、最後一棒合規 ✅；Δr GMAT 3,496.451 m vs Python 3,496.108 m（差 0.34 m）。
- **GMAT 固定燃燒版（繳交用）**：命中 ✅、合規 ✅、Δr 3,496 m。腳本 `Degree=0`、0 個 Target/Vary/Achieve、0 非 ASCII。
- **Earth-safe 獨立驗算**：把繳交腳本 6 棒用 DOP853 每 5 s 密集取樣重傳播，全程最低高度 **421.9 km**
  （就是起始軌道高度，門檻 100 km）；終點 Δr 3,496.4 m 與 GMAT 一致。

## 限制

- 這是初賽幾何（圓軌道 A）的解，初賽已結束——價值在方法論：**拆後分數要進搜尋的選擇**，不能只看拆前。
- 拆棒器雜訊底線 ~1e-4（C3 文件 §6），98.3190 vs 98.3177 的差（0.0013）在底線之上，是真的。
