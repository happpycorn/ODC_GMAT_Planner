# 這個工具怎麼算出任務規劃的

給想知道「這個分數/這組燃燒方案是怎麼跑出來的」的人看。怎麼**用**這個工具（安裝、設定、執行、看輸出）請看 [README.md](README.md)，這份只講背後的原理跟為什麼這樣設計。

規則本身的正式定義以 `rules/` 裡的官方 PDF 為準，這份文件只是把「規則 → 程式碼怎麼實作」這條路徑講清楚。

---

## 1. 問題是什麼

- **太空船 A**：只受重力（可選 J2 攝動）影響，被動、不機動。
- **太空船 B**（我方）：可以執行多次瞬時脈衝機動 Δv_k。
- **目標**：在時間上限 `T_max = T_MAX_PERIOD_MULTIPLE × A的軌道週期` 內，讓 B 跟 A 的相對距離 Δr 降到 ≤ 5 km（視為攔截成功），同時兼顧燃料消耗（ΔV_team）跟任務時間（T_team）。
- **限制**：單次機動 Δv ≤ `MAX_DV_MPS`、兩次機動間至少間隔 `MIN_MANEUVER_INTERVAL_SEC` 秒。
- **關鍵細節**：規則只要求 Δr ≤ 5 km，超過門檻的精準度**不會多加分**——`Δr_min = max(實際距離, 5)`。這一點對整個設計策略影響很大，後面第 5 節會再展開。

這些數字全部是 `config.json` 的 `rules` 區塊，見 README。

---

## 2. 整體流程

```
config.json (軌道六根數 + 規則參數)
        │
        ▼
① 軌道初始化：poliastro 算出 A/B 在 t=0 的 (r₀, v₀)
        │
        ▼
② L-SHADE 全域搜尋（粗略但快，JIT 加速，數千次評估探索解空間）
        │
        ▼
③ L-BFGS-B 局部微調（在②找到的解附近精修，有安全回退）
        │
        ▼
④ 高精度重播：含攝動的 Lambert 微分修正，算出最終燃燒清單跟預估分數
        │
        ▼
⑤ 產生 GMAT script → 自動呼叫 GmatConsole 無頭驗證
        │
        ▼
outputs/output.txt (GMAT script) + 終端機印出 Python 預測 vs GMAT 實測對照
```

②③④ 都在 [`src/optimizer.py`](src/optimizer.py) 的 `MissionOptimizer` 裡；①用 [`src/propagator.py`](src/propagator.py)；⑤在 [`src/script_generator.py`](src/script_generator.py) + `main.py` 的 `run_gmat_verification`。

---

## 3. 物理模型：軌道怎麼傳播

核心是 [`src/core_math.py`](src/core_math.py) 的 `propagate_dop853`：

- **動力學方程**（`fast_dynamics`）：二體重力 `-μr/|r|³` 加上 zonal harmonic 攝動項。
  `strategy.GRAVITY_DEGREE` 決定算到第幾階：`0`=純點質量、`2`=J2、`3`=J2+J3、`4`=J2+J3+J4。
  三個係數不是抄教科書常數，是直接從 GMAT 用的同一份 `JGM2.cof` 反算出來的，確保兩邊
  算的是同一個重力場。J3/J4 的加速度公式是從 zonal geopotential 的梯度自己推導、再拿
  已知正確的 J2 公式回頭驗證過，J3 另外跟 `poliastro` 的獨立實作逐項比對吻合。
- **積分方法**：**DOP853**（8 階 Dormand-Prince，自適應步長）。係數表直接取自 scipy
  的 `dop853_coefficients`，誤差估計照抄 scipy `Dop853._estimate_error_norm`，移植後先
  跟 `scipy.integrate.solve_ivp(method='DOP853')` 對同一個 ODE 比對過才用 numba 重寫。
- **容忍度**：`rtol=1e-12, atol=1e-9`。這個值不是隨便挑的——舊值 `1e-9/1e-6` 在
  **高離心率軌道**上會安靜地算錯，而且誤差是隨「**近地點通過次數**」累積，不是隨傳播
  時長：一條 SMA=70,000 / ECC=0.9 的軌道，過 1 次近地點誤差 0.08 km、過 2 次 18.95 km、
  過 4 次就到 **90 km**。實際害過一次——Python 自己回報「命中 3.5 km」看起來完全正常，
  拿 GMAT 對照才發現真實脫靶 88 km。收緊之後歸零，端到端速度幾乎沒差。

**演進過程**（留著當紀錄，因為每一步都是實測逼出來的）：固定 60 秒 RK4 → RK45 自適應
→ DOP853。固定步長那版在「繞很多圈」的情境下誤差大得離譜（普通 LEO 傳播 3 天/44 圈就
差 17 公里）；RK45 把同一個案例壓到 411 公尺，但短弧線反而比 RK4 慢；DOP853 再壓到
222 公尺，而且中長弧線比 RK45 快 1.2~2.3 倍，沒有取捨。中途也完整實作並驗證過 Encke's
method（更準，但因為解析 Kepler 解每次呼叫比直接算加速度還貴 4.4 倍而全面較慢），
程式碼留在 `core_math.py` 但主線沒有呼叫。

這整套是純 Numba JIT（`@njit`）編譯的，本身跑起來很快；真正的計算量來自「這個傳播器
會被叫幾百萬次」，不是傳播器本身慢。

---

## 4. 決策變數怎麼編碼成一個最佳化問題

`fast_fitness_evaluator`（`optimizer.py`）吃的是 mealpy 丟進來的一維陣列 `x`，結構是：

```
[t_wait, (r, θ, φ, coast_frac) × (N-1), final_leg_frac, offset_r, offset_θ, offset_φ]
```

- `t_wait`：任務開始後等多久才開第一槍。
- 中間 N-1 次燃燒（如果燒 N 次）：每次是 **球座標** `(r, θ, φ)` + 這次燃燒後要滑行多久（`coast_frac`，映射到實際秒數）。
  - **為什麼用球座標，不是直角座標 `(x, y, z)`**：球座標的 `r` 本身就是 Δv 大小，把 `r` 的上下界直接夾在 `[0, MAX_DV_SOFT]`，天生保證每一組解都合規。舊版用直角座標 `(x,y,z)` 各自 `±MAX_DV`，會留下「三軸合成起來超標」的無效角落，靠事後扣分排除，浪費搜尋預算在不合法的解上。
- 最後一次燃燒不是直接給 Δv 向量，而是 `final_leg_frac`（決定這一段飛多久）+ `offset_r, offset_θ, offset_φ`（決定「瞄準 A 附近哪一點」），實際的 Δv 交給 Lambert 反推——見下一節。

---

## 5. 最後一棒：Lambert 攔截 + 命中容許範圍的利用

最後一次機動用的是 **Lambert 問題**求解：已知出發點 `r_curr`、抵達點 `r_aim`、飛行時間 `t_final_leg`，反推需要多大的初速度才能剛好在那個時間點抵達那個位置。用的是 poliastro 的 `izzo` 演算法（純 Numba，速度很快）。

**這裡的關鍵設計是命中容許範圍的利用**：規則只要求 Δr ≤ 5 km 就算成功，且 `Δr_min` 會被地板夾在 5（`max(實際距離, 5)`），所以瞄準 A 的**精確位置**跟瞄準 A 附近容許球內隨便一點，只要都在 5 km 內，得到的距離分數完全一樣——但需要的 Δv 可能差很多。所以最後一棒不是死盯著 A 的真實位置打，而是讓 `offset_r/θ/φ` 這三個決策變數自己去找「容許球內最省油的落點」：

```
r_aim = A在intercept_time的位置 + offset向量
```

`offset_r` 的上界是 `MISS_TOLERANCE_SOFT`（見第 6 節的安全邊界），保證最終瞄準點一定落在規則允許的範圍內。

其他兩個實作細節：

- **所有 Lambert 分支都算，取 Δv 最小的**：
  - **順向/逆向**：A/B 兩軌道傾角差大時，逆向解常常明顯省油（實測省一半以上），只算順向會漏掉更好的解。
  - **圈數 M（多圈轉移）**：2026-08-28 加。原本寫死 `M=0`，等於只看「不繞滿一圈就直接過去」的轉移——但 `T_max` 是 A 的**四個週期**，多圈轉移本來就在規則允許範圍內。實測官方範例題目：`M=0` 最省的合法單棒要 430.1 m/s，`M=1` 只要 **267.3 m/s（省 38%）**，代價是抵達時間從 6,145s 拉到 11,928s。用 `strategy.LAMBERT_MAX_REVS` 控制，**預設 4**（2026-08-29 從 0 改過來；規則的 `T_max` 是四個週期，最多塞得下約 4 圈）。可以放心開的理由是結構性的：分支選擇是在**固定的 `t_final_leg`** 下取需求 Δv 最小的那條，所以同一個決策向量的抵達時間不變、時間分不變，只有燃料可能更便宜——**嚴格更大的搜尋空間，不可能變差**。成本實測 1.01 倍，分支本身經 ESA pykep 驗到機器精度。設 `0` 可退回舊行為。
  - **`lowpath`**：`M=0` 時只有一組解、這個旗標沒有意義；`M≥1` 時同一個飛行時間有兩組解而且差很多（上面那組 267.3 就是 `lowpath=False` 才找得到的），所以兩個都要算。

  ⚠️ **搜尋端（`fast_fitness_evaluator`）跟重播端（`reconstruct_mission_logs`）的分支挑法必須完全一致。** 加多圈轉移時差點只改搜尋端——那會變成「搜尋照多圈解評分，但重播跟產生出來的 GMAT 腳本是不繞圈的解」，回報的數字跟實際交出去的東西對不起來。
- **`refine_lambert_burn` 的 J2 差分修正**：`izzo` 給的是「無擾動二體」下的理想解，但真實世界有 J2/J3/J4 等攝動。這個函式把 `izzo` 的猜測值丟進含攝動的 `propagate_dop853` 裡，用牛頓法（三個座標軸各自做一次有限差分算 Jacobian）反覆修正，直到收斂到「加入攝動後仍然準確命中瞄準點」的 Δv。邏輯上等同 GMAT 的 `Target/Vary/Achieve`（DC1）在做的事，只是搬進 Python 裡先做一次，不用真的開 GMAT 就能高精度預覽分數。

---

## 6. 安全邊界：為什麼不卡在規則的精確邊界上

搜尋跟微調階段用的「內部目標」都比規則的真實上限更嚴一點，理由是**避免數值誤差/模型落差把解推過合法邊界**：

| 邊界 | 內部值 | 真實規則 | 為什麼要留 |
|---|---|---|---|
| Δv 上限 | `MAX_DV_SOFT = MAX_DV - 10 m/s` | `MAX_DV_MPS` | 避免 L-BFGS-B 微調的數值梯度在邊界上把解推過真正的 ΔV_lim 才被扣分 |
| 命中容許範圍 | `MISS_TOLERANCE_SOFT = MISS_TOLERANCE_KM - 1.5 km` | `MISS_TOLERANCE_KM`（≤5km） | GMAT 打靶的 `Achieve` 每軸容許誤差只有 0.01 km，理論最差合起來 ~17m，但實測跨多種軌道幾何做壓力測試後發現，J2 以外的殘餘模型落差不是穩定的幾十公分等級——SMA 差距懸殊（LEO 對到接近 GEO 高度）的情境實測落差衝到 863 m。1.5 km 留了將近一倍的緩衝 |

這些都是「目前測過的情境」歸納出來的經驗值，不是嚴謹的數學上界。正式測資公布後拿到真實軌道參數，最好針對那組實際場景再測一次確認邊界仍然夠用（見 STATUS.md 的待辦）。

`MAX_DV_SOFT` 的邊界從 2026-08-28 起可以用 `strategy.MAX_DV_MARGIN_MPS` 調（預設仍是 10 m/s）。會想調的原因：最佳解如果是「把一發大燒拆成兩段」，就會有棒數**頂到上限**，那 10 m/s 是白丟的（實測官方範例題目約值 0.03 分）。調小之後一定要確認回報的違規次數還是 0。

### 6.1 ⚠️ 保守的邊界也可能把**合法解**擋在外面

安全邊界的方向是「寧可少賺，不要違規」，但 2026-08-28 踩到一個反例，值得單獨記著。

舊版的撞地球安檢 `check_constraints()` 比的是**密切軌道的近地點半徑**——跟太空船會不會真的飛到那裡完全無關。而「把一發超過每棒上限的大燒拆成兩段幾乎同向的燒」是繞過 `ΔV_lim` 的標準手法，中間那個暫態軌道的近地點常常落在地表以下；太空船只在上面待 100 秒（機動間隔下限）就被下一棒拉回來，實際飛過的高度完全安全。舊版把**整個這種家族**判 0 分。

官方公布的範例參考解正是這一類（中間軌道近地點 5,517 km，在地表以下 860 km）。實測影響：那題在抵達時間 ≤5,600 秒的範圍內，修正前**找不到任何合法解**；修正後找到 2,224.7 m/s / 3,185 秒，比官方參考解還好一點。順帶讓 `perigee_kick_test` 從 2,434.4 m/s 進步到 2,392.4 m/s，離能量下限只剩 0.06%。

修正在 `core_math.reaches_perigee()`：用 Kepler 關係算出「接下來 dt 秒內會不會真的經過近地點」。

- **會經過** → 照舊比近地點半徑（`check_constraints`）。
- **不會經過** → 這段弧的最小半徑就是**兩端取小者**（中間就算越過遠地點也只會更高），檢查兩端即可。

兩種情況合起來對純二體是**精確**的，不是放寬標準。階梯種子那裡滑行整整一圈、一定會經過近地點，所以維持原本的嚴格判定。驗證在 `tests/test_arc_safety.py`（拿暴力掃描實際軌跡的最小半徑當基準逐一比對，不是手算常數）。

---

## 7. 最佳化演算法

### 全域搜尋：L-SHADE
[mealpy](https://github.com/thieu1995/mealpy) 的 L-SHADE（差分演化的一種自適應變種）。目標函式（`fast_fitness_evaluator`）裡有好幾處硬跳躍（撞地球直接判 0 分），不連續、不可微，比起需要梯度資訊的方法，DE 類的演化演算法比較適合。

- **族群大小**：`n_dims × POPSIZE`（決策變數維度數 × config 設的倍數），不是固定總數。舊版公式在低燃燒次數時會嚴重超編（1 次燃燒只有 2 維決策變數，舊公式卻給 360 個個體，等於 180 倍維度，遠超過 DE 類演算法常見的 10~20 倍維度經驗值，純粹浪費運算時間）。
- **早停**：`MAX_EARLY_STOP` 代沒有改善（超過 `TOL` 的量）就提前結束，不用每次都跑滿 `MAXITER` 代。

### 初始族群的種子：為什麼純隨機初始化不夠

L-SHADE 預設從均勻隨機的初始族群開始。這在多數情境沒問題，但遇到**窄窗地形**就完全
失效——實測過一組情境，合法解確實存在（單棒 1,191.7 m/s）卻只佔搜尋空間的 **0.0086%**
（約 200 秒寬的時間窗），跑 2,000 代都撞不到；反而一支用網格掃描的手寫腳本找到了。
原因不是演算法爛：那片地形幾乎完全平坦（違規但已命中的區域對計分函式來說處處同分），
沒有梯度可以引導，隨機漂移撞上窄縫的機率趨近於零。而網格掃描雖然單次評估慢 36 倍，
卻**結構性保證不會跳過任何比步長寬的窗口**。

所以 `_generate_seed_candidates` 把「便宜的低維度網格掃描」的結果當成初始族群的種子
（**額外加進去，不是取代隨機初始化**——種子猜錯的最壞情況只是浪費粗掃那幾十秒，
隨機探索這個安全網完整保留）。目前有兩個種子家族：

1. **窗口種子**（單棒，多棒則接力成多棒格式）：粗掃「A 何時靠近地球」跟「A 何時靠近
   B 的軌道平面（節線附近）」兩種候選來源 → 中解析度 Lambert 篩選 → 細網格精修 →
   轉成決策向量。兩種來源分別對應離心率窄窗跟傾角窄窗，實測傾角差 90 度時窗寬會比
   5 度時窄 12 倍，只看距地距離會整批找偏。
2. **近地點階梯種子**（只在需要時才生成）：前一種種子在多棒情境下會把前面幾棒都填成
   `Δv=0` 的空燒，本質上是「偽裝成多棒的單棒解」——單棒夠用時這完全正確（實測會正確
   退化），但單棒在**能量上根本不可能**時就整批落在錯的區域。這時改用物理構造直接生成
   「真的有燒」的種子：在近地點沿速度方向連續推進（Oberth 效率最高）、每次滑行整數個
   週期回到同一個近地點、最後一棒用 Lambert 收尾。觸發條件是「能量下限 > 每棒上限」，
   而能量下限有封閉解（微秒等級），所以單棒就搆得到的一般情境完全不花成本。

**種子光丟進族群還不夠**：種子通常只佔族群 5% 上下，而 L-SHADE 的 current-to-pbest
變異會把整個族群往當前最佳拉，種子還沒收斂就先被稀釋掉（實測過：一個很接近最佳值的
種子演化完幾乎沒動）。所以 `_optimize_burn_case` 會**對每個原始種子再單獨做一次
L-BFGS-B 精修**（不受族群其他個體干擾），贏過 L-SHADE 自己找到的贏家就取代它。
注意這裡有個前提：違規懲罰是離散跳躍（每次 -10 分），梯度法在斷點附近會瞎眼，所以
種子必須**本身就已經合法**，不能只是「接近合法」——階梯種子每一棒都夾在上限內就是
為了這個。

### 平行化
兩層平行：
1. **跨燃燒次數（process 層級）**：`config.optimization.MAX_BURNS`（例如 `[1,2,3]`）每個數字各自開一個獨立 process（`ProcessPoolExecutor`），互不干擾，同時搜尋，最後比較三者的最佳解，選分數最高的。
2. **單一燃燒次數內（thread 層級）**：`fast_fitness_evaluator` 標記 `nogil=True`（純數值運算，沒碰任何 Python 物件），mealpy 用 `mode='thread'` 讓同一代族群的評估平行跑在多條執行緒上，真的能吃到多核心而不是被 GIL 卡住。

`NUM_THREADS <= 0`（含預設 `-1`）時自動用「可用核心數 ÷ 燃燒次數案例數」估一個合理的執行緒數。

### 可重現性 vs 速度的取捨
設了 `SEED` 就會自動退回單執行緒。原因：mealpy 的 `seed=` 只控制它自己建的 RNG，但 L-SHADE 內部算突變參數用的是 `scipy.stats.cauchy.rvs`，沒有帶 `random_state`，實際上吃的是 numpy 的全域隨機狀態；而多執行緒下好幾個執行緒同時搶同一個全域 RNG，誰先誰後看 OS 排程，即使種子固定，每次跑到的執行順序還是不同——這是實測驗證過的（同 seed 關執行緒完全重現、開執行緒就對不上）。所以「要重現性」跟「要速度」二選一，沒法兩者兼得。

### 局部微調：L-BFGS-B + 安全回退
L-SHADE 找到的解丟進 `scipy.optimize.minimize`（L-BFGS-B）在窄範圍內精修（角度/方向類參數給較寬容忍度，時間類參數給較嚴格容忍度，避免微調打亂已經算好的攔截時序）。

**安全回退**：L-BFGS-B 的 `success` 只代表「收斂了」，不代表「比原本的解更好」——目標函式裡的硬跳躍讓數值梯度在不連續處不可靠，微調完分數反而變差是真的會發生的。所以一定會實際比一次微調前後的 fitness，沒有變好就退回微調前的解，並且區分「沒有改善」（scipy 自己覺得收斂了，但退步）跟「沒有收斂」（撞到 maxiter 之類，scipy 自己都不信任這個結果）兩種訊息，不要混為一談。

---

## 8. 計分公式怎麼實作的

規則第 5 節的公式（`src/scorer.py` 的 `calculate_score`）：

```
Δr = max(Δr_min, 5)
Score = 50·exp(-(Δr-5)/100)                       # 距離分：Δr=5(剛好合格) 時滿分 50，越大越衰減
      + 25 / (1 + exp(k_t·(T_team - C_t)))          # 時間分：T_team < C_t 時接近滿分 25，超過就快速衰減
      + 25 / (1 + exp(k_v·(ΔV_team - C_v)))          # Δv分：ΔV_team < C_v 時接近滿分 25，超過就快速衰減
      - 10 × 違規次數                                # 每次 Δv 超過 MAX_DV_MPS 扣 10 分
```

`k_t/C_t/k_v/C_v` 是主辦方每次比賽前才公告的環境參數（`config.rules`），跟軌道分布狀況有關；跟軌道六根數一樣，目前 repo 裡的數字都是還沒等到公告前的測試值。

`_optimize_burn_case`（搜尋階段）跟 `_replay_mission`（最終重播）都呼叫同一個 `calculate_score`，唯一差別是搜尋階段用比較粗的模型快速估分，重播階段用含攝動微分修正過的高精度解算真實分數。


### 8.1 分數之外：規則第 6 節的平手判定

分數只決定**分數**，名次還有一層。規則第 6 節規定同分時依序比 `Δr_min`（小者前）→
`ΔV_team`（少者前）→ `T_team`（短者前）→ 設計理論報告。工具在 `tiebreak_rank_key()`
實作這個字典序，用在兩個地方：`_pick_best_case()`（不同燃燒次數的候選誰出線）跟
`sweep_burns.py` 的結論表。

**順帶修掉的一件更重要的事：挑贏家改用「真實分數」而不是「代理值」。**
平手判定需要 `Δr_min/ΔV_team/T_team`，那要重播才算得出來，所以 `_pick_best_case()`
現在會對每個燃燒次數的候選各做一次重播——而重播算出來的是**真實成績**，
搜尋用的 `fast_fitness_evaluator` 只是代理（最後一棒直接用純二體 Lambert 的 Δv，
不含 J2/J3/J4 修正）。舊版是拿代理值挑贏家。

兩者差多少完全看飛行時間：

| 情境 | T_max | 代理與真實分數的落差（最大 / 中位數）|
|---|---|---|
| 官方範例題目（LEO）| 23,204 s（6.4 小時）| 0.07 / **0.02 分** |
| `hard_mode` 診斷變體 | 1,258,841 s（14.6 天）| 77.24 / **8.26 分** |

初賽是 LEO、`T_max = 4×T_A` 只有幾小時，代理幾乎完全精確；但多日尺度的情境上代理會
嚴重高估（最誇張一例：代理 78.3 分、真實 1.1 分，因為 J2 修正後的 Δv 從代理以為的量
暴增到 13,661 m/s）。**用真實分數挑顯然才對——官方是用真實成績計分的。**

**為什麼這在這個工具裡不是邊角案例**：計分函式有三片平原——`Δr ≤ 5km` 內距離分是
常數、燃料/時間 sigmoid 飽和時該項是常數、多棒解常退化成跟少棒解位元相同。三種情況
都會產生**浮點數等級一模一樣**的分數。舊版在這種時候是「哪個行程先跑完就用哪個」。

**`Δr` 是唯一會出現「數字有差但差距沒意義」的量**，因為它是唯一被計分地板夾住、
搜尋對它沒有梯度的量。兩個後果：

1. 比較時量化到 `TIEBREAK_MISS_RESOLUTION_M = 10 m`（GMAT DifferentialCorrector 的
   Achieve Tolerance 就是每軸 0.01 km，更細的差距傳不到交出去的腳本裡）。
2. `_tiebreak_polish()` 會在**分數一分都不能少**的前提下把它壓到最小。燃料項飽和時
   這是白拿的（實測 3,000 m → 0.035 m，分數位元不變）；沒飽和時會什麼都不做，
   因為那時搜尋已經做過權衡了。

**實測量出來的權衡尺度**：`playground` 情境把瞄準偏移從 0 拉到規則極限 3.5 km，
分數只變化 **0.001 分**。所以預設值（`TIEBREAK_SCORE_EPS = 1e-9`）等於為了 0.001 分
讓出 3.5 km 的優先序 1 優勢。把它設成 `0.005` 就是賭「官方比分數時會四捨五入到
小數點後兩位」——這是個賭注，規則沒說，工具不替使用者決定，只把代價印出來。

**還有一個規則層級的歧義**：第 6 節優先序 1 的符號 `d_min,team` 全文沒有定義，
第 4/5 節計分用的則是 `Δr_min = max(Δr, 5)`。套地板的話，所有攔截成功的隊伍這一項
全都是 5，優先序 1 對成功組完全失效。工具預設用原始距離，但兩種讀法選出不同方案時
會明講。

---

## 9. GMAT 驗證在做什麼、為什麼需要

Python 端（第 3~8 節）已經能自己估出一個相當準的分數，**但這不是主辦方認可的計分依據**——規則附則明講「所有結果以主辦單位驗證程式為準」。GMAT 是業界標準的任務分析工具，用的是比 Python 這邊更高階的積分器（`RungeKutta89`）跟真實重力場模型（`JGM2.cof`），所以每次執行都會自動把產生的 script 丟給 GMAT 無頭跑一次（`GmatConsole --exit --run`，不開 GUI），拿它的結果跟 Python 的預測對照。

GMAT script（[`script_generator.py`](src/script_generator.py)）裡幾個值得知道的設計：

- **`Target/Vary/Achieve`（DifferentialCorrector）**：最後一棒的燃燒方向/大小，GMAT 自己還會再修一次，讓 `ShipB` 的最終位置精準命中 `Achieve` 指定的目標點。這個目標點**必須是 Python 算好的 `aim_point`（第 5 節那個容許球內的省油偏移點），不能是 A 的精確位置**——早期版本這裡曾經是個真 bug：GMAT 的打靶目標寫死瞄準 `ShipA` 的精確位置，會讓 GMAT 自己的 DC 悄悄把 Python 刻意設計出來的「打偏一點比較省油」的方案修正掉，等於白做了第 5 節的優化。修成瞄準絕對座標的 `aim_point` 之後才修好。
- **`FinalBurnDvMps`/`FinalBurnLegal`**：GMAT 的 DC 可以自由調整最後一棒的方向/大小去命中目標點，所以它實際收斂後的 Δv 不一定等於 Python 預測的那個值。`InterceptSuccess` 只檢查距離，不檢查這個——GMAT 自己的打靶器完全可能悄悄修出一把超過規則上限的燃燒而沒人發現。所以額外算了 `FinalBurnDvMps`（真實收斂後大小）跟 `FinalBurnLegal`（是否 ≤ `MAX_DV_MPS`），兩者都要看才能確認這一棒真的合規。
- **裝飾用參數**：`DryMass`/`Cd`/`Cr`/`DragArea`/`SRPArea`/`Isp`/`GravitationalAccel` 這些欄位不影響任何計算結果——`ForceModel` 的 `Drag=None`、`SRP=Off`（阻力/太陽輻射壓根本沒開），`BurnB*.DecrementMass=false`（質量不會因燃燒減少）。純粹是 GMAT 建立物件的必填欄位，填一艘典型中型化學推進衛星的量級讓腳本看起來完整。
- **script 內容全程限定 ASCII**：GMAT 的解析器碰到中文/非 ASCII 字元會直接報錯，所以 script 裡（不是 Python 端的 print/註解，是實際寫進 `outputs/output.txt` 的內容）一律用英文。

### 9.1 為什麼還有第二份「固定燃燒版本」（`outputs/output_submit.txt`）
一般版本的最後一棒要靠 GMAT 的 DC（`Target/Vary/Achieve`）在執行當下即時收斂，這代表**繳交當天在主辦單位的電腦上執行時，DC 的收斂行為理論上得跟我們自己測試時一致，結果才會一樣**——雖然 DC 是 GMAT 核心功能不是外掛，正常不會「跑不出來」，但求解器的收斂路徑本來就比較不容易保證跨環境完全一致。

所以 `main.py` 在一般版本驗證乾淨通過（`InterceptSuccess`/`targeter_converged`/`FinalBurnLegal` 都成立）之後，會多做一步：把 GMAT 剛剛**自己收斂出來**的最後一棒 VNB 分量（不是 Python 的估計值，是 GMAT 實際跑出來的答案，透過 `Report_Intercept` 多加的三欄讀回來）直接當常數寫進一份新腳本，跟其他棒一樣用固定的 `Maneuver` 施加，整份腳本完全不含 `DifferentialCorrector`。這份腳本本身也會再送去 GMAT 驗證一次，確認重新單純傳播出來的結果跟一般版本幾乎一致（實測過差距在公尺等級，可視為雜訊）。

換句話說：**用求解器去找出正確答案（一般版本），但用單純的、確定性的傳播去交出答案（固定版本）**——找答案跟交答案分開，繳交的東西不依賴任何環境相關的收斂行為。

---

## 10. 已知限制

比較完整的待辦/評估過程見 [STATUS.md](STATUS.md)，這裡只列跟這份文件內容直接相關、值得知道的限制：

- **傳播誤差跟「近地點通過次數」掛鉤，不是跟傳播時長掛鉤**（第 3 節）。目前的容忍度
  (`rtol=1e-12`) 在測過的情境裡都已收斂到公尺以下，但如果之後遇到離心率更極端、又要
  繞很多圈的軌道，值得再用解析解（純二體時）或更緊的容忍度回頭驗一次。收緊容忍度的
  代價**跟情境高度相關**，換情境時值得重新量一次（見第 3 節）。
- 只實作 zonal harmonic（J2/J3/J4，只跟緯度有關），沒有 tesseral/sectoral 項。產生的
  GMAT script 因此把 `Order` 固定在 0，讓兩邊算同一個物理——這是為了「可以互相驗證」
  而犧牲 GMAT 端真實感的刻意取捨。
- ~~只探索單圈 Lambert（`M=0`）~~ **2026-08-28 已解除**：多圈轉移已整合，用 `strategy.LAMBERT_MAX_REVS` 控制，**預設 4**（2026-08-29 從 0 改過來；規則的 `T_max` 是四個週期，最多塞得下約 4 圈）。可以放心開的理由是結構性的：分支選擇是在**固定的 `t_final_leg`** 下取需求 Δv 最小的那條，所以同一個決策向量的抵達時間不變、時間分不變，只有燃料可能更便宜——**嚴格更大的搜尋空間，不可能變差**。成本實測 1.01 倍，分支本身經 ESA pykep 驗到機器精度。設 `0` 可退回舊行為。2026-08-14 那次「評估後決定暫不整合」的結論被官方範例題目推翻——當時只測了兩個情境，官方那題 `M=1` 省 38%。見第 5 節。
- 第 6 節的安全邊界數字是從目前測過的軌道情境歸納出來的經驗值，換了完全不同的軌道幾何（例如正式測資公布後）最好重新驗證一次。
