# 初賽當天作業手冊（2026-09-05）

**這份是比賽當天照著做的東西。** 怎麼用工具看 [README.md](README.md)，
原理看 [METHODOLOGY.md](METHODOLOGY.md)，做過什麼看 [STATUS.md](STATUS.md)。

來源：官方《參賽選手注意事項》(2026-08-28 發布) + 初賽規則 PDF。
**凡是這份文件裡沒有標「官方」的判斷，都是我們自己的推論，現場以工作人員說的為準。**

---

## 一、時間與地點（官方）

| | |
|---|---|
| 日期 | **2026-09-05（六）**，備用日 9/6（日） |
| 地點 | 淡江大學淡水校園 **新工學大樓** |
| 13:00–13:30 | 報到（三樓，E309 教室外）|
| 13:30–14:00 | 開幕及規則說明（E309）|
| **14:00–15:30** | **正式競賽（E213 / E214），固定 90 分鐘** |
| 15:30–16:00 | 休息、成績統計（安排實驗室參觀）|
| 16:00–16:30 | 成績公告（E309）|

- 第 **01–29** 組在 **E213**，第 **30–57** 組在 **E214**。**共 57 隊，前 8 名晉級決賽。**
- **報到要帶有照片的有效證件**（學生證／身分證／駕照／健保卡都可以）。
- 出發前先去國家太空中心官網確認**名冊與自己的組別**（官方明文要求各隊自行確認）。
- 競賽時間固定 90 分鐘，若開始時間挪動，結束時間跟著挪。

## 二、設備（官方）

- 每隊 4–5 人配 3 台電腦、3 人以下配 2 台。其中 **1 台是「正式競賽使用機」**，其餘可自行決定用不用。
- **可以自己帶筆電**。但官方明講**不保證有額外插座或充電設備** → **充飽電、帶行動電源、帶延長線**。
- 不得自行更換教室或使用其他隊伍的設備。
- 電腦／軟體出問題**立刻找工作人員**，不要自己改系統設定。

## 三、規則限制（官方原文重點）

> 「競賽期間，可使用生成式 AI（例如 ChatGPT、Gemini 等）協助設計軌道，除此之外，
> 不可使用網路尋求其他協助，例如搜尋網路文獻、開源程式碼，或者請未能實體出席的同隊隊員協助。」

- ✅ **生成式 AI 明文允許。**
- ❌ 不可用網路搜文獻／開源程式碼／找場外隊友。
- 至少一人出席即可，但**禁止與未出席隊員透過網路討論**。

### ⚠️ 開賽前一定要問工作人員的一件事

**「事先自己寫好、帶在筆電裡的程式可不可以用？」**

官方明文允許自帶筆電，禁的是「**用網路**尋求協助」，照字面讀我們的工具沒問題
（它是自己寫的，不是現場從網路抓的開源程式碼）。但這是我們整套策略的命門，
**報到或開幕說明時直接問清楚，不要自己解讀**。官方也說「如有任何疑義，應於現場立即向工作人員反映」。

如果被判定不能用 → 退路：手動用 GMAT 的 `Target/Vary/Achieve` 做，
或用生成式 AI 現場協助算 Lambert。**這個退路要事先演練過一次。**

---

## 四、90 分鐘怎麼分配

工具在這台機器上跑完官方範例題目（含 GMAT 驗證）只要 **80 秒**，所以時間非常充裕，
瓶頸是「抄數字」跟「判斷該押哪個解」，不是計算。

| 時間 | 做什麼 |
|---|---|
| 0–5 分 | 抄題目：A/B 六根數 + `k_t/C_t/k_v/C_v`。**兩個人各抄一次互相核對。** |
| 5–8 分 | **跑 `check_problem.py` 體檢抄好的 config**（見下），確認衍生量都合理再往下 |
| 5–8 分 | 跑 `feasibility.py`，知道最少要幾棒、單棒可不可行 |
| 8–15 分 | 跑第一次 `main.py`，拿到基準解並讓 GMAT 驗證 |
| 15–60 分 | **跑「省油」跟「快」兩條分支各一次，直接比分數**（見下）|
| 60–75 分 | 選定方案，跑最後一次完整流程（含 GMAT 驗證）|
| 75–85 分 | **檢查清單**（見下），確認繳交檔案 |
| 85–90 分 | 依工作人員指示提交 |

### 4.1 runtime 上界與降級旋鈕（HAP-25，2026-09-03 實測）

在這台 16 核機器上實測（`scratch_overnight/runtime_ceiling.py`，SEED=777，完整旋鈕 =
`MAX_BURNS[1,2,3]` / `MAXITER 600` / `POPSIZE 20` / `LAMBERT_MAX_REVS 4`，只計搜尋、不含 GMAT）：

| 題目類型 | T_max | 搜尋時間（完整旋鈕） |
|---|---|---|
| LEO-ish（官方範例，A 圓軌道 SMA 6,978）| 0.27 天 | **~121 秒** + 一次性 JIT ~23s |
| 大 SMA／能量門檻（hard_mode，A SMA 100,000、ECC 0.5）| 14.6 天 | **~55 分鐘**（兩點外推、已扣固定開銷）|

**唯一真正的驅動因子是 T_max，不是搜尋難度。** 早停實測**從不觸發**（連官方範例都跑滿 600
代），所以搜尋時間 ≈ `案例數 × 世代數 × 每次評估成本`，而每次評估成本由「被傳播的 B 在各段
飛過幾圈軌道」決定——也就是 T_max。官方範例每代 0.20s、hard_mode 每代 5.4s（**27 倍**），
差距全在傳播弧長。

**當天的判斷點**：建好 config、跑 `check_problem.py` 後先看印出來的 **T_max**：
- T_max 是**小時**級（初賽 A 是圓軌道，正屬這一類）→ runtime 完全不是問題，~2 分鐘一輪，照上表節奏跑。
- T_max 是**天**級（大 SMA 或高離心率 A）→ 完整旋鈕一輪可能數十分鐘，**一開始就降級**，別跑滿三輪才發現來不及。

另外有一筆跟搜尋預算無關的**固定開銷**：最後的高精度重播 + 平手微調，在 T_max 14.6 天的長弧上
就要 ~93 秒。趕時間時 `strategy.TIEBREAK_POLISH: false` 可以省掉那段微調。

#### 降級旋鈕（官方範例實測的邊際效果，比例可套到其他幾何）

由左到右 = **先退代價最小／最省時的**：

| 退這個 | 相對加速 | 分數代價 |
|---|---|---|
| `MAX_BURNS` [1,2,3]→[1,2] | ×2.0 | 單棒就夠的題目：**0**（例：官方範例）|
| `MAX_BURNS` [1,2,3]→[1] | ×4.3 | 同上 **0**——但要拆棒繞 ΔV_lim 的題目會掉分 |
| `POPSIZE 20→10` + `MAXITER 600→300` | ×4.1 | 好搜尋題 **0**；難搜尋題可能掉分（探索變少）|
| `LAMBERT_MAX_REVS 4→0` | ×1.2 | 多圈有利的題目掉分（範例題值 ~0.2 分）——**最差槓桿，最後才退** |

**先砍 `MAX_BURNS`**：最大槓桿，而且**只要 `feasibility.py` 說單棒可行就零代價**（2/3 棒會退化成
單棒，分數一樣）。要拆棒的題目（能量門檻 > 單棒 ΔV_lim，如 hard_mode）才需要留多棒——那種
題目要靠 popsize/iters 降級，不是砍棒數。`LAMBERT_MAX_REVS` 放最後：省最少、又最容易掉分。

（實測分數在通用計分參數下是 79.1，跨所有降級檔**不變**——重點是「解沒變」而非絕對分數。
原始資料 `scratch_overnight/runtime_ceiling_results.json`；先前 §四開頭記的「80 秒」偏樂觀，
那是暖機後、或早停到的較快 seed。）

`k_t/C_t/k_v/C_v` 有可能在 13:30–14:00 的「競賽規則說明」就公告，如果是，**那半小時就可以先把
config 的計分參數填好**，14:00 一開始只要補 A/B 六根數。

### 🔴 抄計分參數時的單位陷阱（已經踩過一次的坑）

官方 2026-08-28 給範例題目參數時，寫的是：

```
kt = 0.003982 1/s        Ct = 3763.526 s
kv = 1.1862 (km/s)^-1    Cv = 2.955723 km/s
```

**`kv` 是 `(km/s)^-1`、`Cv` 是 `km/s`，但本工具的 Δv 一律用 m/s。直接照抄會讓燃料項整個算錯。**

- `k_v`（config）= 官方 `kv` **÷ 1000**
- `C_v`（config）= 官方 `Cv` **× 1000**
- `k_t`/`C_t` 本來就是 1/s 跟 s，照抄

**抄完一定要驗算**：如果官方有給參考解，用它的 `ΔV`/`T` 手算一次總分。
範例題目的參考解算出來剛好 **90.00 分**——主辦方是照參考解校準參數的，
所以「參考解 ≈ 90 分」就是單位有沒有換對的檢查點。算出來離譜就是換算錯了。

### 從範例題目學到的：時間比想像中重要得多

`C_t` 被壓在參考解的攔截時間附近，而且 sigmoid 很陡。範例題目上：

| 方案 | ΔV | T | 時間分 | 總分 |
|---|---|---|---|---|
| 快解（2 棒）| 2,224.7 | 3,185 s | 22.73 | **90.33** |
| 省油解（1 棒）| 412.1 | 6,121 s | **0.00** | 73.84 |

**省 5.4 倍的燃料只多拿 6.4 分，卻賠掉 22.7 分的時間分。**
先看 `C_t` 落在 `T_max` 的哪個位置——如果像範例題目那樣壓在很前面，就直接主攻快解。
**`LAMBERT_MAX_REVS` 預設就是 4，不用管它。** （2026-08-29 從 0 改過來。）

我一度以為「時間項很陡時要關掉多圈，因為多圈是拿時間換燃料」——**那是錯的**。
分支選擇是在**固定的抵達時間**下取最省的那條，所以時間分根本不會變，只有燃料可能更便宜。
同 `SEED` 實測範例題目：`REVS=0` 得 90.21，`REVS=4` 得 90.43。成本 1.01 倍。

**時間 vs 燃料的換算還是要自己算**（決定該追快解還是省油解）：
時間項斜率（分/秒）除以燃料項斜率（分/(m/s)）= 「早 1 秒值多少 m/s」。
範例題目是 **1 秒 ≈ 1.21 m/s**，時間很貴。

### 一定要跑的兩條分支

官方範例題目上，這兩條差很多而且**哪條贏完全看 `k_t/C_t`**：

| 分支 | 怎麼跑 | 範例題目的結果 |
|---|---|---|
| 省油 | 預設 + `LAMBERT_MAX_REVS: 2` | 1 棒 412 m/s，但要 6,121 s |
| 快 | 加 `rules.T_MAX_SEC`（設成想要的抵達上限）| 2 棒 2,225 m/s，只要 3,185 s |

一次 `main.py` 只要 ~80 秒，**兩條都跑，把兩邊的 Score 直接比**，不要用猜的。
（`T_MAX_SEC` 會蓋掉 `4×T_A`；決定方案之後記得**改回**正式的 config 再跑最後一次。）

---

### 抄完題目先體檢（不要直接跑 main.py）

手抄 12 個六根數是整場最脆弱的一步，打錯一個分數照樣算得出來、未必看得出。抄完先跑：

```bash
uv run python check_problem.py --config configs/<今天的檔>.json
```

它用 `MissionOptimizer` 初始化（回顯工具真正會用的數值），逐項核對：

- **A、B 平面夾角** —— INC/RAAN 抄錯這個數會跟著歪（範例題目是 93.84 度）。
- **週期、遠近地點高度** —— SMA/ECC 抄錯 → 週期或高度變離譜；近地點在地表以下會亮紅旗。
- **`C_t / T_max`** —— <0.30 主攻快解、>0.70 省油解可能有戲（見第七節第 6 點）。
- **參考解該得幾分** —— 若這是範例題目，算出來要剛好 90；不是 90 就是 `k_v/C_v` 單位沒換算。

上面任何一個數字看起來不對，就是抄錯了，回頭跟題目核對，不要硬跑。

## 五、實際操作

### 1. 建 config

`configs/` 沒有進 git，當天要自己建一份。照抄這個範本改數字：

```json
{
    "orbit_A": {"SMA": 0.0, "ECC": 0.0, "INC": 0.0, "RAAN": 0.0, "AOP": 0.0, "TA": 0.0},
    "orbit_B": {"SMA": 0.0, "ECC": 0.0, "INC": 0.0, "RAAN": 0.0, "AOP": 0.0, "TA": 0.0},
    "rules": {
        "MAX_DV_MPS": 1500.0,
        "MIN_MANEUVER_INTERVAL_SEC": 100.0,
        "T_MAX_PERIOD_MULTIPLE": 4.0,
        "k_t": 0.0, "C_t": 0.0, "k_v": 0.0, "C_v": 0.0
    },
    "strategy": {"GRAVITY_DEGREE": 4, "MISS_TOLERANCE_KM": 5.0},
    "optimization": {
        "MAX_BURNS": [1, 2, 3], "MAXITER": 600, "POPSIZE": 20,
        "NUM_THREADS": -1, "MAX_EARLY_STOP": 60, "TOL": 0.01, "SEED": null
    }
}
```

`MAX_DV_MPS` / `MIN_MANEUVER_INTERVAL_SEC` / `T_MAX_PERIOD_MULTIPLE` 是規則第 2、3 節
規定的（1500、100、4×T_A），**除非當天規則說明講了不一樣的數字，否則不要動**。

### 2. 三個指令

```bash
uv run python feasibility.py --config configs/contest.json
```

```bash
uv run python main.py --config configs/contest.json
```

```bash
uv run python sweep_burns.py --config configs/contest.json --burns 1-5
```

`main.py` 會自動叫 GMAT 無頭驗證，並產生兩份腳本：`outputs/output.txt`（含求解器）
與 **`outputs/output_submit.txt`（燃燒值寫死、無求解器，繳交用這份）**。
GMAT 路徑用 `--gmat-console` 指定，或寫進 config 的 `local.gmat_console_path`。

### 3. 值得試的幾個開關

| 設定 | 什麼時候調 |
|---|---|
| `strategy.LAMBERT_MAX_REVS` | **預設 4，不用動。** 設 `0` 只在想重現 2026-08-28 之前的行為時才用 |
| `rules.T_MAX_SEC: <秒數>` | 想**強制**找快解時用，會蓋掉 4×T_A。拿來探「限定 N 秒內最省要多少」 |
| `strategy.MAX_DV_MARGIN_MPS` | **預設已是 2.0**（2026-08-29 從 10 改過來）。再往下壓實測會讓搜尋落到較差的盆地，不建議 |
| `strategy.TIEBREAK_SCORE_EPS: 0.005` | 賭官方比分數會四捨五入到小數點後兩位，用幾乎不影響分數的代價換平手判定的優先序 1 |
| `optimization.MAX_BURNS` | 先 `[1,2,3]`；`feasibility.py` 說單棒不可行就往上加 |

---

## 六、繳交前檢查清單

- [ ] `main.py` 最後印的 **違規次數 = 0**
- [ ] GMAT 驗證區塊：**命中 ✅ 成功 / Targeter ✅ 收斂 / 最後一棒 ✅ 合規**
- [ ] **固定燃燒版本**也通過驗證（「👉 命中且合規，可以直接繳交」）
- [ ] `Δr_min` < 5,000 m
- [ ] `T_team` < T_max
- [ ] GMAT 跟 Python 的落差在公尺等級。實測參考：短程直接轉移 **0.6~1.1 m**、
      多圈轉移 **11.7 m**（飛行時間越長落差越大，正常）。**差到公里等級就要查。**
- [ ] 記下 **Δr_min / ΔV_team / T_team** 三個數字——規則第 6 節同分時比這三個
- [ ] 繳交的是 **`outputs/output_submit.txt`**，不是 `output.txt`

---

## 七、已知地雷

1. **GMAT 腳本不能有非 ASCII 字元** —— 中文會讓解析器直接失敗。這個坑歷史上踩過三次。
   工具產生的腳本本來就全英文，但**如果現場手改腳本，絕對不要打中文註解**。
2. **`configs/` 沒進 git** —— 當天一定要自己建檔。
3. **分數打平不是用 Score 排名次** —— 規則第 6 節是先比 `Δr_min`、再比 `ΔV_team`、
   再比 `T_team`。工具會自動處理並印出來，但要看懂。
4. **「軌道匹配」的直覺會大幅高估成本** —— 規則只要求位置差 ≤5 km，不要求速度匹配。
   官方範例題目兩軌道面夾角 93.84°，純平面轉向要 11,120 m/s，但實際攔截只要 412 m/s。
   **不要因為看到傾角差很大就以為做不到。**
5. **Targeter 卡住的備援** —— 先看 GMAT stdout 的 `Variance`：如果第一次 nominal pass
   就已經在容許（0.01 km）內，問題不在 targeter，別亂調。真的要救，在
   `Create DifferentialCorrector DC_Targeter;` 後面插一行，其餘不動：
   `GMAT DC_Targeter.Algorithm = 'Broyden';`（擾動傳播少 6 倍）或
   `GMAT DC_Targeter.DerivativeMethod = 'CentralDifference';`（成本 2 倍，
   Jacobian 品質有問題時最有機會救）。五種演算法實測在初始猜測歪 35% 時全部收斂，
   最終 Δr 差異最大 3.1 m。細節見 `scratch_overnight/gmat/DC_ALGORITHMS.md`。
6. **先算 `C_t / T_max`，那決定哪些解的家族還活著** —— 拿到參數第一件事就算這個比值。
   官方範例題目是 `3763 / 23204 = 16%`（很緊）：時間分要拿到哪怕 1 分都得在 4,562 秒內
   抵達，所以「出去繞一圈再回來」的整個類別（雙橢圓高遠地點平面變更、長滑行多圈）
   **全部直接死掉**——最小的雙橢圓（遠地點 10,000 km）來回就要 7,715 秒，時間分 0.0000，
   就算 ΔV = 0 總分也只有 75，輸給我們的 90.43。
   **反過來，如果 `C_t` 相對 T_max 很寬鬆，那些家族就活了**，而且那正是 `coast_frac`
   非線性映射（已知的搜尋弱點，見 STATUS.md）會關住好解的地方。那種情況要特別注意
   長滑行的解有沒有被搜到，必要時手動指定 `t_wait`/coast 附近再跑一次。
7. **快解跟省油解可能是完全不同的兩個方案** —— 官方範例題目：省油解 1 棒 412 m/s 但要
   6,121 秒；快解 2 棒 2,225 m/s 只要 3,185 秒。**哪個分數高完全看當天公告的 `k_t/C_t`**，
   兩邊都跑一次再決定。

---

## 八、開賽前一晚的準備

- [ ] `git pull` + `uv sync`，跑一次 `uv run python main.py --config configs/official_sample.json` 確認整條流程通
- [ ] 跑 `uv run python tests/test_tiebreak.py` 跟 `uv run python tests/test_arc_safety.py`
- [ ] 確認 GMAT 路徑還在、`GmatConsole` 跑得起來
- [ ] 筆電充飽 + 行動電源 + 延長線 + USB 隨身碟（萬一要在他們的機器上繳交）
- [ ] 證件
- [ ] 上太空中心官網確認組別與名冊

---

## 九、賽前這幾天的規劃（風險驅動）

**規劃原則**：問「9/5 當天什麼會讓我們失敗、而現在能先拆掉？」——不是「backlog 裡哪些做得完」。
工具本身已在範例題目驗到 90.43，賽前工程的邊際價值只有兩種：**趁現在抓出會在正式題目上
咬人的潛藏 bug**，以及**確保應付得了正式題目的未知數**。

### 當天可能失敗的六個點

| # | 風險 | 緩解 | 狀態 |
|---|---|---|---|
| 1 | 手抄題目打錯 | `check_problem.py` 體檢（見第五節） | ✅ 已做 |
| 2 | 搜尋跑不完 90 分鐘 → 交白卷 | 量最壞情況 + 降級旋鈕 | ✅ 已量（第 4.1 節）|
| 3 | 潛藏 frame bug 靜默汙染答案 | z 軸旋轉不變性測試 | ✅ 已做（乾淨，無 bug）|
| 4 | 正式題目幾何弄爆工具 / 無解時空手 | 無解優雅降級 + 近共面 | ⬜ HAP-26 子集 |
| 5 | 當天部署/流程炸（GMAT 路徑、乾淨機器） | **計時彩排** | ⬜ 團隊 |
| 6 | 策略押錯（快解 vs 省油） | C_t/T_max、交換率（第四節） | ✅ 已做 |

風險 1、2、3、6 已拆掉（2、3 於 2026-09-03 完成）。剩風險 4（HAP-26，無解優雅降級）與
風險 5（團隊計時彩排，程式幫不上）——後者投報率最高。

### 賽前工程範圍（只有三項，其餘全部賽後）

照風險排序，不是照 backlog。**2026-09-03 更新：HAP-30、HAP-25 已完成，剩 HAP-26。**

1. **HAP-30 z 軸旋轉不變性**（~1h）—— ✅ **已做，基礎乾淨無 frame bug**（`tests/test_rotation_invariance.py`）。
   把 A、B 同繞地球自轉軸轉一角，分數/ΔV/抵達時間必須不變（J2/J3/J4 是 zonal，只對自轉軸軸
   對稱，所以 z 軸旋轉即使攝動開著也必須不變）。實測點質量任意旋轉 Δ≤1.9e-11、開 J 繞 Z
   Δ≤2.1e-9；並補了 `get_r0_v0` 的 RAAN 平移覆蓋（HAP-41）。
2. **HAP-25 最壞情況 runtime + 降級旋鈕**（~1–2h）—— ✅ **已量，見第 4.1 節**。順序 `MAX_BURNS
   → popsize/iters → LAMBERT_MAX_REVS` 經實測證實正確（分別 ×4.3 / ×4.1 / ×1.2）。初賽 A 是
   圓軌道（T_max 小時級）→ runtime 非問題；大 SMA／高離心率 A（T_max 天級）才要一開始就降級。
3. **HAP-26 無解優雅降級 + 近共面**（~1h）—— 確認正式題目就算很難，工具也交得出「目前最好
   （可能違規）的解 + 明確標記」，而不是崩潰或空手。

**明確排除在賽前之外**：HAP-19/20/21/22/23/27/28/31/32 全是賽後品質/回歸，不影響 9/5。
HAP-18（coast_frac）當天算 C_t/T_max 就好，無賽前工作。

### 團隊動作（最高 ROI，程式碼幫不上忙）

- [ ] **計時彩排**：兩人、完整 90 分鐘、最好在現場機器或 USB 全新環境。比上面三項工程加起來
      更能防止當天翻車。照第四節的時間表走一遍，抓部署層級的坑。
- [ ] 確認團隊真的完成報名（STATUS 留過待確認）。
- [ ] 開幕（13:30–14:00 規則說明）當場問三件事：題目 Script 用六根數還是狀態向量、用自己
      筆電還是現場機器、`k_t/C_t` 是規則說明時就公告還是在題目裡。
