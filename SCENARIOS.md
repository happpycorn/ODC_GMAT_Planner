# 測試情境一覽（含完整參數，可重建）

`configs/` 整個被 `.gitignore` 排除，所以**設定檔不會跟著 git 走**：換一台機器、或不小心
刪掉，就真的沒了。2026-08-15 換到 WSL 這台時就差點無法重建 `weird_test.json`（STATUS.md
當時只記了「A 是 SMA=150000/ECC=0.93、B 是一般 LEO」，`orbit_B` 的 INC/RAAN 完全沒寫）。

**這份文件是那些情境的唯一備份。** 下面每一組都有完整的六根數跟規則參數，照著貼成
`configs/<名字>.json` 就能重建。新增長期要用的情境時，記得同步更新這裡。

⚠️ **所有數字都是我們自己編的**，主辦方從來沒公布過任何測資。正式測資出來後，
`configs/config.json` 整組都要換掉，這裡的情境只能當壓力測試用。

共用的欄位（每組都一樣，下面不重複列）：

```json
"rules": { "MAX_DV_MPS": 1500.0, "MIN_MANEUVER_INTERVAL_SEC": 100.0, "T_MAX_PERIOD_MULTIPLE": 4.0 },
"strategy": { "MISS_TOLERANCE_KM": 5.0 },
"optimization": { "NUM_THREADS": -1, "MAX_EARLY_STOP": 40, "TOL": 0.02, "SEED": null },
"local": { "gmat_console_path": "<你這台機器的 GmatConsole 路徑>" }
```

---

## 一覽表

| 情境 | 測什麼 | 單棒可行？ | 實測難度 |
|---|---|---|---|
| `smoke_test` | 環境有沒有裝好 | ✅ 輕鬆 | 15 秒跑完全流程 |
| `playground` | **中等難度，拿來玩的** | ✅ 但合法解佔 0.4% | 50 秒跑完，Score 90.7 |
| `weird_test` | **窄窗**：合法解藏在極窄的時間縫裡 | ✅ 但只佔 0.0086% | L-SHADE 跑 2000 代都撞不到 |
| `perigee_kick` | **能量門檻**：單棒在數學上不可能 | ❌ 0 個（39,200 組實掃） | 2 棒有 174 組合法，工具找得到 |
| `hard_mode` | **能量門檻 + 稀有**：目前最難 | ❌ 0 個 | 2 棒 0 解、3 棒僅 0.11%；**工具原本找不到**，加階梯種子後解決 |
| `config` | 預設檔（數字都是編的） | — | — |

---

## smoke_test —— 環境煙霧測試

同平面圓軌道、單棒、30 代，含 GMAT 驗證約 15 秒。換機器後第一個該跑的東西，
確認 Python 環境 + GMAT 路徑 + 腳本產生 + 無頭驗證整條路都通。

```json
"orbit_A": {"SMA": 9000.0, "ECC": 0.0, "INC": 0.0, "RAAN": 0.0, "AOP": 0.0, "TA": 0.0},
"orbit_B": {"SMA": 7500.0, "ECC": 0.0, "INC": 0.0, "RAAN": 0.0, "AOP": 0.0, "TA": 0.0},
"rules":   { ..., "k_t": 0.0001, "C_t": 11000.0, "k_v": 0.005, "C_v": 1200.0 },
"strategy": { "GRAVITY_DEGREE": 2, ... },
"optimization": { "MAX_BURNS": [1], "MAXITER": 30, "POPSIZE": 5, ... }
```

參考結果：Δv 509.2 m/s、0 違規、Score 90.2，GMAT 命中 1.1 m。

---

## playground —— 中等難度（互動玩用）

難度刻意卡在中間：能量門檻只有 386.5 m/s（遠低於 1500 上限，所以**單棒就夠**、不用煩惱
多棒），挑戰來自 28 度傾角差跟相位——合法單棒解只佔搜尋空間 **0.395%**（對照
`antialigned` 的 16.5% 太簡單、`weird_test` 的 0.0086% 太難）。`T_max` 只有 16.4 小時，
全流程含 GMAT 驗證約 50 秒，適合反覆改參數試。

```json
"orbit_A": {"SMA": 13000.0, "ECC": 0.3, "INC": 28.0, "RAAN": 60.0, "AOP": 40.0, "TA": 150.0},
"orbit_B": {"SMA": 7200.0, "ECC": 0.02, "INC": 0.0, "RAAN": 0.0, "AOP": 0.0, "TA": 0.0},
"rules":   { ..., "k_t": 0.00005, "C_t": 25000.0, "k_v": 0.003, "C_v": 1500.0 },
"strategy": { "GRAVITY_DEGREE": 4, ... },
"optimization": { "MAX_BURNS": [1, 2], "MAXITER": 400, "POPSIZE": 15, ... }
```

參考結果：等 3,332s 後單棒 624.9 m/s，Δr_min 3,499.9 m、0 違規、**Score 90.67**，
GMAT 只差 0.33 m。注意這個 624.9 m/s **比網格掃出來的「最小單棒 875.7 m/s」還省**——
因為網格瞄準 A 的精確位置，而最佳化器會利用 3.5 km 命中容許範圍刻意打偏省油。

這組有個刻意留的取捨可以玩：最省 Δv 的解要等到 t≈38,000s，但那時時間分數只剩 8.5/25；
想快就得多花 Δv。調 `k_t`/`C_t` 會直接改變工具的偏好。

## weird_test —— 窄窗（長期基準）

A 是 SMA 15 萬公里、ECC 0.93 的大偏心軌道，跟 B 有 77 度相對傾角。合法單棒解**存在**
（1189.73 m/s），但藏在只佔 `T_max` **0.0086%**（約 200 秒寬）的時間窗裡，粗網格掃不到、
L-SHADE 跑 2000 代也撞不到——這組是「物理直覺初始種子」整個機制的起因。

```json
"orbit_A": {"SMA": 150000.0, "ECC": 0.93, "INC": 175.0, "RAAN": 270.0, "AOP": 333.0, "TA": 17.0},
"orbit_B": {"SMA": 6800.0, "ECC": 0.001, "INC": 98.0, "RAAN": 45.0, "AOP": 0.0, "TA": 200.0},
"rules":   { ..., "k_t": 0.000002, "C_t": 1800000.0, "k_v": 0.05, "C_v": 1200.0 },
"strategy": { "GRAVITY_DEGREE": 4, ... },
"optimization": { "MAX_BURNS": [1], "MAXITER": 1000, "POPSIZE": 20, ... }
```

**這組是長期基準，不要直接編輯它做一次性實驗**（2026-08-15 凌晨發現它被臨時測試的
`SMA=35000,ECC=0.8` 覆蓋過）。要改請另存新檔。

---

## perigee_kick —— 能量門檻（單棒數學上不可能）

B 在 6800 km 圓軌道，A 在 42164 km（GEO 高度）傾角 5 度。把遠地點抬到 A 的半徑至少需要
**2,391 m/s**，是能量下限、任何方向時機都不可能更便宜，而它超過每棒 1500 m/s 上限——
所以**合法單棒解在數學上不存在**（39,200 組 Lambert 實掃 0 命中印證）。

```json
"orbit_A": {"SMA": 42164.0, "ECC": 0.0, "INC": 5.0, "RAAN": 0.0, "AOP": 0.0, "TA": 0.0},
"orbit_B": {"SMA": 6800.0, "ECC": 0.001, "INC": 0.0, "RAAN": 0.0, "AOP": 0.0, "TA": 0.0},
"rules":   { ..., "k_t": 0.000015, "C_t": 150000.0, "k_v": 0.005, "C_v": 2600.0 },
"strategy": { "GRAVITY_DEGREE": 4, ... },
"optimization": { "MAX_BURNS": [1, 2, 3], "MAXITER": 1000, "POPSIZE": 20, ... }
```

參考結果：工具找到真實的 2 棒解（1283.0 + 1151.4 m/s，兩把都在近地點、間隔 100 秒），
總 2434.4 m/s、0 違規、Score 88.2，只比物理下限高 1.8%。GMAT 差 0.6 m。
**這組是「多棒機制確實會動」的證據。**

---

## hard_mode —— 目前最難（能量門檻 + 解極稀有）

B 鎖在 6800 km 低軌，A 的**近地點就在 50,000 km 外**（B 只能自己爬上去），再加 63.4 度傾角。

```json
"orbit_A": {"SMA": 100000.0, "ECC": 0.5, "INC": 63.4, "RAAN": 40.0, "AOP": 270.0, "TA": 0.0},
"orbit_B": {"SMA": 6800.0, "ECC": 0.001, "INC": 0.0, "RAAN": 0.0, "AOP": 0.0, "TA": 0.0},
"rules":   { ..., "k_t": 0.000004, "C_t": 780000.0, "k_v": 0.002, "C_v": 3500.0 },
"strategy": { "GRAVITY_DEGREE": 4, ... },
"optimization": { "MAX_BURNS": [1, 2, 3], "MAXITER": 1000, "POPSIZE": 20, ... }
```

實測難度：**1 棒 0 解**（能量下限 2502.5 m/s 的證明 + 51,200 組網格）、**2 棒 0 解**
（118,800 組取樣，最好的第二棒還超標 43%）、**3 棒僅 0.11%**（345,600 組裡 382 組，
最省 1100+600+1485.4 = 3185.4 m/s，攔截於 6.93 天）。

**這組是唯一一個「有已知正確答案可以衡量搜尋能力」的基準**，工具原本找不到那個解
（收斂到違規的 2924.8 m/s），加了近地點階梯種子之後才解決。

### hard_mode 的診斷變體（k_t 壓平）

同樣的軌道，只把 `k_t` 從 `4e-6` 改成 `5e-7`。用途：原始參數下合法解只贏違規解 0.35 分
（違規解只花 8.6 小時、合法解要 6.93 天，陡峭的時間分數幾乎補回 10 分懲罰），
「找不到」跟「沒誘因找」兩個失敗原因分不開。壓平後合法解改為贏 5.57 分，
用來確認失敗是**搜尋能力**而不是誘因——實測仍然失敗，證實是能力問題。

階梯種子的驗收也是在這個變體上做的：**-75.03（違規）→ -83.86（0 違規）**。

---

## 已淘汰的情境（保留紀錄，說明為什麼不留）

這兩組當初是為了測特定假設而做，實測後發現假設本身有問題，作為測資的價值不高，
檔案已刪除。參數留在這裡，需要重現當時的結論時可以重建。

### apoapsis_planechange（設計前提錯誤）

```json
"orbit_A": {"SMA": 50000.0, "ECC": 0.85, "INC": 90.0, "RAAN": 0.0, "AOP": 0.0, "TA": 210.0},
"orbit_B": {"SMA": 48000.0, "ECC": 0.84, "INC": 0.0, "RAAN": 0.0, "AOP": 0.0, "TA": 20.0},
"rules": { ..., "k_t": 0.00001, "C_t": 200000.0, "k_v": 0.01, "C_v": 1300.0 }
```

原本想用 90 度傾角差逼出「在遠地點轉平面」的教科書策略，解析算出遠地點轉向要 1202 m/s、
近地點要 13820 m/s。**但實際搜尋只花 721 m/s 就攔截成功，而且燃燒點不在遠地點**——
因為任務只要求撞到位置、不要求共軌，用平面轉向公式估成本會系統性高估。
設計計算留在 `scratch_overnight/design_apoapsis_planechange_case.py`。

### antialigned_highecc（比預期簡單太多）

```json
"orbit_A": {"SMA": 90000.0, "ECC": 0.92, "INC": 0.0, "RAAN": 0.0, "AOP": 0.0, "TA": 180.0},
"orbit_B": {"SMA": 100000.0, "ECC": 0.93, "INC": 35.0, "RAAN": 0.0, "AOP": 180.0, "TA": 180.0},
"rules": { ..., "k_t": 0.000003, "C_t": 300000.0, "k_v": 0.005, "C_v": 900.0 }
```

兩條超高離心率橢圓、不共面、遠地點指向相反（初始張角 180 度）——聽起來最刁鑽，
實測卻是**最簡單的一組**：Lambert 網格 51,200 組裡 8,464 組合法（16.53%），最小單棒
只要 299.4 m/s。原因是高離心率為了讓近地點浮出地表，SMA 被迫拉到 9~10 萬，遠地點速度
只剩 0.38 km/s——**在那裡做什麼機動都便宜，而且傳播還特別準**（GMAT 只差 0.53 m）。
設計計算留在 `scratch_overnight/design_antialigned_highecc_case.py`。

---

## 這些情境教會我們的事（設計新測資時可以直接用）

**讓問題變難的**
1. **能量門檻**——B 卡在低軌道爬不上去。可以用封閉解**證明**，不受網格解析度影響，
   是最可靠的難度來源（`feasibility.py` 的第一層檢查就是這個）。
2. **窄窗**——合法解存在但極稀有，考驗搜尋不是考驗物理。
3. **相位**——目標在偏心軌道上跑，可攔截的時機受限。

**反直覺、讓問題變簡單的**
1. **高離心率不會變難，反而變簡單**——ECC 越大遠地點越慢，那裡機動便宜、傳播也準。
2. **純位置攔截遠比軌道匹配便宜**——任兩個軌道平面必然相交於節線，Lambert 只要求抵達
   位置。任何用平面轉向公式估出來的難度都會高估（這個坑 2026-08-15 一天內踩了兩次）。

**物理天花板**
- 從最低的實用圓軌道（200 km 高度）完全逃逸也只要 **3,224 m/s ≈ 3 棒**。所以
  「B 從 LEO 出發」這個設定**最多只能逼出 3 棒左右**，想要更多棒得換機制
  （例如平面轉向另外吃預算，或 A 走雙曲線）。
