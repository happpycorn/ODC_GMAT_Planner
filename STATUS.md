# 專案狀態筆記（交接用）

給下一個 session（不管是我自己回來還是你自己看）快速抓回上下文用的，這份主要是「現在做到哪、還缺什麼、為什麼」的整理。**怎麼用這個工具看 [README.md](README.md)；演算法/物理模型原理看 [METHODOLOGY.md](METHODOLOGY.md)**；更細節的技術決策看 commit log 跟程式碼註解。

最後更新：2026-08-15（白天，在**另一台機器**上——見下面「換機器」那段）。
`improve-optimizer-and-gmat-integration` 跟 `overnight-generalization` 兩個分支
都已經 merge 回 `Master` 並刪除——**現在直接在 `Master` 上開發**。

8/14 那一整天以及 8/15 凌晨夜間開發的所有改動都已經 commit 且 push 完畢
（`cf8c316` 為止）。8/15 白天新增 `829afac`（GMAT R2026a 顏色欄位棄用修正）、
`7216d47`（多棒種子正式預算驗證 + 兩組新極限測資設計），也已經 commit。

### ⚠️ 換機器了：現在是 WSL / Ryzen 5800X，不是 MacBook Air

8/15 白天起改在 **WSL2 (Ubuntu 24.04) + Ryzen 5800X (16 threads)** 上開發，
不是先前那台會熱降頻的 MacBook Air。這份文件前面幾節提到「MacBook Air 熱降頻」
的效能討論（`sweep_burns.py` 那節、「還沒做」清單第 11/12 項）**要用這個前提
重讀**——那些「先不做，因為在會降頻的機器上不保證是淨賺」的判斷，在這台有塔扇
散熱的桌機上結論可能不一樣，值得重新評估。

這台機器的環境設定（都是機器本地、不進 git）：
- GMAT R2026a 裝在 `/home/corn/software/GMAT/GMAT/R2026a`，路徑寫在各個
  `configs/*.json` 的 `local.gmat_console_path`，不用每次帶 `--gmat-console`。
- **GMAT GUI 可以在 WSLg 上跑**，但需要兩個修補，包成
  `~/software/gmat-gui.sh` 啟動器：(a) `libtiff.so.5` 在 Ubuntu 24.04 已經沒有了，
  從 jammy 抓一份放在 `~/software/gmat-compat-libs/`，只用 `LD_LIBRARY_PATH`
  掛給 GMAT，不動系統的 libtiff6；(b) OSG 的格式外掛 (`osgdb_jpeg.so` 等) 是
  `dlopen` 進來的，而 `RUNPATH` 不會傳遞給 dlopen 的模組，所以 GMAT 的 `lib/`
  也必須進 `LD_LIBRARY_PATH`，否則貼圖/模型/字型全部載入失敗。另外 `arial.ttf`
  這類微軟字型 Linux 沒有，用 `~/software/gmat-compat-fonts/` 裡指向 DejaVu 的
  符號連結 + `OSG_FILE_PATH` 補上。`libsm6`/`libpcre2-32-0` 等系統套件要 apt 裝。
- `configs/` 整個被 gitignore，換機器不會自動帶過來，要自己重建。

**⚠️ `practice_scenario.json` 在這台機器上不存在**。這份文件底下有十幾處寫「用
`practice_scenario.json` 跑一次回歸測試確認沒壞」——那是 8/13~8/14 在 Mac 上的
標準做法，但這個檔案沒有進 git，**WSL 這台沒有**，照著做會直接失敗。這台目前有的
configs：`config.json`、`weird_test.json`、`smoke_test.json`（快速煙霧測試，同平面
圓軌道、單棒、30 代，約 15 秒含 GMAT 驗證）、`apoapsis_planechange_test.json`、
`perigee_kick_test.json`。**要做「一般規模」的回歸測試，用 `smoke_test.json` 代替**，
或是重建一份 practice_scenario（原始參數沒有完整記錄下來，這也是下面第 16 項那個
教訓的另一個實例）。

**最新一輪 (2026-08-15 白天) 做的事**，細節見「2026-08-15 白天」那一節：
- 正式預算重跑夜間的多棒 A/B 表 → 種子機制確認有效且證據更強，但「棒數越多分數
  越好」被推翻（多棒解全是 Δv=0 的退化單棒）
- 設計兩組新極限測資：`apoapsis_planechange_test.json`（否定結果，而且否定了設計
  前提本身）、`perigee_kick_test.json`（**單棒數學上不可能**，多棒在這組確實找出
  真實的兩棒合法解，結論反轉）
- **統一結論：多棒退化成單棒是正確行為，不是缺陷**——單棒夠用時退化，單棒不可能
  時就會找出真多棒解
- 修 GMAT R2026a 顏色欄位棄用警告（會影響繳交檔案）`829afac`
- 換機器到 WSL / 5800X，含 GMAT GUI 在 WSLg 上跑起來的一整套修補

**傍晚到晚上追加**，細節見「傳播器容忍度太鬆」那一節：
- 🔴 **傳播器容忍度太鬆，高離心率軌道會安靜地算錯**（使用者實測 Python 3.5km /
  GMAT 88km）→ 收緊到 `rtol=1e-12, atol=1e-9`，落差降到 0.154m
- 階梯種子在 `num_burns=2` 時產生 0 個 → 修好後該情境 67.86 → 77.76 分、違規歸零
- **正式預算 A/B 表整組重跑**：種子優勢維持 +14.45 分，但舊表裡「棒數之間差 2 分」
  被證實**整個是積分誤差**——三個有種子案例現在收斂到同一個數字
- GMAT 3D 視角兩個相機距離都改成依情境縮放 `27597dd`
- `METHODOLOGY.md` 補上 DOP853 / zonal harmonics / 種子機制的設計理由 `1feaa2b`

---

**8/13~8/14 那個 session 做的事，由前到後**（各自的細節都在下面對應章節，這裡只列
索引方便快速定位）：
1. 排位賽雙曲線軌道輸入端準備 (`config_validator.py` 放寬 SMA/ECC、`rules.T_MAX_SEC`
   覆寫) → 見「排位賽輸入端準備」
2. `strategy.USE_J2` 換成 `strategy.GRAVITY_DEGREE` (0/2/3/4)，Python 力學引擎補上
   J3/J4 → 見「重力場模型可設定」
3. 傳播器三部曲：固定 RK4 → RK45 自適應 → **DOP853 (目前主線)**，中途評估過 Encke's
   method (驗證正確但比較慢，沒有換上) → 見「傳播器：RK45...」「DOP853」兩節
4. GMAT script 新增太空船 B 追蹤視角，過程中意外抓到一個 `run_gmat_verification()`
   會誤讀殘留舊報表檔的真 bug，已修 → 見「太空船 B 追蹤視角」
5. 系統性掃描 SMA/ECC/雙曲線找出工具的可信邊界：**SMA 不是問題，ECC>0.95 才是**
   → 見「系統性測試軌道參數極限」
6. 追加：進度條的粒度問題 (使用者發現，同一天稍晚已修，見下面「還沒做」清單第 9 項)
7. 追加：`sweep_burns.py` 粗掃階段依維度公平分配世代預算 + 加時間上限，過程中
   發現 MacBook Air 熱降頻讓「順便修 n_workers」這個念頭不再是穩賺 → 見
   「sweep_burns.py 粗掃階段依維度公平分配世代預算」一節
8. 追加：`weird_test.json` 深度診斷——單棒死局分析、拆分燃燒陷阱、多棒長程解重演
   模型分岔問題、k_t/C_t 實驗、最後找到一個藏在 0.0086% 窄窗口裡的合法單棒解
   (1189.73 m/s，L-SHADE 2000 代都找不到) → 見「weird_test.json 深度診斷」一節；
   順便修了 `View_ShipBChase` 鏡頭跑進地球裡面的 bug → 同一節提到

**上一次 (8/13) 最重要的發現，這次 session 已經拆解出根因，見下面「拆解 GMAT 對不上的
真正原因」那一節**：「極端軌道」(SMA ~80,000km、ECC ~0.87) 上 Python 預測跟 GMAT 實測
差到四五個數量級、GMAT 求解器收斂不了——原本猜的三層原因中，第三層 (長時間傳播下的
模型落差) 這次 session 後段又有新進展 (J3/J4 補上去 + 意外發現固定步長比原本以為的
影響更大)，細節見下面新增的兩節，「還沒做」清單第 1/3 項已經照最新理解更新。

## 這是什麼

TASA/淡江大學辦的「第一屆軌道設計競賽」初賽用的任務規劃工具。太空船 B（我方）要在時間/燃料限制下攔截太空船 A（被動、只受重力）。`rules/` 資料夾裡有三份官方文件（正式規則 PDF ×2、0510 線上說明會簡報）。

規則重點：Δv ≤ 1500 m/s/次、機動間隔 ≥100s、T_max=4×A的軌道週期、**Δr ≤ 5km 即算成功且以內都是同分**（這點很關鍵，見下）、繳交格式是「Script + 至少模擬一次產生的 Report」上傳。初賽 A 是圓軌道；後面的排位賽/四強賽是完全不同玩法（A 變雙曲線／即時追逐戰），還沒開始準備。

**時程**（2026-08-13 查證，來源：官方簡報 + 淡江航太系官網 + 報名表單本身）：線上報名 5/11~**8/21 週五下午5點截止**（查證當下還沒截止，剩沒幾天，記得確認團隊真的報名了）；GMAT 實體訓練 6/27（已過）；**初賽原訂 8/29，因沙德爾颱風官方於 2026-08-25 來信延後一週，改為 9/5（六）**，淡江大學淡水校園（時間地點官方說近日另行寄發，要再確認）；決賽 11/09，大臺南會展中心。官方目前完全沒公布任何測試用的軌道數字或範例——`k_t/C_t/k_v/C_v` 跟 A/B 六根數都要等「題目 Script」發布才知道，詳細計分/繳交規則也還沒完整公告，要持續關注 TASA 官網。

## 這個 session 做了什麼

### 正確性
- Python 端加了含 J2 的差分修正 (`refine_lambert_burn`)，讓 Lambert 的理想解貼近 GMAT 實測，不用開 GMAT 就能高精度預覽分數
- Lambert 同時算順向/逆向，取較小 Δv（大傾角差情境影響很大，實測過省一半以上）
- 燃燒方向改球座標參數化，天生保證合規，不用事後懲罰排除
- NLP 微調加安全回退（比較微調前後 fitness，沒變好就退回，且區分「沒改善」vs「scipy 沒收斂」兩種訊息）
- **「命中容許範圍」利用**：規則只要求 Δr≤5km 就是滿分，所以最後一棒改成瞄準 A 附近容許球內最省油的點，不用死盯著精確位置（`MISS_TOLERANCE_KM`，預設 5.0，可調）
- 修了一個實測抓到的真 bug：GMAT script 的打靶原本還是瞄準 ShipA 精確位置，會把 Python 算好的「刻意打偏省油」設計悄悄蓋掉——現在改成瞄準絕對座標的瞄準點
- 安全邊界：Δv 內部軟上限 1490 m/s（真實 1500）、命中容許範圍軟上限比設定值少 1.5km（原本只留 0.1km，壓力測試發現某些軌道幾何落差可到 900m 才加大）
- **GMAT 實際收斂後的 Δv 現在會被記錄並檢查**（`FinalBurnDvMps`/`FinalBurnLegal`）——之前 `InterceptSuccess` 只看距離不看 Δv，GMAT 自己的打靶器完全可能悄悄修出一把超過 1500 m/s 的火而沒人發現，現在補上了

### 速度
- `fast_fitness_evaluator` 加 `nogil=True` + mealpy 改 `mode='thread'`，原本 `n_workers=1` 其實完全沒生效（預設 `mode='single'` 會忽略它）
- 族群大小改成跟真實決策變數維度掛鉤，舊公式在低燃燒次數時嚴重超編（180倍維度）
- 全流程實測從 373 秒壓到 40~60 秒量級（同樣的軌道規模）

### GMAT 整合
- `GmatConsole --exit --run` 自動無頭執行，`main.py` 跑完自動呼叫、自動讀報表對照，不用手動開 GUI（`--gmat-console` 換路徑，`--no-gmat` 跳過）
- GMAT script 內部：`Ship1/2`→`ShipA/B`、`OpenFramesInterface`（外掛依賴，官方電腦可能沒裝）→`OrbitView`（標準內建）、加 `ReportFile` 自動輸出 `InterceptSuccess`、修掉中文/非 ASCII 字元會讓 GMAT 解析器直接報錯的問題

### 其他
- `USE_J2` 開關（Python/GMAT 兩邊同步），`SEED` 可重現（但會自動退回單執行緒，多執行緒下亂數搶用有 race condition 沒法兩者兼得）
- `--config` 切換設定檔、`outputs/history/` 版本化、`outputs/run_history.jsonl` 執行紀錄
- `configs/config.json` 的 `orbit_A.ECC` 改成 0（照簡報說初賽 A 是圓軌道）——但 SMA/INC/其他軌道參數仍然是隨便編的測試值，等正式測資公布要整組換掉

## 這次追加的工作（同一天，config 驗證 + 多圈 Lambert 評估）

### Config 欄位驗證（`src/config_validator.py`，新檔案）
- `main.py` 的 `load_or_create_config()` 讀完 JSON 後會呼叫 `validate_config()`，把型別錯/缺欄位/物理上不合理的值 (SMA≤0、ECC 落在 [0,1) 之外、近地點在地球表面以下、`MAX_BURNS`/`MAXITER`/`POPSIZE` 等不是正整數……) 一次全部收集起來，印一份清楚列點的錯誤訊息後 `sys.exit(1)`，不會再讓打錯字的設定一路跑到 poliastro/mealpy 深處才炸出看不懂的 traceback。
- 另外對「型別合法但可疑」的值印警告但不擋執行：`MISS_TOLERANCE_KM > 5`（optimizer 會悄悄夾回 5，設的值不會真的生效）、`k_t`/`k_v` 是負的（會讓分數方向跟規則意圖相反）。
- 實測：合法設定照常跑完全流程；故意做壞的設定 (缺欄位/型別錯/ECC≥1/穿地球軌道/`MAX_BURNS` 含 0 或負數) 都各自觸發預期的清楚錯誤，`main.py --config <壞檔>` 乾淨結束、exit code 1，沒有 traceback。

### 多圈 Lambert (M>0)：評估後決定暫不實作

> **⚠️ 2026-08-28 更新：這個結論被官方公布的範例題目推翻了，多圈轉移已經實作。**
> 當時只測了兩個情境（極端傾角差、純 SMA 落差同平面），兩個都得到「M>0 救不了 / 不需要」，
> 就下了通則。官方範例題目上 `M=1, lowpath=False` 比 `M=0` **省 38%**（267.3 vs 430.1 m/s）。
> 另外當時完全沒測 `lowpath=False`——M≥1 時同一個飛行時間有兩組解，而那組省 38% 的正是
> 高路徑解。詳見本文件最後的「2026-08-28」那一節。
> **教訓：兩個情境不足以支撐一個通則，尤其當那兩個情境都是自己編的。**
STATUS.md 原本寫「大 SMA 落差的情境可能受益，但還沒驗證投報比」，這次用兩個刻意刁難的合成情境(不是真的官方測資) 實測驗證：

1. **極端傾角差** (照目前 config，A/B 傾角差到 100°)：單純比較 Lambert 轉移本身，M=1 確實常常比 M=0 省 10~20% Δv；但套用近地點合規檢查後發現，這個情境不管 M 多少，單棒直飛的 Δv 都遠遠超過 1500 m/s 上限——M>0 省下的那 10~20% 完全不夠填平缺口，救不了。
2. **純 SMA 落差、同平面** (B: SMA 7500 圓軌道，A: SMA 18000 圓軌道，INC/RAAN/AOP 全部對齊，排除掉傾角差這個干擾變因，直接對應原本的假設)：`M=0/1/2` 在整個 `[0, T_max]` 的飛行時間網格裡完全找不到合規解，`M=3` 只在一個窄窗 (~56000s 附近) 找到剛好壓線合規的解 (~1475 m/s)。單看這個結果，會覺得 M>0「解鎖」了原本 M=0 打不到的解。
   但是——這個小實驗只固定了「等待時間=0、瞄準 A 的精確位置」，沒開放真正 optimizer 擁有的兩個自由度：**t_wait 可以自由選擇出發時機**、以及**命中容許範圍可以讓 Lambert 瞄準偏移點省油**。把這個純 SMA 落差情境丟進真正的 `MissionOptimizer` 跑一次 (`MAX_BURNS=[1]`，M 依然固定 0)，最佳化器自己找到 t_wait=373.3s、單棒 Δv=1492.6 m/s、**完全合規**、Score 100/100。也就是說：M=0 配合現有的時機選擇 + 命中容許範圍利用，已經能解掉這個「特意設計來為難 M=0」的情境，不需要 M>0。

**結論**：M>0 的 Lambert 解單獨拿出來看確實常常比較省油，但把它接進真正的 optimizer 需要 (a) 處理每個 M 是否可行的邊界情況 (`izzo` 對太大的 M 會丟例外)、(b) 每次 fitness evaluation 的 Lambert 呼叫數再乘上好幾倍 (直接推翻這個 session 前半段把全流程從 373 秒壓到 40~60 秒的工作)。而目前測過的兩個「刻意刁難」情境，既有的自由度 (t_wait 時機選擇、多棒自由方向噴射、命中容許範圍瞄準偏移) 都已經足夠達到 100 分，看不出 M>0 會多解鎖什麼。**決定跟下面清單裡的「固定 60 秒 RK4 步長」「物理直覺初始種子」一樣先不做**，等正式測資公布後，如果真的出現這兩個合成情境沒設想到的幾何 (才知道是什麼樣子)，再回頭考慮。
實驗腳本沒留在 repo 裡 (純粹一次性驗證，不是可重用工具)，這裡的數字是實際跑出來的記錄。

### GMAT script 裝飾用參數潤飾
- `DragArea`/`SRPArea` 原本是 15/1 (SRP 受光面積比阻力面積還小，物理上不太合理)，改成自洽的 6/8。`ForceModel` 的 `Drag=None`、`SRP=Off`，這些欄位本來就是裝飾用不影響結果，純粹是改得更像樣。
- 在 script 裡加了英文註解，講清楚 `DryMass`/`Cd`/`Cr`/`DragArea`/`SRPArea`/`Isp`/`GravitationalAccel` 為什麼是裝飾用 (Drag/SRP 關閉、`DecrementMass=false`)，不用下次再重新想一遍。
- **過程中抓到自己的一個回歸**：第一版註解寫成中文，直接會重踩這個分支前面已經修過的雷 (`887e64e`：非 ASCII 字元讓 GMAT 解析器直接報錯)。改成跟其他註解一致的純英文，並且實際掃過產生出來的 `outputs/output.txt` 確認 0 個非 ASCII 字元，再整套跑一次含 GMAT 驗證確認腳本還是能正常解析/收斂 (`InterceptSuccess`/`FinalBurnLegal` 都是 true) 才收工。

### tqdm 進度條修復 + console 輸出簡化
- **根因**：`_optimize_burn_case`（跑在 `ProcessPoolExecutor` 的子行程裡）直接呼叫 `print`/`tqdm.write`，但外層進度條活在主行程——子行程不知道進度條的游標位置，好幾個子行程各自時間點搶著寫同一個終端機，跟主行程的 `\r` 覆寫互相打架，導致進度條沒辦法原地更新，越印越往下（使用者回報的症狀）。
- **修法**：子行程改成只回傳資訊 (`epochs_run` + 一個簡短備註字串)，所有 print 都收斂到主行程做，讓 tqdm 全程只在單一行程裡運作。用 `cat -vet` 看過原始字元確認整段進度條重繪現在是完整不被打斷的 `\r` 序列。
- 順便把使用者覺得「印太多」的搜尋階段雜訊砍掉：3 條「核心啟動」併成 1 條、每個推進次數完成從 2 行併成 1 行、拿掉每次分數進步就喊一次的「發現新最佳解」（改成搜尋結束後只報一次最終選了哪個方案）、兩段計算時間分隔線併成 1 行、拿掉跟 GMAT 驗證結果重複的提醒。任務清單/最終分數/GMAT 驗證這些真正要看的輸出完全沒動。
- 實測：全流程含 GMAT 驗證重跑一次，分數/Δv/T_team 跟改之前完全一致 (100/100, InterceptSuccess ✅)，確認只是砍雜訊沒動到邏輯。

### 把規則規定的 3 個數字搬進 config
翻 `Regulations_PrelimRound-20260605.pdf` 對照程式碼，發現 `ΔV_lim`（1500 m/s）、機動間隔下限（100s）、`T_max` 的週期倍數（4 倍）這三個規則數字是寫死在 `optimizer.py` 裡，沒有跟旁邊的 `k_t/C_t/k_v/C_v`（同樣是規則數字）放在一起。新增三個 config 欄位：`MAX_DV_MPS`、`MIN_MANEUVER_INTERVAL_SEC`、`T_MAX_PERIOD_MULTIPLE`，預設值等於現在初賽規則的數字，`config_validator.py` 也加了必填 + 值域檢查（必須是正數）。
- 動機：規則第 7 節說晉級賽「會有更具挑戰性的情境與動態環境條件」，但這份 PDF 只涵蓋初賽，沒寫這三個數字會不會變——不確定，但不管會不會變，這樣改都是合理的架構收斂：晉級賽如果規則數字真的不一樣，改 config 就好，不用再回來翻 `optimizer.py`。
- 實測：`validate_config` 對三個新欄位的必填/值域檢查都如預期觸發；直接 instantiate `MissionOptimizer` 塞不同的數字進去，確認 `self.MAX_DV`/`self.MIN_COAST_TIME`/`self.T_max` 三個屬性都正確反映 config 的值（不是巧合等於預設）；用預設值跑一次完整流程 (含 GMAT)，分數/Δv 跟改之前完全一致，確認沒有把初賽這一輪跑壞。

### config.json 分組整理（使用者反映參數變多、有點亂）
上面幾輪陸續加欄位後，config 頂層累積了 12 個欄位（`orbit_A`/`orbit_B`/`optimization` 三個巢狀物件 + 9 個攤平的純量欄位），改成 4 個頂層區塊，依「誰決定這個數字」分組：
- `orbit_A` / `orbit_B`：軌道六根數，不變
- `rules`：主辦方規定/公告、我們不能改的數字 —— `MAX_DV_MPS`/`MIN_MANEUVER_INTERVAL_SEC`/`T_MAX_PERIOD_MULTIPLE`/`k_t`/`C_t`/`k_v`/`C_v` 全部搬進來
- `strategy`：我們自己的任務設計選項，不是規則要求 —— `USE_J2`/`MISS_TOLERANCE_KM` 搬進來
- `optimization`：純演算法搜尋設定，不變

改動範圍：`configs/config.json`、`main.py`（`DEFAULT_CONFIG` + `append_run_history` 記錄的欄位）、`src/optimizer.py`（`MissionOptimizer.__init__` 改讀 `config["rules"]`/`config.get("strategy", {})`）、`src/config_validator.py`（拆成 `_validate_rules`/`_validate_strategy` 兩個新函式）、`README.md` 欄位表。
- 實測：新結構的 config 通過驗證；故意拿掉整個 `rules`/`strategy` 區塊、`strategy.USE_J2` 型別錯、`rules.k_t` 是負的（軟性警告）都如預期觸發；設定檔不存在時自動生成的預設範例也是新結構，且能自己通過驗證；用不變的正式 config 跑一次全流程 (含 GMAT)，分數/Δv/T_team 跟改之前完全一致 (100/100, InterceptSuccess ✅)；順便確認 `run_history.jsonl` 記錄的是完整的 `rules`/`strategy` 物件，不是拆散的欄位。

### 新增 METHODOLOGY.md（拆分文件：怎麼用 vs 怎麼算的）
使用者反映應該把「怎麼用」跟「怎麼算的」拆成兩份文件。`README.md` 本來就幾乎全是「怎麼用」的內容（安裝/config/執行/輸出/提交前檢查），不用大改；新寫了 [METHODOLOGY.md](METHODOLOGY.md)，把散落在程式碼註解跟這份 STATUS.md 裡的技術知識整理成一份對外可讀的說明：問題設定、整體流程、物理模型 (RK4+J2)、決策變數編碼 (球座標參數化)、Lambert 攔截 + 命中容許範圍利用、安全邊界設計、L-SHADE/L-BFGS-B 最佳化、計分公式、GMAT 驗證流程 (含 aim-point sync 那個 bug 的故事)、已知限制。兩份文件互相加了連結。
- 寫的時候把引用的具體數字都回頭對照過原始碼/STATUS.md 抓錯了一個：族群大小超編寫成「快 200 倍」，實際是「180 倍」(360 個體 / 2 維)，已修正；其他引用數字 (108m 積分誤差、863m 命中容許壓力測試落差、17m 理論最差 Achieve 誤差) 都對照過原文確認無誤才留著。
- 副作用：這份文件剛好也對得上規則第 6 節「設計理論」平手加賽的要求（同分要上台講 5 分鐘軌道設計方法論），晉級賽如果真的平手用得到。

### 新增 sweep_burns.py：掃描一個情境需要燒幾次
起因：用 `configs/practice_scenario.json`（自己編的中等難度練習情境，不是官方測資，用來模擬跑一次比賽的感覺）實測發現「燃燒次數越多、搜尋時間越長」——族群大小是決策變數維度 × POPSIZE，維度隨燃燒次數線性長，但 `MAXITER`（世代數）不會跟著長，導致高燃燒次數案例在同樣代數預算下天生吃虧。實測驗證過這個效應是真的：`MAX_BURNS=[1..6]` 時 6 次燒 (`MAXITER=1000`) 目標值 -99.9863，明顯輸 2 次燒的 -99.9941；把 `MAXITER` 拉到 3000 (3倍)，6 次燒追到 -99.9952，反而小幅超過——證實純粹是預算不夠冤枉了它，不是本質上比較差。

新增 [`sweep_burns.py`](sweep_burns.py)，把這個結論實作成兩階段流程：
1. **粗掃**：`MAX_BURNS` 開一個寬範圍（預設 1-6），`MAXITER` 刻意調低（預設 300），快速找出分數大概從哪個燃燒次數開始不再明顯進步（`find_elbow`）。這階段的數字不能直接當結論，只能抓候選範圍。
2. **精細驗證**：只針對候選範圍附近（`--window` 控制往上延伸幾格），用 config 原本的 `MAXITER`（使用者已經調過、信任的預算）重新跑一次「公平」比較，這一步的數字才拿來下結論。

`MissionOptimizer` 加了 `self.burn_case_results` 屬性（`run_study()` 完成迴圈裡順手記錄每個燃燒次數的 fitness/代數/備註），純增量、不影響 `run_study()` 原本的回傳值，`main.py` 的正常流程不受影響。

值得注意的細節：工具判斷「已經打平」用的容忍度（`--plateau-tol`，預設 0.05 分）是主觀取捨——在 Δv/時間預算寬鬆的情境下（像 practice_scenario 這種），連 1 次燒都能拿到 99.98 分，容忍度一寬就會建議「用最少的那個就好」，但**分數打平不代表 Δr_min/ΔV_team/T_team 這些平手判定用的原始數字也打平**（規則第 6 節：先比 Δr_min，再比 ΔV_team，再比 T_team），這點在工具的結論輸出裡有特別提醒，正式方案還是要回頭看 Mission Plan 的細節數字，不能只看建議就定案。
- 實測：對 `practice_scenario.json` 跑過完整兩階段（`--burns 1-6`）跟縮小範圍的快速版（`--burns 1-3 --coarse-iters 150`），兩次都正確跑完、印出趨勢表跟建議，`--output-config` 能正確把建議寫成新的 config 檔。

### 新增「固定燃燒版本」的繳交腳本 (`outputs/output_submit.txt`)
起因：使用者擔心一般版本最後一棒靠 GMAT 的 `Target/Vary/Achieve`（DifferentialCorrector）即時求解，換到比賽當天主辦單位的電腦上執行時，求解器的收斂行為可能跟我們自己測的不一樣。討論後確認方向：**不是拿掉求解器，而是把求解器已經找到的答案變成常數**——先用一般版本 + GMAT 的 DC 把正確答案找出來，驗證乾淨通過後，把 GMAT 自己收斂出的最後一棒 VNB 分量（不是 Python 的估計值）直接寫死，產生一份完全不含任何求解器的新腳本，單純傳播 + 施加燃燒。

改動：
- `script_generator()` 加 `final_burn_fixed_vnb`/`output_filename` 兩個參數，`None`（預設）走原本的 DC 路徑；給值就走固定路徑（最後一棒跟其他棒一樣直接套用，不進 `Target/Vary/Achieve`）。共用同一份函式，只有 Burns 區塊跟 Mission Sequence 的最後一段分支，其餘（Spacecraft/ForceModel/Propagator/OrbitView/ReportFile）完全共用，避免兩份重複的樣板。
- `Report_Intercept` 多加三欄（最後一棒的 `Element1/2/3`），讓 `main.py` 讀回 GMAT 實際收斂後的燃燒向量。
- `main.py`：一般版本驗證乾淨通過（成功+收斂+合規）後，自動用讀回來的向量產生 `outputs/output_submit.txt`，**再送一次 GMAT 驗證**確認這份「重新單純傳播」出來的結果站得住腳，兩者印出來對照。新增 `--no-fixed-script` 可以跳過這一步（省幾秒，開發迭代時用）。`append_run_history` 多記一個 `fixed_script_verified` 欄位。
- 實測：`configs/practice_scenario.json` 跑一次完整流程，一般版本 MissDistance 3984.786m，固定版本重新驗證出來是 3985.204m——只差 0.4m，證實「寫死 GMAT 收斂後的值再重跑」確實能重現幾乎一樣的結果；另外確認固定版本產生的 script 裡完全沒有任何 `Target`/`Vary`/`Achieve`/`DifferentialCorrector` 實際指令（只有註解提到），也確認 0 個非 ASCII 字元（沒有重踩 `887e64e` 那個雷）。

### 清掉沒用到的重量級依賴 + 加「部署到新電腦」的說明
起因：使用者問「這個要部署到一台全新電腦要怎麼做」——查 `pyproject.toml` 發現 `torch`/`optuna`/`pymoo`/`line-profiler` 四個依賴，整個 repo (`src/`、`main.py`、`sweep_burns.py`) 完全沒有 import 過，應該是早期實驗階段（`GPU_Trial` 分支、舊版 `optuna_optimizer.py`）留下的殘留，這兩個都已經在前面的 session 清掉了，但 `pyproject.toml` 沒有跟著清。`torch` 又特別重，`uv lock` 重新解析後移除了一整串 NVIDIA CUDA 函式庫（`nvidia-cublas`/`nvidia-cudnn`/`nvidia-cusolver`...）、`sqlalchemy`、`sympy`、`networkx` 等一堆間接依賴——這台完全沒有 GPU 的機器上根本用不到。
- 改動：`pyproject.toml` 的 `dependencies` 從 7 個砍到 3 個（`astropy`/`mealpy`/`poliastro`，`numpy`/`numba`/`scipy`/`tqdm` 是這幾個的間接依賴，不用列）；重新 `uv lock`；README 補了「Python 3.8+」跟 `pyproject.toml` 實際要求的 3.12+ 對不上的錯誤，順手修正。
- 實測：清完後跑 `configs/practice_scenario.json --no-gmat` 完整流程一次，確認 tqdm（雖然被移除出直接依賴，但透過 mealpy 間接帶進來）跟其他套件都還在，沒有 ImportError，結果正常。目前 `.venv` 大小 1.1GB（清之前含 torch+CUDA 套件應該大好幾倍，沒留清之前的數字對照，但移除的套件清單看得出差很多）。
- README 新增「🖥️ 部署到一台全新電腦」章節，分兩種情況講清楚：(A) 要在新電腦跑整套設計工具（`configs/` 被 gitignore 排除、GMAT 要另外裝、`--gmat-console` 一定要蓋掉寫死的預設路徑）；(B) 比賽當天主辦單位的電腦——**其實不需要部署這整套東西**，`outputs/output_submit.txt` 本身就是一份純文字 GMAT script，直接帶去在對方的 GMAT 裡開檔執行就好，不需要 Python/uv/這個 repo 的任何程式碼。這個區分本身也是「固定燃燒版本」那個功能存在的意義。

**⚠️ 這次清理留了一個真的會炸的回歸，之後補上了（見下一節）**：上面「實測」那句寫的「tqdm 透過 mealpy 間接帶進來」是錯的——當時本機的 `.venv` 是清理前就裝好的舊環境，`uv run` 沒有重新驗證每個 import 是不是真的能被目前的 lockfile 滿足，所以在自己機器上測不出問題。隊友在全新電腦上 `uv run main.py` 直接 `ModuleNotFoundError: No module named 'tqdm'`。教訓：**改完依賴之後，光靠既有 `.venv` 測不出「全新環境裝不裝得起來」這種問題，得真的刪掉 `.venv` 重來一次**，或者請別人在全新環境上實測。

### 修 tqdm 遺漏（上面那個回歸的修復）
`src/optimizer.py` 直接 `from tqdm import tqdm`，但 `tqdm` 從來沒被列進 `pyproject.toml` 的 `dependencies`，一直是「賭它會被間接帶進來」。這次清理把 `torch`/`optuna`/`pymoo`/`line-profiler` 拿掉之後，間接依賴的解析結果變了，`tqdm` 完全沒被解析進 lockfile——不是只有回報的那位隊友的環境特殊，是**這次清理之後所有全新安裝都會炸**。
- 修法：把 `tqdm` 明確加進 `pyproject.toml` 的 `dependencies`（本來就該這樣，直接 import 的東西不該賭它靠間接依賴活著）。
- 實測：這次真的刪掉本機 `.venv` 重來，`uv lock` 印出 `Added tqdm v4.70.0`、`Added colorama v0.4.6`（tqdm 在 Windows 上的顏色套件依賴）——證實清理前 `tqdm` 真的完全沒被解析進來，不是我看錯。重新從乾淨環境跑一次完整流程確認正常。
- 因為是會擋住別人立刻沒辦法動的回歸，這次直接 commit + push，沒有照平常「累積幾個 commit 再問要不要推」的節奏。

### 新增 `local.gmat_console_path`（選填的 config 欄位）
起因：使用者發現 `GMAT_CONSOLE_DEFAULT` 寫死在 `main.py`（被 git 追蹤），換一台電腦/換一個人開發，這個值一定要改，但改了又變成本地修改污染 git、容易在 pull/merge 卡住。既然 `configs/config.json` 本來就被 gitignore 排除，機器相關的設定放在那裡才是一致的做法，不該混進 `main.py`。
- 新增 config 第五塊（選填，不在 `top_required` 裡）：`local`，目前只有 `gmat_console_path` 一個欄位。`config_validator.py` 加 `_validate_local`（型別檢查：必須是字串，不強制要有這個區塊）。
- `main.py` 的 `--gmat-console` 解析優先順序：CLI 參數 > `config["local"]["gmat_console_path"]` > `GMAT_CONSOLE_DEFAULT`（最後備援，就是我這台機器的路徑）。
- 實測：故意在 config 裡塞一個錯的路徑（不帶 `--gmat-console`），確認真的印出「找不到 GmatConsole (那個錯路徑)」的警告，不是悄悄退回寫死的正確路徑；換回正確路徑、完全不帶 `--gmat-console`，確認 GMAT 驗證正常跑完；最後確認 `--gmat-console` 給錯路徑時還是蓋得過 config 的正確值——三層優先順序都驗證過。

### 順手驗證：USE_J2 關掉，Python 端跟 GMAT script 是不是真的同步
使用者問的，追蹤程式碼確認 `strategy.USE_J2` → `optimizer.USE_J2`/`J2_VAL`（Python 端傳播）→ `main.py` 兩處 `script_generator()` 呼叫（一般版本+固定版本）都帶 `use_j2=optimizer.USE_J2` → GMAT script 的 `GravityField.Earth.Degree/Order` 跟著變 0/0。重力場開關那段是共用模板，不在 DC/固定版本的分支邏輯裡，兩種腳本都會同步。實測：`USE_J2=false` 跑一次，`outputs/output.txt` 裡 `Degree`/`Order` 確認都是 0，GMAT 驗證照樣成功。結論：**兩邊確實同步，沒有問題**。

### 修 `outputs/` 資料夾不存在的崩潰（隊友回報）
跟 tqdm 那個回歸同一個病因：`outputs/*` 整個被 `.gitignore` 排除，全新 `git clone` 下來這個資料夾根本不存在 (git 不會建空資料夾)，`script_generator.py` 寫 `outputs/<file>` 之前沒有 `os.makedirs("outputs", exist_ok=True)`（只有 `outputs/history/` 那層有建，但建的時機已經在第一次寫檔失敗之後）。這台機器測不出來是因為 `outputs/` 資料夾在這個 session 開始前就已經存在（裡面還有更早、跟這個專案無關的舊檔案）。
- 修法：`script_generator()` 開頭補上 `os.makedirs("outputs", exist_ok=True)`。
- 實測：這次真的把本機的 `outputs/` 整個資料夾搬到別的地方（模擬「完全沒有」的狀態），確認能正常建立資料夾並寫檔，測完把原本累積的 session 紀錄搬回來。
- commit `713523b`，因為會擋住隊友下一步，直接 push。

### 找不到 GmatConsole 的警告訊息，補上 `local.gmat_console_path` 的提示
`DEFAULT_CONFIG` 刻意不生成 `local` 區塊（沒有通用預設路徑），所以隊友全新安裝跑出來的警告只提了 `--gmat-console`/`--no-gmat`，沒提到這個新選項——得自己去翻 README 才知道。改成警告訊息裡直接印出要加的 JSON 片段。commit `cf94da7`。

### 🔴 極端軌道測試：一個危險改動的完整故事 (加了又撤銷)
使用者故意想測極限情境（`orbit_A`: SMA=80000km, ECC=0.87, INC=90°——遠地點快 15 萬公里，接近月球距離）。過程分四階段，**第三階段那個改動已經被撤銷，只留第四階段的修復**：

1. **一開始「跑不出結果」**：其實不是壞掉，是真的在算，只是慢——`T_max=4×週期` 隨 SMA 三次方成長，這組軌道的 `T_max` 高達 90 萬秒 (10.4 天)，而 RK4 固定 60 秒步長代表單次傳播要走 1.5 萬步，比一般測過的情境 (`T_max` 幾萬秒等級) 貴 10~35 倍。
2. **危險的「修復」(已撤銷)**：把 `dt` 改成跟 `T_max` 成比例放大 (`max(60.0, T_max/1600.0)`)，讓極端情境的步數回到跟一般情境差不多——**這個改動上了 commit `ebaf728`、也 push 了**，速度確實從 150+ 秒壓到 14 秒，一般情境 (`practice_scenario.json`) 的回歸測試也過了。
3. **但這個改動是錯的**：因為步長是跟 `T_max`（整個任務的時間尺度）綁定，沒考慮到中途一次機動可能製造出一個近地點很低、局部速度很快的軌道——那一段用「整體平均」算出來的粗步長積分，RK4 會發散。實測抓到：某組候選解的 `r_curr` 在傳播中飆到 `9.88×10^10 km`（比太陽系還大 600 多倍），完全是數值爆炸，餵進 Lambert 求解器後不是崩潰就是算出一個「看起來合法但其實是垃圾」的解。**用 `git revert` 撤銷了 (`91bd68f`)，已 push。極端軌道重新變回慢，但至少不會再算出假答案。**
4. **順便修的另一個問題**：`izzo` (Lambert 求解器) 內部的 Householder 疊代對退化幾何會丟 `RuntimeError("Failed to converge")`，`fast_fitness_evaluator` 原本沒接住，一個候選解踩到就讓整個 `model.solve()` 當掉，白白浪費掉那個燃燒次數案例已經算完的搜尋結果 (實測：3 個案例裡 2 個因此報廢)。這個問題不是新引入的，是本來就有、只是沒有情境慢到能撞見過。用 numba 的 `try/except` 接住 (驗證過 numba 能正確 catch 巢狀 njit 函式丟出的例外，不會退回 object mode)，兩個方向都失敗就當作這組候選解是 0 分，讓 L-SHADE 自然淘汰它，不要拖垮整次搜尋。commit `62f8d6c`，已 push。**這個修復是對的、有留著。**

**修復後重新完整測過兩組極端情境（含 GMAT 驗證），結果**：

| | orbit_A 極端 (B 正常 LEO) | orbit_A + orbit_B 都極端 |
|---|---|---|
| 崩潰？ | ✅ 沒有 (3 案例都跑完) | ✅ 沒有 (3 案例都跑完) |
| 耗時 | 29.1 分鐘 | 26.8 分鐘 |
| Python 預測 Δr_min | 3488m（判定成功） | 3496m（判定成功） |
| **GMAT 實際 Δr_min** | **50,229,818 m** | **25,401,794 m** |
| GMAT Targeter | ❌ 未收斂 | ❌ 未收斂 |

**結論（很重要）**：崩潰/數值爆炸的問題確實修好了，但底下曝露出一個更深、跟這次改動完全無關、原本就存在的限制——**這個工具目前只在「一般規模」軌道（大概到中高軌道 MEO 等級）驗證過**。SMA 大到 80,000km、ECC 高到 0.87 這種近月距離、高離心率的極端軌道，Python 端的簡化模型 (二體+J2、`refine_lambert_burn` 的牛頓修正) 算出來的答案跟 GMAT 真實模擬差了四五個數量級，連 GMAT 自己的 DC 都收斂不了——不是安全邊界不夠寬 (那是差幾百公尺等級的問題)，是模型本身的假設在這個尺度下完全站不住腳。**還沒找出精確的「能信任」跟「不能信任」的軌道規模分界線在哪，也還沒修——這是下面「還沒做」清單的新增第一優先項。**

用到的兩組測試 config 沒留在 repo 裡（純粹一次性診斷，存在對話紀錄裡，不是 repo 裡的檔案）：
```
情境一 orbit_A: SMA=80000, ECC=0.87, INC=90, RAAN=5, AOP=5, TA=5 / orbit_B: SMA=7500, ECC=0, INC=0, RAAN=0, AOP=0, TA=0
情境二 orbit_A 同上 / orbit_B: SMA=80000, ECC=0.87, INC=0, RAAN=5, AOP=5, TA=5
```

## 這個 session 做了什麼（2026-08-14 凌晨，接續 8/13 晚間那次的極端軌道發現）

使用者要求深入查「極端軌道測試」那一節發現的問題，不是先猜答案，用受控實驗一層一層拆。

### 拆解「GMAT 對不上」的真正原因：三層，不是原本猜的「模型在這個尺度失準」那麼簡單

**第一層：計分公式的實際規則，工具原本就照規則實作，不是 bug**
讀了官方規則 PDF (`rules/Regulations_PrelimRound-20260605.pdf_...pdf` 第 5 節) 才確認：
超過 Δv 上限的燃燒，官方規則就是「固定扣 10 分/次違規燃燒，扣到 0 分為止」，**不是取消
資格、也不是依超標倍數扣分**。`src/scorer.py` 的 `penalty_count * 10.0` 完全照規則實作。
含義：搜尋找到一個「違規但快」的解、分數贏過「合法但慢」的解，這是規則本身允許（甚至
在分數上更優）的策略，不是搜尋演算法選錯。實測驗證：情境一極端軌道上，一個 48,561 m/s
（超標 32 倍）的單棒解拿 58.6 分，贏過一個假設的合法解 (2900 m/s，慢很多) 的 51.3 分。

**第二層：GMAT DC 收斂失敗的直接原因——`Vary` 邊界寫死卡在合法 Δv 範圍**
[script_generator.py:122](src/script_generator.py#L122) 的
`Vary DC_Targeter(... {Lower = -max_dv, Upper = max_dv, ...})` 把搜尋空間鎖在
±1500 m/s。如果 Python 認為真正需要的燃燒本來就超過這個範圍 (像上面 48,561 m/s 那個
解)，DC 不管怎樣都碰不到那個值，一定收斂失敗，報表印出的是「卡在邊界上的垃圾值」，不是
「模型不準」的訊號。受控實驗證實：把 Python 自己算出的燃燒值直接繞過 DC 寫死進 script
(固定燃燒版本)，GMAT 的真實傳播結果跟 Python 預測只差 **1.8 公尺**（即使這棒是
48,561 m/s 這種誇張的燃燒）——證明 Python 的二體+J2 模型本身在這個案例裡完全沒問題，
問題出在「透過 GMAT DC 求解」這條路線在超標時走不通，不是物理模型錯了。

**第三層（還沒解決，是目前最優先的未解問題）：長時間傳播下，模型確實會分岔**
把「最後一棒超標直接 0 分淘汰」硬套進搜尋 (monkeypatch 過的 fitness function，逼搜尋
只能選合法解) 重新跑情境一，這次真的找到一個完全合法的 4 棒解 (ΔV_team=2606 m/s，全部
在 1500 m/s 內)。這次 GMAT 的 DC **真的收斂了**（最後一棒 916 m/s，合法），但因為這個
合法解需要用掉幾乎全部的 T_max (896,537 秒 ≈ 10.4 天，A 繞了快 4 圈)，GMAT 實測
Δr_min 是 **481,496 公尺 (481.5 公里)**，Python 預測卻只有 1257.5 公尺。拆解後確認：
不是最後一棒瞄準的問題 (DC 已經精準命中 Python 給的瞄準點)，是 **Python 自己對「A (被動
目標) 在 89 萬秒後會在哪裡」的預測，跟 GMAT 用更高精度模型算出來的實際位置，差了 480
幾公里**。另外測過純步長效應 (`dt=60s` vs `dt=0.1s`，同一個 Python 模型內部比較，不牽
涉 GMAT)：對同一段 T_max 傳播只造成 **2.1 公里**誤差——遠遠不夠解釋 481 公里的落差。
主因很可能是 [script_generator.py:193](src/script_generator.py#L193) 裡 GMAT 用
`Degree=4, Order=4` (J2~J4 都算)，但 [core_math.py:69](src/core_math.py#L69) 的 Python
端只實作純 J2——這個落差在短轉移可以忽略，累積 10 天、多次近地點通過 (每次都是相位誤差
最敏感的地方) 後被放大成公里等級的位置誤差。

**修正對「極端軌道」問題的理解**：原本以為問題跟 SMA/ECC 的數值大小有關；現在看起來真正
的變數是**這個解需不需要用掉接近 T_max 的傳播時間、繞行目標軌道好幾圈**——同一個極端軌
道，快速方案 (幾百秒) 精準吻合，被迫拉長的合法方案 (10 天) 卻對不上。也就是說**任何場
景**(不限於刻意找的極端 SMA/ECC) 只要被 Δv 預算逼著繞行目標好幾圈，理論上都可能踩到
同一個問題——只是「一般規模」軌道通常不需要繞這麼多圈就能湊到合法解，還沒被測出來過。
下面「還沒做」清單第 1 項已經照這個新理解改寫。

### 修復：讓工具能誠實產生「命中但違規」的可繳交方案

前面三層原因合起來代表：如果一個違規解才是規則框架下分數最高的方案，工具應該要能幫忙產生
一份可以繳交、可以重現的 script，而不是讓它在 DC 收斂失敗時直接消失、什麼都拿不到。

改動：`main.py` 的「固定燃燒版本」現在有兩種燃燒值來源
([main.py:329](main.py#L329) 附近)：
- `gmat_dc` (原本就有)：一般版本的 DC 乾淨收斂 (命中+收斂+合法) 時，用 GMAT 自己收斂
  出的值，最可信。
- `python_fallback` (新增)：DC 沒有乾淨通過時 (沒收斂、命中失敗，或理論上的超標——雖然
  邊界卡死代表 DC 自己收斂出的值不可能超標)，改用 Python 自己 (`refine_lambert_burn`)
  算出的值，繞過 DC 直接寫死進 script 驗證，誠實印出命中距離跟真正的 Δv 是多少。

`outputs/run_history.jsonl` 的 `fixed_script_verified` 多了 `source` 欄位記錄用了哪一
種。`script_generator.py` 的 `final_burn_fixed_vnb` 文件說明也更新，講清楚這個值現在
不保證合法。

實測過三種情境 (皆為真正跑過 `main.py` + 真實 GMAT，不是模擬)：
1. 情境一極端軌道 (48,561 m/s，超標 32 倍)：DC 收斂失敗 → 自動 fallback → GMAT 驗證
   命中成功 (3516m)，正確標示超標，印出規則第 5 節「扣 10 分不是取消資格」的提醒。
2. `practice_scenario.json` (一般規模，非刻意刁難)：最後一棒自然收斂到 1500.3 m/s
   (剛好超標一點點，見下面設計筆記) → DC 命中失敗 (GMAT 5634~5746m，超出 5km 門檻) →
   fallback 老實印出「這組解本身站不住腳」——不是隨便都能靠 fallback 救回來，這裡正確
   反映了失敗。
3. `clean_dc` 分支的判斷式 (原本的三條件 if) 跟 refactor 後逐條件比對過，行為沒變。

### 設計筆記：最後一棒不設 Δv 上限是刻意的，不是要修的 bug

這個 session 一開始把「最後一棒沒有 Δv 硬上界」當成一個該修的缺陷 (提案：比照中間棒或
撞地球，超標直接 0 分淘汰)。**使用者的反饋糾正了這個方向**：最後一棒的任務是「一定要
命中」，Δv 大小是命中之後才由分數去判斷值不值得——如果把它也硬夾進合法範圍，Lambert
可能根本找不到解、直接打不中 (distance 那 50 分直接歸零)，比超標扣 10 分嚴重得多。
**這個「不設上限」是刻意的設計，維持現狀，不要改。**

（測試過程中也意外發現：即使在 `practice_scenario.json` 這種一般規模、沒有刻意刁難的
場景，最後一棒也會自然落在 1500.3 m/s 這種「剛好超標一點點」的邊界值——因為只有中間棒
有 `MAX_DV_SOFT` 這個軟上限，最後一棒完全沒有。這不代表工具壞了，是上面這條設計原則
自然的結果，往後看到類似情況不用當成新 bug 重查。）

## 排位賽輸入端準備（2026-08-14 清晨）

使用者提到「排位賽環境」要開始納入考慮，因為主辦方過去公告資料一直很少、很晚，怕拿到
測資才準備會來不及。查了簡報檔第 9 頁跟中文版規則 PDF，排位賽目前唯一公開的資訊：

- **目的**：決定前四強進決賽。
- **機制跟初賽幾乎一樣**：主辦方設定 A/B 軌道參數，B 在時間+燃料限制下攔截 A，一樣是
  Script+Report 上傳流程。
- **唯一明確差異**：**Spacecraft A 改成雙曲線軌道飛越地球**（不再是圓/橢圓封閉軌道）。
- 詳細計分規則「擬定後將公告」——**目前還沒公布**。

（四強賽是完全不同的東西：排位賽前二搶金牌、三四搶銅牌，比賽當天抽籤決定追逐/躲避方，
雙方在會場公用電腦即時下指令、電競直播模式，不是 Script+Report——這次沒有動這部分，
現有的離線最佳化+腳本產生架構完全不適用，需要的話是另一個量級的工作，之後再說。）

雙曲線軌道對現有架構是結構性問題，改了兩處讓工具至少不會直接拒絕載入/算出 NaN：

1. **`src/config_validator.py`**：`_validate_orbit` 原本只接受 `0<=ECC<1` 搭配
   `SMA>0`（橢圓/圓軌道），現在同時接受 `ECC>1` 搭配 `SMA<0`（雙曲線，依慣例）。
   `SMA*(1-ECC)` 這個近地點公式對兩種情況都成立 (代數上驗證過)，用同一套檢查。只擋
   「符號兜不起來」的組合跟 `ECC==1` (拋物線，退化邊界情況，不支援)。用 poliastro
   實際建立過一個 `SMA=-20000, ECC=1.5` 的雙曲線軌道確認 `Orbit.from_classical` 接受
   這個慣例、算出來的 r0/v0 合理。
2. **`src/optimizer.py`**：`T_max = 4×A的軌道週期` 這個公式對雙曲線 A (沒有週期) 沒
   有意義，會對負數開根號炸掉。新增 `rules.T_MAX_SEC` 選填欄位，給值就直接覆寫 T_max
   (不管 A 是不是雙曲線都適用)；A 是雙曲線但沒給這個覆寫值時，直接丟清楚的
   `ValueError` 而不是讓 NaN 一路流到最佳化器深處。排位賽的 T_max 定義方式官方還沒
   公告，所以這裡刻意不猜公式，等公告後把算出來的秒數填進這個欄位就好。

兩處都寫了測試案例驗證 (合法橢圓/合法雙曲線都 PASS，符號矛盾/拋物線/近地點穿地球都
正確 REJECT，雙曲線 A + `T_MAX_SEC` 覆寫值的組合能正常初始化 `MissionOptimizer`)。

**還沒做的部分**：`check_constraints`（撞地球檢查）跟 `propagate_rk4`（RK4 積分）的
數學本身對雙曲線軌道已經成立，不用改；但排位賽的計分公式細節、Δv/時間限制是否跟初賽
一樣、T_max 到底怎麼算，都要等官方公告才知道——目前只做到「不會直接壞掉」，還沒做到
「排位賽也能正確規劃任務」，那部分等測資/規則公告後再補。

## 重力場模型可設定：J2/J3/J4（2026-08-14 清晨）

使用者提到比賽當天實際開的重力擾動階數不一定跟現在假設的一樣，希望把 J2/J3/J4 變成
可選項，順便把 J3/J4 也補進 Python 的力學模型（直接呼應上面「拆解 GMAT 對不上的真正
原因」那一節找到的第三層問題：GMAT 用 `Degree=4,Order=4`，Python 只有純 J2）。

### 改動
`strategy.USE_J2` (布林值) 換成 `strategy.GRAVITY_DEGREE` (整數 0/2/3/4：0=純點質量,
2=J2, 3=J2+J3, 4=J2+J3+J4)。`src/core_math.py` 的 `fast_dynamics` 新增 J3/J4
zonal harmonic 加速度項；`GMAT` 那邊的 `Order` **改成不管 Degree 是多少都固定收在
0**——GMAT 的 Degree/Order 分別對應球諧重力場的階 (n) / 序 (m)，m=0 那排就是 zonal
harmonic (J2/J3/J4，只跟緯度有關)，m>0 是 tesseral/sectoral (經度也有關的重力異常)。
Python 端只做得到 zonal，固定 Order=0 讓 GMAT 也只算 zonal，兩邊變成完全一樣的物理
模型——這是刻意的取捨，犧牲 GMAT 端的「真實感」換「兩邊模型對得齊、能互相驗證」。
連帶效果：就算維持預設值 `GRAVITY_DEGREE=2`（跟原本 `USE_J2=true` 行為一樣），GMAT
端也從舊的 `Degree=4,Order=4` 變成 `Degree=2,Order=0`，比以前更貼近 Python 的純 J2
假設，不用特地開 J3/J4 就已經減少一個系統性落差來源。

J2/J3/J4 三個係數不是抓教科書上的通用常數，是**直接從 GMAT 用的同一份 `JGM2.cof`
重力場檔案反算**（`RECOEF 2/3/4 0` 那三個正規化係數乘上 `sqrt(2n+1)` 去正規化、取
負號）：`J2=1.082626724e-3`（反算出來跟原本寫死的 `1.08262668e-3` 只差 `4.4e-11`，
確認反算方法正確）、`J3=-2.5323078e-6`、`J4=-1.62042999e-6`。

J3/J4 的加速度公式沒有照抄任何地方，是自己從 zonal geopotential
`U=-(mu/r)[1-ΣJn(Re/r)^n Pn(sinφ)]` 的梯度重新推導的一般公式，推導完先拿已經在用、
確認正確的 J2 公式回頭驗證 (x/y/z 三分量逐項推導都精確吻合)，方法確認沒問題後才套用
到 J3 (再跟 `poliastro.core.perturbations.J3_perturbation` 的獨立實作逐項比對，三分量
都精確吻合——poliastro 原始碼註解寫「這個擾動還沒完整驗證過」，但這裡的獨立推導證明
公式本身是對的，那個警語應該是指數值傳播層級的驗證，不是公式有錯) 跟 J4 (poliastro
沒有實作，這是唯一一個完全沒有第三方實作可以逐項核對的部分，所以下面另外做了 GMAT
交叉驗證)。

### 實測驗證（跟真實 GMAT 對照，不是只看公式）
1. **Regression**：`GRAVITY_DEGREE=2` 時，跟改動前的純 J2 行為做位元級比對 (獨立
   重刻一份簡單 RK4 當基準)，位置差異 `4.5e-12 km`，機器精度等級——確認 refactor
   沒有動到原本 J2-only 的行為。
2. **J3/J4 正確性**：拿一個普通 LEO-ish 軌道 (SMA=7000, ECC=0.1, INC=45°)，傳播
   3 天 (259200 秒，約 44 圈)，比較 Python `GRAVITY_DEGREE=4` 跟真實 GMAT
   (`Degree=4, Order=0`，兩邊現在算的是完全一樣的物理模型) 的最終位置：
   - Python (`dt=60s`，實際搜尋用的步長) 跟 GMAT 差 **17.2 km**（J2-only 時是
     29.2 km，J3/J4 補上去有明顯縮小差距，但還沒到「準」的程度）。
   - 把 Python 的步長縮小到 `dt≈0.2s`（逼近真解）後，跟 GMAT 只差 **270 公尺**
     （44 圈、3 天的傳播裡差 270m，相對誤差非常小）。
   - **結論：J3/J4 公式本身是對的**——一旦排除步長造成的積分誤差，Python 的
     J2+J3+J4 模型精準吻合 GMAT 的 zonal-only 模型。

### 意外發現，修正了這次 session 前段的一個結論
上面第 2 點的實驗連帶量出一件事：**光是固定 60 秒的 RK4 步長，在一個完全普通的 LEO
軌道上傳播 3 天 (44 圈)，就造成 17 公里的誤差**——遠遠大於今天前段用「極端軌道 A 傳播
一次 T_max (4 圈)」量到的 2.1 公里。這代表前段「拆解 GMAT 對不上的真正原因」那一節下的
結論（「步長本身大概率不是主要原因，缺失的 J3/J4 才是更可疑的方向」）**只對了一半**：
J3/J4 補上去確實有幫助 (縮小了 41% 的落差)，但步長本身在圈數夠多的情境下，影響其實
比原本量到的更大——**真正的變數看起來是「繞了幾圈」，不是「跟 T_max 比起來的秒數」**。
一個軌道週期短的情境 (例如 B 在低軌快速繞行、coast 很久) 就算總秒數遠低於 T_max，
只要圈數夠多，固定步長累積的誤差一樣可能不小。

這個發現讓「還沒做」清單第 1/3 項的評估要再往前修一次，已經在下面更新。

## 傳播器：RK45 自適應步長 + Encke's method（2026-08-14 上午）

延續上面「固定步長誤差比想像中大」的發現，把 `src/core_math.py` 的核心積分器換掉，
連續做了兩輪，用同一套「跟 GMAT 交叉驗證」的紀律驗證過。

### 第一輪：Dormand-Prince RK5(4) 自適應步長 (取代固定 60 秒 RK4)

`fast_dynamics`/`rk4_step`/`propagate_rk4` 換成 `rk45_step`/`propagate_rk45`——標準
Dormand-Prince 7 段嵌入式係數 (跟 scipy `RK45`/MATLAB `ode45` 同族，也是 GMAT 自己
預設用的積分器家族，只是 GMAT 通常用更高階的版本)。步長完全由**這一步自己的局部
誤差估計**決定 (混合相對/絕對容忍度、RMS norm，公式跟 scipy 一致)，不是任何跟軌道
規模/T_max 有關的全域啟發式——這是跟被撤銷的 `ebaf728` 那次「步長跟 T_max 成比例
放大」的關鍵差異，近地點自然用小步、遠地點自然用大步，沒有那次數值爆炸的風險。

`optimizer.py`/`propagator.py` 裡所有 `propagate_rk4(...)` 呼叫都改名成
`propagate_rk45(...)`，介面 (參數順序/數量) 沒變，只有第 4 個參數 `dt` 的意義從
「固定步長」變成「初始步長猜測」。

**驗證結果**（3 天/44 圈的 LEO 測試案例，跟真實 GMAT 比對）：
- 固定 dt=60 RK4：誤差 17.2 公里
- RK45 自適應 (預設 rtol=1e-9, atol=1e-6)：誤差 **410.7 公尺**（42 倍進步）
- 完整 `main.py` 流程重跑一次 `practice_scenario.json`：Python vs GMAT 的
  MissDistance 差距從 32.9m 壓到 **0.36m**

**代價，講清楚不是雙贏**：短時間 (幾百到幾千秒) 的一般情境，RK45 反而比舊 RK4
慢 (每步 7 段斜率 vs 4 段，加上誤差估計開銷，短弧線舊方法本來就只要 1-2 步就夠)。
同一組 config 完整流程從 4.73 秒變 8.78 秒 (慢 1.86 倍)。**這個取捨已經套用到
主線程式碼——RK45 現在是實際在用的積分器**，準度大幅提升換一定的速度代價。

### 第二輪：評估 Encke's method（結論：正確且更準，但沒有更快，先不換上）

使用者接著想試 Encke's method (解析二體參考軌道 + 只對偏差量數值積分 + 定期校正)，
理論上短弧線/長弧線都該比 RK45 快。完整實作在 `core_math.py`
(`encke_f`/`encke_delta_accel`/`encke_rk45_step`/`propagate_encke`)，架構：

- 參考軌道：poliastro 的 `farnocchia_rv` (universal variable，精確二體解，橢圓/
  雙曲線通用，已確認能從 njit 內部呼叫)
- `f(q)` 特殊函數 (避免大數相減精度損失)：**一開始從記憶重刻的公式是錯的**——
  用 sympy 符號驗證發現原本寫的 `q*(3+3q+q²)/(1+(1+q)^1.5)` 跟正確答案
  `1-(1+q)^-1.5` 代數上根本不相等 (q=0.01 就已經差 1.5%，q=5 差了 13 倍)。重新
  用 sympy 從 `1-(1+q)^-1.5` 符號化簡推導出正確的穩定形式
  `q*(q+√(1+q)+2) / [(1+q)^1.5*(1+√(1+q))]`，再用 mpmath 50 位精度當基準逐一
  比對：原始公式在 q<1e-8 開始損失精度、q=1e-16 直接歸零，這個穩定形式全程維持
  機器精度等級 (~1e-16 相對誤差)。**這是這次 session「先驗證再用」原則的一次
  真實案例——如果沒有用 sympy/mpmath 交叉驗證就直接把記錯的公式塞進 core_math.py，
  會是一個很難被發現的隱藏 bug。**
- `q` 的定義、`r²=ρ²(1+q)` 這個代數恆等式也另外用隨機亂數值數值驗證過。

**正確性驗證**：純二體 (無擾動) 時 Encke 精確退化成 `farnocchia` 本身的解析解，
誤差 0.0 公尺 (符合預期——沒有擾動，delta 恆為 0)。有 J2+J3+J4 的 3 天/44 圈測試，
跟 GMAT 比對誤差 **274.3 公尺**，比 RK45 的 410.7 公尺還準。

**速度：不如預期，這是最重要的誠實結論**。理論上「二體交給解析解、不用管繞幾圈」
應該讓 Encke 又快又準，但實測從短弧線到長弧線 **全面比 RK45 慢**（優化前 0.27x~
0.54x，優化後 0.36x~0.62x，優化細節見下）。根因：`farnocchia_rv` 單次呼叫要
1.545 微秒，而 `fast_dynamics` (完整二體+J2+J3+J4，直接封閉式公式，不用疊代)
只要 0.348 微秒——**解析 Kepler 解一次呼叫反而比直接算加速度貴 4.4 倍**，因為
Kepler 方程本身要疊代解 (Newton 法)，不是真的「零成本」。Encke 一個 RK45 step
要呼叫 7 次這個「解析解」，即使總步數比 RK45 少 (實測約少 27%)，每步貴 4~5 倍
還是讓總時間變長。

**已經做的一個優化**：一開始的版本每次呼叫都用 `farnocchia_rv` (內部每次都重新
把 r,v 轉成軌道根數 `rv2coe`，實測這個轉換單獨佔 0.498 微秒，~32%)，但同一個
校正區間內參考軌道根數是固定的、只有飛行時間在變——改成在校正時算一次根數
(`rv2coe`)，區間內每個 RK45 stage 只呼叫 `farnocchia_coe`(解 Kepler 方程) +
`coe2rv`(根數轉回 r,v)，省掉重複的根數轉換，3 天測試的速度從 0.27x 提升到
0.36x RK45——**有幫助但沒有翻轉結論，Encke 目前這個實作還是全面比 RK45 慢**。

**目前決定**：`propagate_encke` 完整實作、驗證過，留在 `core_math.py` 裡 (正確、
比 RK45 準)，但**沒有接進主線的 `optimizer.py`/`propagator.py`**——目前實際在跑的
還是 RK45。要不要換成 Encke (拿更好的準度換更慢的速度，或者投入更多工夫優化
Kepler 求解的成本) 待使用者決定。

### 收尾驗證：拿回上一個 session 的「情境一/二」極端測資重新實測

用今天做完的完整版本 (RK45 + `GRAVITY_DEGREE=4` 含 J2/J3/J4 + `main.py` 的違規燃燒
fallback 機制) 重新跑一次上個 session 發現「差五個數量級」問題的那兩組極端測資
(`MAX_BURNS=[1,2,3]`, `MAXITER=200`, `POPSIZE=10`, `SEED=42`)，直接驗收今天一整天
的修復成果：

| | 情境一 (A極端/B正常LEO) | 情境二 (A/B都極端) |
|---|---|---|
| **舊結果 (上個 session，固定RK4+純J2)** | | |
| 耗時 | 29.1 分鐘 | 26.8 分鐘 |
| GMAT 實測 Δr_min | 50,229,818 m | 25,401,794 m |
| GMAT Targeter | ❌ 未收斂 | ❌ 未收斂 |
| **新結果 (這個 session，RK45+J2~J4+fallback)** | | |
| 耗時 | **87 秒** (~20倍快) | **32 秒** (~50倍快) |
| 找到的解 | 違規但快 (最後一棒 44,529 m/s，超標30倍) | **完全合法** (ΔV_team=1.8 m/s，0違規) |
| GMAT DC 版本 | ❌ 未收斂 (4,216,069m，DC 邊界搆不到違規目標，預期中的行為) | ✅ 收斂 (3487.5m) |
| **固定燃燒/DC 驗證的真實誤差** | **0.16 m** (3492.44 Python vs 3492.29 GMAT，fallback 版本) | **12.5 m** (3500.0 Python vs 3487.5 GMAT) |
| 提交就緒？ | ✅ 命中成功但違規 (fallback 產生，扣10分但可提交) | ✅ 命中成功且合規，可直接提交 |

**結論**：兩組原本「差五個數量級、求解器收斂不了」的極端測資，現在都能在 1~2 分鐘內
跑完，而且 Python 預測跟 GMAT 真實模型的誤差都壓在幾公尺到零點幾公尺等級——情境一
證實了今天稍早的診斷 (DC 收斂失敗是「搆不到違規目標」的假象，不是模型不準，繞過 DC
後 Python 跟 GMAT 幾乎完全吻合)；情境二證實合法解本身也能被找到、DC 也能正常收斂、
準度同樣是公尺等級。這組驗證把這個 session 開頭「極端軌道完全不能信」的疑慮，實測
收斂成「已經可以信了，且遠遠更快」。

### 順便：拿同一組情境一/二測資直接對照 RK45 vs Encke

既然剛好有這兩組真實情境的基準結果，順手用猴子補丁 (`src.core_math.propagate_rk45`
指向 `propagate_encke`，兩者位置參數簽名一致，不用改 `optimizer.py` 任何一行) 把
Encke 接進同一套 `main.py` 流程，同樣的 config/seed 重跑一次，跟上面 RK45 的結果
直接對照：

| | 情境一 (RK45 → Encke) | 情境二 (RK45 → Encke) |
|---|---|---|
| 耗時 | 87s → 92s (+6%) | 32s → 37s (+18%) |
| 搜尋找到的解 | 完全一樣 (相同 seed，違規但快，44,529 m/s) | 完全一樣 (合法，1.8 m/s) |
| 真實模型誤差 | 0.16m → **0.16m (一樣)** | 12.5m → **12.5m (一樣)** |

兩組情境搜尋到的解、跟 GMAT 比對出來的真實誤差，RK45 跟 Encke **完全相同**——因為
兩邊在 `GRAVITY_DEGREE=4` 這個精度下都已經準到不影響 L-SHADE 找到哪個候選解，最後
決定對錯的是真實 GMAT 驗證，不是內部用哪個傳播器。差別只剩下速度，而且實際跑完整
流程 (含 GMAT 呼叫、NLP 微調等其他不受傳播器影響的固定開銷) 量到的速度差距
(+6%~+18%) 遠比純傳播器層級的微觀 benchmark (慢 1.6~2.8 倍) 溫和很多，因為那些
固定開銷把差距稀釋掉了。

**結論**：以這兩組實際情境的證據，**RK45 是更務實的選擇**——同樣的準度、同樣的搜尋
結果，但更快。Encke 目前看不出在這個專案的實際使用情境下有具體優勢，繼續維持
RK45 是主線積分器的決定不變。

## DOP853 (8 階 Dormand-Prince)：目前看到最好的結果，還沒接進主線

使用者接著問「RK89 之類的可能」——GMAT 自己常見的預設積分器 (`RungeKutta89`/
`PrinceDormand78`) 就是這個檔次，比 RK45 高階很多。理論依據：軌道動力學是平滑函數，
階數越高、同樣步長容許的誤差越小，平滑問題用更高階方法有機會用更少步數達到更高精度
(不像 Encke 那種「改演算法架構」的取捨，DOP853 單純是「同一種 Cowell 式數值積分，
換一組更高階的係數表」)。

**移植方式**：12 段 stage 的係數表比 RK45 大很多，這次**不手動重刻**——直接從
`scipy.integrate._ivp.dop853_coefficients` 匯入 scipy 自己在用、被廣泛驗證過的
A/B/C/E3/E5 係數陣列，程式邏輯照抄 scipy `Dop853._estimate_error_norm` 的誤差估計
公式 (混合 err5/err3，不是簡單 RMS)。移植正確性驗證：純 Python 原型先跟
`scipy.integrate.solve_ivp(method='DOP853')` 對同一個測試 ODE 跑一次，3 天/44 圈
的傳播只差 **4.7 公分**，確認移植正確後才搬進 `core_math.py` 用 numba 重寫。

**驗證結果**：
- 3 天/44 圈跟 GMAT 交叉驗證：**222.4m**（RK45 是 410.7m，DOP853 更準）
- 純傳播器速度 benchmark：短弧線 (100s) 幾乎打平 RK45 (0.99x)，中長弧線**明顯更快**
  (500s: 1.22x, 10000s: 2.00x, 259200s: 2.30x)——不像 Encke 那樣全面變慢，這次是
  真的又快又準。
- 情境一/二真實情境對照 (猴子補丁跑同一套 `main.py` 流程)：

| | RK45 | Encke | DOP853 |
|---|---|---|---|
| 情境一耗時 | 87s | 92s | **85.6s** |
| 情境一真實誤差 | 0.16m | 0.16m | 0.16m (同一個燃燒值，GMAT 驗證結果一致) |
| 情境二耗時 | 32s | 37s | **31.3s** |
| 情境二真實誤差 | 12.5m | 12.5m | **7.7m** |

**結論**：DOP853 在這兩組真實情境上全面持平或贏過 RK45——速度一樣快甚至略快、
準度一樣好甚至更好，沒有觀察到任何 Encke 那種「準度換速度」的取捨。**目前還沒接進
主線** (`optimizer.py`/`propagator.py` 還是呼叫 `propagate_rk45`)，是否要正式換上
DOP853 待使用者確認，但目前證據看起來是三個方案裡最好的。

### 過程中意外抓到的真 bug：GMAT 驗證失敗時可能誤讀殘留的舊報表檔

實作「太空船 B 追蹤視角」(見下一節) 時，新加的 GMAT script 內容不小心混進中文
註解，觸發 GMAT 解析器的非 ASCII 限制 (這個雷 `887e64e` 已經踩過一次，這次自己又
踩了一次)。但真正有價值的意外發現是：**`main.py` 的 `run_gmat_verification()`
在 GMAT 執行失敗時，沒有檢查 subprocess 的 `returncode`，也沒有在跑之前清掉舊的
報表檔**——導致背景跑的「情境二 DOP853」測試因為腳本解析失敗，卻誤讀了幾秒鐘前
「情境一」殘留的舊報表檔，安靜地印出一份張冠李戴、看似合理實則完全對不上的驗證
結果 (MissDistance/Δv/T_team 全部是情境一的數字，不是情境二的)。

已修復：`run_gmat_verification()` 現在會在呼叫 GMAT **之前**先刪掉舊報表檔 (讓
「檔案存在」只可能代表這次真的寫出來了)，並且額外檢查 `returncode != 0` 才判定失敗、
印出 GMAT 實際的錯誤輸出。用故意塞壞字元的腳本重現過這個 bug、確認修復後正確回傳
`None` 而不是殘留資料。這個 bug 跟 DOP853/Encke 本身無關，是既有程式碼的既有缺陷，
只是這次測試流程剛好把它踩出來。

## 太空船 B 追蹤視角 (GMAT script 新增第二個 3D View)

使用者要求在輸出的 GMAT script 加一個「飛船視角」。`script_generator.py` 新增
`View_ShipBChase`：攝影機參考點設成 `ShipB` (跟著 B 移動)、`ViewDirection = ShipA`
(鏡頭一路對準 A)，效果類似追蹤鏡頭——跟原本固定的 Earth 鳥瞰視角 (`View_Intercept`)
同時存在，開啟 script 後兩個 3D 視窗都會顯示。**這個功能本身很小，但過程中意外
踩到、又意外抓出了上面那個報表檔 bug，算是這次修改真正的價值所在。**

## DOP853 正式換上主線積分器

驗證結果確認 DOP853 全面持平或優於 RK45 之後，使用者確認換上——`src/optimizer.py`/
`src/propagator.py` 所有 `propagate_rk45(...)` 呼叫都改成 `propagate_dop853(...)`
(純改名，位置參數簽名一致，不用動邏輯，同一套照 STATUS.md 記錄的作法)。用真實
`main.py` (不是猴子補丁) 跑過一次 `practice_scenario.json` 確認正式接上後行為正常
(6.09 秒完成，Python vs GMAT fallback 版本誤差 ~3m 等級)。`propagate_rk45`/
`propagate_encke` 仍然留在 `core_math.py` 裡 (沒有刪除，只是主線不再呼叫)，之後
如果想比較/切換回去還在。

## 系統性測試軌道參數極限：SMA 完全不是問題，ECC 才是真正的邊界

使用者要求「多測幾個極端數值」「測每個參數的極限在哪裡」。做法：不跑完整的 L-SHADE
搜尋 (太貴)，改成傳播器層級的直接測試——對每個測試軌道，Python (DOP853,
`GRAVITY_DEGREE=4`) 傳播一段時間，同時產生一份極簡 GMAT script (`Degree=4,Order=0`
zonal-only，跟 Python 端同一個物理模型) 跑同樣的時間，比較最終位置誤差。

### SMA 掃描 (ECC=0 圓軌道，傳播 1 個完整週期)

從 SMA=6,578km (200km 高度低軌，安全下限附近) 到 SMA=5,000,000km (遠遠超出地球
真實引力範圍，純粹壓力測試數值穩定性)：**全部維持在 10 公尺以下的絕對誤差**，而且
SMA 越大誤差還越小 (5,000,000km 時只有 4.00m)。**結論：SMA 本身完全不是限制因素，
不管多大多小都準。**

### ECC 掃描：真正的邊界在這裡

兩組獨立測試都指向同一個結論：

1. **固定近地點高度 (~300km)、變 ECC** (SMA 隨 ECC 增加而增加，週期也跟著變長)：
   ECC=0→8.4m，ECC=0.5→36m，ECC=0.8→380m，ECC=0.9→2,079m，ECC=0.95→8,917m，
   **ECC=0.99→117,939m (118 公里！)**。
2. **固定 SMA=200,000km (週期固定在 10.3 天，排除傳播時長這個干擾變因)、只變 ECC**：
   同樣的趨勢——ECC=0→0.14m，ECC=0.5→11m，ECC=0.8→78m，ECC=0.9→775m，
   ECC=0.95→7,236m，ECC=0.968→19,045m。**兩組測試互相印證：這是真正的離心率效應
   (近地點曲率/速度變化太劇烈，兩邊積分器 (Python DOP853 vs GMAT RK89) 的細微
   差異在這種幾何下被放大)，不是傳播時長造成的假象。**

### 對這個 session 一直在用的測試軌道 (SMA=80,000, ECC=0.87) 的直接檢驗

這個離心率 (0.87) 剛好落在「安全」跟「開始有風險」的中間，用實際的軌道規模單獨測
傳播不同比例的週期：10%週期→2.7m，25%週期→6.8m，50%週期→16.7m，**完整 1 個週期
→219.2m**。全部遠低於規則 5km 的命中門檻，**這個 session 一直在用的極端測試軌道，
在整個週期範圍內都是安全的**，不用擔心之前的結論是建立在不可信的資料上。

### 雙曲線掃描 (排位賽相關)：完全沒問題

固定近地點高度 (~700km)、ECC 從 1.01 (幾乎拋物線) 到 10 (非常尖銳的雙曲線飛越)，
傳播 20,000 秒：**全部維持在 6 公尺以下**，跟 ECC 大小幾乎無關。**排位賽的雙曲線
軌道 A，就這個軌道規模的傳播精度而言，不用擔心。**

### 結論與建議的安全邊界

| 離心率範圍 | 完整一圈週期的典型誤差 | 評估 |
|---|---|---|
| ECC ≤ 0.9 | 幾公尺 ~ 2 公里 | ✅ 安全，遠低於 5km 門檻 |
| 0.9 < ECC ≤ 0.95 | 2 ~ 9 公里 | ⚠️ 開始接近/超過 5km 門檻，視實際傳播時長而定 |
| ECC > 0.95 | 可以到 10~120 公里 | 🔴 不可信，需要另外處理 (加警告/強制 GMAT 覆核/更高精度模型) |

**這個結論比原本 STATUS.md 的假設更精確**：原本以為「軌道規模 (SMA)」是問題，
現在確認 SMA 完全無關，**真正的風險因子是離心率，且要離心率相當接近 1 (>0.95)
才會出問題**——初賽 A 是圓軌道 (ECC=0)，本來就不會踩到；排位賽 A 是雙曲線
(ECC>1)，也確認不會踩到；唯一需要留意的是「晉級賽如果出現極高離心率 (>0.95) 的
橢圓軌道」這種還沒被規則提到的情境。

## sweep_burns.py 粗掃階段依維度公平分配世代預算（2026-08-14 下午）

使用者實測 `configs/weird_test.json`（自己編的壓力測試情境：SMA=150,000km、
ECC=0.93、INC=175°，比 8/13 那組「情境一」的 80,000km/0.87 更極端；`--burns 1-8`
逼到 8 棒、決策變數維度 33）時發現粗掃階段（`--coarse-iters 80`）光是跑完就要
15~23 分鐘，而且懷疑粗掃出來的排序不可信——實測抓到證據：MAXITER 從 300 降到
80 後，2 棒的分數從 -65.4137（比 1 棒好）掉到 -50.0905（比 1 棒還差），排序直接
反轉。根因跟 `sweep_burns.py` 檔頭註解講的完全一樣：決策變數維度跟族群大小都隨
燃燒次數線性長，但粗掃階段所有燃燒次數套同一個 `MAXITER`，維度越高的案例天生
越吃虧，粗掃出來的「最高分」本身可能就是被低估的，`find_elbow()` 選出來的
`window` 可能永遠不會碰到真正該測的燃燒次數。

### 改動：MAXITER 支援 {燃燒次數: 世代數} 字典，依維度公平分配

- **`src/optimizer.py`**：新增模組層級函式 `decision_variable_dims(num_burns)`
  （`4×num_burns+1`，跟 `_generate_bounds()` 的陣列長度用同一個公式，
  `_generate_bounds()` 底部加了 `assert` 防止兩邊以後改到不同步）。
  `MissionOptimizer.maxiter` 現在除了原本的單一整數，也接受
  `{燃燒次數: 世代數}` 字典；新增 `_maxiter_for(num_burns)` 統一處理兩種情況，
  `_optimize_burn_case`/`run_study`（進度條 total、`_bump_progress` 的每案例上限、
  完成訊息的分母、提早停止判斷）全部改成呼叫這個方法，不再假設所有案例共用同一個
  `self.maxiter`。
- **`src/config_validator.py`**：`_validate_optimization` 的 `MAXITER` 檢查
  多接受字典形式（key 是 >=1 的燃燒次數整數、value 是 >=1 的世代數整數，且必須
  涵蓋 `MAX_BURNS` 裡的每個燃燒次數），不然粗掃階段自己組出來的 `stage_config`
  會被誤判成壞設定擋下來。
- **`sweep_burns.py`**：新增 `scaled_coarse_iters(burns, base_iters, min_iters,
  max_iters)`——`base_iters` 套在範圍裡維度最小的燃燒次數上，其他燃燒次數依維度
  比例往上調（縮放比例是從上個 session「6 棒要把 MAXITER 從 1000 拉到 3000 才
  追上 2 棒」那個實測歸納出來的經驗法則，維度比 25/9≈2.78、代數比 3 倍，大致
  吻合線性關係，不是嚴謹推導）。新增 `--coarse-popsize`（粗掃階段獨立的族群大小
  槓桿，不給就沿用 config 原本的值）跟 `--coarse-iters-cap`（世代數硬上限，見
  下一段為什麼需要這個）。`run_stage()` 的表頭印出改成字典感知，會印
  `MAXITER(依維度縮放)={1:25, 2:45, ...}` 而不是單一數字。

**驗證過**：純邏輯測試（`decision_variable_dims`/`validate_config` 吃字典、缺
key/壞 value 會不會正確擋下來）先過，再用便宜情境（4 棒、`--coarse-iters 20`）
跑一次完整兩階段流程——粗掃階段已經看得出「3、4 棒分數往下掉」是預算不夠的假象
（跟精細驗證「其實都打平」對照就看得出來），粗掃/精細驗證選出的最佳棒數一致
（都是 2 棒），17.9 秒粗掃 + 16.5 秒精細，比舊版更快也更可信。

### 重要修正：維度公平縮放不會自動變快，甚至可能更慢——加了硬上限

第一版建議使用者用 `--coarse-iters 30 --coarse-popsize 6` 重跑 `weird_test.json`
時，事後算帳發現這組參數對 8 棒（維度 33）算出來的世代數（198 代）乘上腰斬的
族群，總運算量跟舊版「80 代 × 族群 15」幾乎一樣貴——因為公平縮放的方向是「往上
補」高維度案例的預算，不是「整體往下砍」，而 `run_study()` 是並行跑所有燃燒次數
案例，**總耗時看最慢的那一個，不是看總和**，補上去的部分直接抵銷了族群減半省下
來的時間。這是在還沒讓使用者實測、只講了个「應該會快」的判斷後才自己抓出來的
錯誤，教訓：講「會變快」之前要先把最貴的那個案例單獨算一次，不能只看縮放邏輯
「感覺合理」。

修法：`scaled_coarse_iters` 加 `max_iters` 硬上限參數（`--coarse-iters-cap`），
不管維度比例算出來多大都封頂，犧牲最高幾個燃燒次數之間的公平性換一個看得到的
時間上限。用 `--coarse-iters 25 --coarse-iters-cap 100 --coarse-popsize 6` 算過
帳：8 案例加總運算量降到舊版的 45%，決定總耗時的最貴案例（8 棒）降到舊版的
50%——這次有把最貴的案例單獨驗算過，不是只看總量。**這組參數還沒讓使用者實際
跑過 `weird_test.json` 驗證預估的 7~8 分鐘準不準**，下次接手可以補這個驗證。

### 發現：MacBook Air 熱降頻，讓「順便修 n_workers 沒榨乾」這個念頭也收回

> **⚠️ 2026-08-15 起這一節的硬體前提已經不成立**：開發機換成 WSL / Ryzen 5800X
> （塔扇散熱、16 threads），不再是無風扇的 Air。這一節記錄的是 8/14 當下的決策
> 脈絡（保留原文不改寫歷史），但**下面「先不做」的判斷在新機器上要重新評估**，
> 見「還沒做」清單第 11 項。

討論過程中發現 `_optimize_burn_case` 裡 `n_workers`（每個案例內部開幾條 thread）
是案例剛開始時依「總共有幾個案例」算好就固定住，不會隨著其他案例陸續跑完、
釋放出核心而增加——本來想提議修掉，但使用者確認他的機器是 **MacBook Air（無
風扇）**，粗掃這種長時間高負載下會熱降頻，CPU 已經有 80%+ 的時間被吃滿。在這種
機器上，讓剩下的案例多搶 thread 不保證是純贏——更多核心同時全速衝，更容易撞到
熱牆，可能讓降頻更早/更嚴重，實際效果不確定。**這個修法先不做**，不是因為不值得
做，是因為在熱降頻的硬體上沒把握是淨賺，貿然做了可能又要重新驗證一次「這樣真的
比較快嗎」。

**結論**：`weird_test.json` 是刻意編的壓力測試情境，不是官方題目——初賽 A 已知是
圓軌道，落在「系統性測試軌道參數極限」那節確認的安全範圍，不會踩到這個量級的
負擔。這個工具在熱降頻的 Air 上跑這麼極端的情境本來就會慢，是硬體限制，不是
程式碼還沒調好；決定不再花時間在這個工具的速度上，改成如果之後方便，找一台
有塔散熱器的桌機（使用者提到有一台 5800X）實測比對——**這次的比對還沒做**，
見下面「還沒做」清單。

## weird_test.json 深度診斷：單棒死局、拆分陷阱、以及藏在 0.0086% 窄窗口裡的合法解（2026-08-14 傍晚）

延續上面 sweep_burns.py 那節，使用者繼續拿 `weird_test.json`（自編壓力測試：`orbit_A`
SMA=150,000km、ECC=0.93、INC=175°，`orbit_B` 一般 LEO）追問一個更根本的問題：
**這個工具在極端情境下找到的解，到底可不可信**。這一節記錄的是一次很長、多輪自我
修正的診斷過程，中途有兩次結論被後來的實測推翻，最後推翻的方向剛好相反——先講
結論方便之後快速定位：

**MAX_BURNS=1（單棒）在這個情境下，長期被以為是「77度傾角差的死局，不管怎麼喬
時間都要 10~15 km/s」——這個結論最後被推翻了：細網格掃描找到一個真正合法的單棒
解（1189.73 m/s，低於 1500 m/s 上限），只是它藏在一個只佔 `T_max` 0.0086% 的
極窄時間窗口裡，連跑 2000 代的 L-SHADE 都找不到。這不是工具的 bug，是這個問題
的地形（幾乎完全平坦、只有一根極窄的尖峰）天生就對隨機搜尋不友善。**

### 第一階段：以為是死局（後來證明這個結論本身沒錯，只是「單棒做不到」這句話下錯了範圍）

- **t_wait×flight_time 粗網格**（步長用「天」）：最低 Δv 落在 7~13 km/s，遠遠
  超標。
- **M-rev Lambert（多繞圈）**：M=1 幾乎沒改善，M≥2 直接不收斂（`izzo` 對大 M
  的已知限制，見 `_optimize_burn_case` 的例外處理註解）。
- **flight_time 掃到快 4 圈**（A 的完整週期是 T_max 的上限）：11~15 km/s 反覆
  震盪，沒有隨時間繼續下降的趨勢。
- **MOID 幾何分析**（不管時間，直接算兩個橢圓軌道在空間中最近能靠多近）：最近
  6,816.8 km，該處相對速度 10.5 km/s，跟 Lambert 網格的數字互相印證。

當時的結論：SMA=150,000/ECC=0.93 的 A 近地點都還有 10,500km，比 B 的 ~6,800km
軌道遠了 3,700km，疊加 77 度傾角差，單棒不管怎麼安排時間都要不了 10 km/s 等級
的 Δv。**這個結論後來被第四階段推翻——但推翻的不是物理判斷本身，是判斷的方法本身不夠精細（見下）。**

### 第二階段：拆分燃燒陷阱——同方向硬拆反而更貴

使用者問「機動間隔只要 100s，能不能拆成好幾棒各自合法」。手動把一個 11,935 m/s
的單次燃燒拆成 8 棒（每棒 1490 m/s、每棒間隔 100s，全部同一個方向）直接測試：
**最後一棒要花 40,811.6 m/s 修正，比什麼都不做還貴。** 原因：拆分燃燒之間有滑行，
飛船是沿著「部分改完的速度」在飛，路徑跟「一次改完直接上轉移軌道」完全不同，
累積下來偏離原設計軌道很多。**這不代表拆分這個方向錯，是「同方向硬拆」這個土法
煉鋼的實作方式不對——每一棒的方向/滑行時間該是各自獨立的自由變數，這正是真正
的 L-SHADE 多棒搜尋在做的事，不是等份切割。**

### 第三階段：多棒長程解重演「第三層」模型分岔問題，這次在更極端的軌道上

用 `main.py` 真的跑一次 `MAX_BURNS=[3]`（真實 `MAXITER=1000`，耗時 33.75 分鐘）
找到一組 Python 內部評分很高的解：等 1,124,066.9 秒（~13天，Burn 1 是 0 燃燒，
純粹佔位）、Burn 2 在 t=2,020,634.2s（203.8 m/s）、Final Burn 在 t=2,288,326.9s
（990.8 m/s，逆向），總 ΔV_team=1194.6 m/s，Python 預測 Δr_min=3086.2m（判定
成功）。**GMAT 實測完全不同：MissDistance=26,545.853m，Targeter 未收斂，固定
燃燒版本 (繞過 DC) 重播也一樣沒命中 (26,400.6m)。**

拆解發現：這組解沒有想飛到 A 的遠地點（289,500km），是算準 A 繞完快 4 圈後會
盪回到離地球比較近的一段（實測：攔截當下 A 距地心 29,639km，B 燒之前距地心只有
7,049km），在那裡用便宜的 Δv 碰頭——策略本身很聰明（用等待+多棒把「轉平面」
安排在對的地方），**但這個策略的代價是要精準預測 A 在 26.5 天（快 4 圈）後的
確切位置，而這正是 STATUS.md 前面「拆解 GMAT 對不上的真正原因」那節記錄的第三層
問題——Python 簡化模型跟 GMAT 高精度模型在長時間傳播下會分岔，這次差了 23 公里，
比之前那次（480 幾公里，J3/J4+DOP853 升級前）小很多，但還是遠遠超過 5km 門檻。**
`GMAT DC` 卡在 `2598.1 m/s`（= `1500×√3`，三軸都頂到 ±1500 m/s 邊界角落）這個值
在這次診斷裡總共出現了三次，證實是 DC 搆不到目標時的結構性行為，不是巧合。

### 第四階段：k_t/C_t 實驗——沒有解決問題，但意外證實了「地形飽和」這個核心機制

使用者提議「官方數字都還沒公布，先拿 `weird_test.json` 自己編的 k_t/C_t 做實驗」。
把 `k_t` 從 0.0001 降到 0.000002、`C_t` 從 11000 拉到 500000，讓 `score_time` 在
0~26 天這個範圍內不要提早飽和（原本的參數，`T` 超過半天 `score_time` 就已經趨近
0，不管等 7 天還是 26.5 天對分數完全沒差）。**實測 (`MAX_BURNS=[3]`,
`MAXITER=300`)：搜尋沒有轉向去找更早、更安全的窗口，反而找到一個更誇張的「立刻
噴、完全不管合不合法」解（ΔV_team=100,439.2 m/s，比原本的 93,259.8 還誇張）。**
再拿同樣的 `k_t/C_t`、`MAX_BURNS=[1]`、`MAXITER=2000` 跑一次更誇張：
**ΔV_team=2,911,980.8 m/s（光速的 0.97%），命中精度反而更好（Δr_min=3.0m）。**

原因：`score_dv` 對任何巨大違規值都飽和趨近 0（93,000 跟 2,911,980 對分數沒有
差別），`score_dist` 一旦命中 5km 內也封頂在 50 分（642m 跟 3m 沒有差別）——
「違規但已命中」這整塊區域對計分函式來說是一片完全平坦的高原，搜尋在這片平原上
怎麼走都是同一個分數，往哪個方向漂純粹是隨機噪聲。**這個發現直接鋪陳了下一階段
真正的答案：問題不是搜尋沒有誘因，是搜尋在一片沒有梯度的平原上，唯一的例外是
藏在裡面一根極窄的尖峰，隨機噪聲式的漂移幾乎不可能剛好撞上去。**

### 第五階段：真正的答案——單棒合法解存在，只是窄到 0.0086%，L-SHADE 撞不到

使用者追問一個關鍵問題：「他只是要撞到而已又沒有要共軌，轉什麼玩意」——這個提醒
讓我意識到自己犯了一個方法論錯誤：第一階段的 MOID 分析算的是「共軌速度匹配」的
成本，不是「純撞擊」的成本；`izzo`/Lambert 本來就只要求抵達位置，不要求抵達速度，
真正該信的是 Lambert 網格，但那個網格用「天」當步長掃 `t_wait`，完全沒有解析度
去抓到 B 自己 93 分鐘軌道週期這個維度。

改用 `scipy.differential_evolution` 做全域優化直接找最小 Δv：**第一次嘗試
(`workers=-1` 平行) 卡住跑了 9 分鐘沒結果，換成手動分批平行**（每個 worker 一次
拿一大塊 t_wait 範圍在內部算完才回傳，不是每算一個點就跨行程通訊一次）**才在
31.5 秒內掃完 72,000 個點**，找到 Δv=1519.2 m/s（`t_wait≈19.84天`,
`flight_time=2小時`）——已經很接近合法。局部精細收斂（±2000s、25s 步長）找到
真正的最小值：

```
Dv = 1189.73 m/s  ✅ 合法（<1500 m/s 上限）
t_wait = 1,714,683s（19.8459 天）
flight_time = 6,800s（1.889 小時）
```

**這個窗口有多窄，實測量過**：固定 `flight_time`，只掃 `t_wait`，合法（≤1500m/s）
的範圍只有大約 **200 秒寬**，佔整個 `T_max`（2,312,639秒）的 **0.0086%**。如果
用原本網格的 600 秒步長去掃，最好也只會抓到 2735.2 m/s——完全解釋了為什麼之前
每一次分析（粗網格、M-rev、4圈掃描、MOID）都錯過它。用原本的 `k_t=0.0001,
C_t=11000` 算這組解的分數：`score_dist=50 + score_time≈0 (等 19.85 天早就飽和)
+ score_dv≈12.8 (Δv=1190 接近 C_v=1200) - 0 (無違規) ≈ 62.8 分`——**比目前搜尋
找到的「立刻噴、違規」那組 58.71 分還高，但 `MAX_BURNS=[1]` 跑過 80/300/1000/
2000 代都沒找到它。**

**⚠️ 這組 1189.73 m/s 是 Python/Lambert 預測，還沒拿真的 GMAT 驗證過**——照這個
session 一直在守的紀律，短飛行時間（1.89小時）的解過去驗證過都很準，但這組數字
本身還沒有實測資料背書，下次接手/使用者在自己機器上重跑時要記得補這一步。

### 為什麼 L-SHADE（正式演算法）找不到，但一支手寫網格腳本反而找到

兩邊都查證過，不是「憑感覺」：

- **單次評估速度**：正式的 `fast_fitness_evaluator` 整個函式都是 `@njit` 編譯，
  300,000 次評估只要 36 秒（120 微秒/次）；手寫的診斷腳本雖然底層一樣呼叫
  `propagate_dop853`/`izzo`，但外層迴圈是純 Python（函式呼叫、`try/except`、
  建立 numpy 陣列都有直譯器開銷），72,000 次評估要 31.5 秒（跨 10 個 process
  平行，換算單一 process 序列時間約 4.4 毫秒/次）——**單次評估其實慢了 36 倍**。
- **但网格贏在「保證不漏看」**：L-SHADE 是隨機演化搜尋，在這種「幾乎全平坦、
  只有一根窄尖峰」的地形上完全沒有梯度可以引導，300,000 次評估大部分都在平原
  上做無意義的漂移；網格搜尋雖然單次慢，但步長只要夠細（跟 B 的軌道週期同量級），
  結構性保證不會跳過任何一個比步長還寬的窗口，反而用比較少的評估次數就撞到答案。
- **這不代表網格搜尋整體比較好**：網格搜尋的成本隨維度指數成長，2 維可行，13 維
  （3 棒）以上完全不可行；L-SHADE 的成本隨維度接近線性成長，這正是它存在的意義。
  兩者是不同場景的工具，不是取代關係。

### 有價值的後續方向（記錄，還沒動工）

真正有意義的啟發是**混合**：用像這次一樣便宜的低維度網格/粗掃，找出候選時間窗口，
拿去當 L-SHADE 初始族群的「種子」（不是取代隨機初始化，是額外加進去），讓搜尋從
已知不錯的起點開始探索其他維度（瞄準偏移、多棒方向）。這個方向剛好呼應「還沒做」
清單裡原本就有、但投報比不確定沒動手的「物理直覺的初始種子」——今天這個發現算是
幫那個提案補上了一個具體、有實測根據的理由，但**這個情境（77度傾角差的極端測試）
是自己編的壓力測試，不是官方題目，值不值得為它投入這個工程量，決定先記錄、緩緩，
等正式測資公布後再評估要不要做**。

### 第六階段：真的動手做了種子機制，過程中踩了三個坑（2026-08-14 晚上）

使用者拍板「做做看」之後，實作 `MissionOptimizer._generate_seed_candidates()`：粗掃
A 距地距離找候選窗口 → 中解析度 Lambert 掃描篩選 → 細網格精修排名前面的候選 →
轉成決策向量，佔初始族群 ~5%（`max(1, round(pop_size*0.05))`，是上限不是保證，真正
種子數受限於實際找到幾個候選窗口）。過程中連續踩到三個坑，都是先觀察到「加了種子
但結果沒變」這個現象，一步步拆解才找到：

1. **對齊到錯的時間**：一開始讓候選 `t_wait` 直接對齊 A 靠近地球的時間，但真正該
   對齊的是「抵達時間」(`t_wait+flight_time`)，不是出發時間——`flight_time` 拉長
   時兩者可以差到快一整天。使用者一句「近地點應該設定成撞擊時間才對？」點出問題，
   改成 `flight_time` 放外層迴圈、對每個 `flight_time` 反推對應的 `t_wait` 搜尋
   中心（`center_t - ft`）才修正。
2. **種子沒做最終安檢**：4 個種子有 3 個評分是 0——`_generate_seed_candidates` 只
   最小化 Δv，沒有檢查 `check_constraints`（轉移軌道途中會不會擦地球），補上跟
   `fast_fitness_evaluator` 最終安檢同一套 `check_constraints` 過濾才修正。
3. **種子放進初始族群沒用**：種子只佔族群 5%，L-SHADE 的 current-to-pbest 變異策略
   會把整個族群往目前最好的方向拉，種子還沒收斂就先被稀釋掉（實測過：一個很接近
   真正最佳值的種子，演化完只從 -44.1064 移動到 -44.1180，幾乎沒變）。改用梯度法
   （`scipy.optimize.minimize(method='L-BFGS-B')`）事後單獨精修種子也不夠——Δv
   超標的懲罰是離散跳躍（`penalty_count`），不是平滑漸變，梯度法在這種斷點附近會
   瞎眼，明明只差幾百秒就跨進合法範圍完全不會移動過去。**最終解法**：在種子生成
   內部就用細網格（±數百秒等級，比窗口寬度還細）鎖定精確位置，種子丟進族群前就
   已經很接近答案；`_optimize_burn_case` 裡再對每個原始種子（不受族群干擾）獨立
   做一次梯度精修當最後一道保險。

修完後端到端驗證（`weird_test.json`，`k_v=0.05`）：找到 68.1646 分的合法解（Δv≈
1198 m/s，對應第五階段找到的窄窗），跟不合法但 Δv 絕對值更小的解相比分數更高，
證明比較機制是誠實的——不是隨便什麼答案都會贏，是真的看分數。

**config 統一**：這個過程中累積了好幾個互相有點drift的測試檔案（`weird_test.json`、
`weird_test_slow_time_decay.json`、還有幾個臨時的），使用者要求統一只留一個，
確認了 `orbit_A.TA=17`（近拱點附近出發）、規則沿用使用者自訂的 `k_t/C_t/k_v/C_v`、
`MAX_BURNS=[1]`，刪掉了 `weird_test_slow_time_decay.json`。**再次提醒：這個檔案
裡所有數字（軌道六根數跟 `k_t/C_t/k_v/C_v`）都是自己編的佔位數字，主辦方從沒
公布過，不能當真。**

**穩健性驗證**：單獨掃 `C_t`（0.13~25天）、`C_t`×`k_v` 聯合掃（135組合），確認
「day-19.85 那個窗口贏」這個結論在絕大多數合理參數組合下都成立，只有 2 組窄幅
組合會讓「day-6」窗口反超；也測過放寬局部搜尋範圍，結果反而更差，證實真正的
最佳值就是一根不能靠加寬網格改善的窄尖峰。另外也實測比較過「機率密度分布取樣」
這個替代方案（使用者主動提議）：就算用抵達時間對齊、2000個樣本、頻寬窄到
100~3000秒，都是 0 個合法解命中——因為合法窗口在 `t_wait` 跟 `flight_time`
兩個維度上都窄（二維窄縫），只收緊其中一維沒有用，機率取樣的命中率是兩個維度
機率的乘積，比單一維度窄還要再窄一個量級；網格搜尋「保證不漏看」這個特性，
在這種地形上是必要條件，不是可有可無的選項。

## 夜間自主開發：三大方向的探索與驗證（2026-08-15 凌晨）

使用者睡前給了三個方向的大方向指示，要求先出計畫、確認後整晚自主開發+測試，完成
後寫報告。這節記錄實際做了什麼、測了什麼、哪些做出來了、哪些測了發現沒用直接放棄。
全部工作在 `overnight-generalization` 分支上進行，每個階段獨立 commit。

### 方向一：種子機制推廣到高傾角差——確認是真的問題，已修好

**假設驗證**：固定 A/B 的 `RAAN` 相同（讓相對傾角差直接等於 `|INC_A-INC_B|`，
球面三角公式在 RAAN 差為 0 時會化簡成這樣），離心率壓低到 0.2 排除干擾，近地點
半徑固定跟 B 軌道接近（7500km vs 6800km，避免半徑差本身造成超標蓋掉傾角效應）。
掃傾角差 5°~170°，量測合法窗口寬度：

| 傾角差 | 5° | 15° | 30° | 45° | 60° | 75° | 90° | 110° | 130° | 150° | 170° |
|---|---|---|---|---|---|---|---|---|---|---|---|
| 窗寬(s) | 2620 | 900 | 460 | 320 | 260 | 240 | 220 | 240 | 280 | 420 | 660 |

窗寬從 5° 的 2620 秒一路窄到 90° 附近的 220 秒（掉了將近 12 倍），過了 90° 又慢慢
放寬——確認傾角差是獨立於離心率之外、真實存在的另一種窄窗成因，不是離心率窄窗的
變形。腳本：`scratch_overnight/inc_sweep2.py`。

**現有機制的真實弱點**：固定在窗寬最窄的 90°，改掃 `AOP`（近地點在軌道平面上的
方位角）。原本的種子機制只用「A 離地球最近的時間」當候選來源，這個訊號只跟離心率
有關，跟傾角差造成的窄窗（真正該看的是「A 何時靠近 B 的軌道平面／節線附近」）是
兩回事——`AOP=90°` 時種子找到 8684.2 m/s，但真正全域最佳只要 1140.8 m/s，差了
7543 m/s，種子機制完全找偏。腳本：`scratch_overnight/inc_aop_adversarial.py`。

**修法**：在 [`_generate_seed_candidates`](src/optimizer.py:348) 的第一步粗掃迴圈裡，
同時算兩個指標（共用同一次 `propagate_dop853`，不用多傳播一次）：A 距地距離（既有，
抓離心率窄窗）、A 到 B 軌道平面的垂直距離 `|r_A(t)·h_B_hat|`（新增，抓傾角窄窗，
`h_B_hat` 是 B 在 t=0 時刻的軌道平面法向量，只當一個便宜的近似候選來源，不要求
精準——精修交給後面既有的中解析度/細網格 Lambert 流程）。兩種來源各自找局部極小值、
各自排序取前 `max(n_seeds,3)` 名，合併去重（`dict.fromkeys` 保序去重）當
`candidate_idxs`，後面的流程完全不用改。

修完後同樣的 AOP 對抗測試：最慘案例從差 7543 m/s 降到差 189 m/s（`AOP=45°`），
大部分案例落在 ±50~200 m/s 內，其中幾個甚至比暴力對照組還好（種子機制自己的
細網格解析度比我拿來當 ground truth 的暴力掃描更細）。原本的離心率案例
（`weird_test.json`）重跑一次迴歸測試，找到 1183.0 m/s（比之前記錄的真正最小值
1189.73 m/s 還好一點點，種子數從 4 個變成 5 個），沒有變差。Commit: `672896e`。

### 方向三：自動拆分超標單棒——測了三種做法，全部失敗，是有意義的負面結果

規則是**每一棒個別檢查 Δv≤1500**（[src/optimizer.py:83](src/optimizer.py:83)、
[src/optimizer.py:176](src/optimizer.py:176)，不是團隊總和上限），理論上把超標的
單棒拆成兩棒、間隔剛好 `MIN_MANEUVER_INTERVAL_SEC=100s`，應該能把「單棒違規」變成
「兩棒都合規」。實際測了三種拆法，全部失敗：

1. **天真等分**（方向相同、大小除以2，中間真的用 `propagate_dop853` 傳播100秒）：
   誤差 13000~18000 公里，兩棒都安檢不過。根因：Lambert 解出來的 `v1` 只在「這個
   精確時刻、這個精確速度出發」才成立，只燒一半的量、用錯誤的中間速度滑行100秒，
   早就不在原本假設的那條弧線上了，剩下的路徑是完全不同的問題，不能只補「剩下
   一半的向量」。
2. **等分+最後一棒重新解 Lambert 校正**：還是失敗，需要的修正燃燒暴增到約
   17000 m/s——這組測試案例本身時序敏感度就很高（跟今晚離心率/傾角窄窗的發現
   一致），100秒的滑行造成的偏移，換算成 Lambert 修正所需的 Δv 相當可觀。
3. **真正的局部最佳化**（用 Nelder-Mead 對「燒完第一棒後的速度」這個自由變數找
   最省油的拆法，第二棒用 Lambert 從落點反推）：收斂到「第一棒 Δv→0」，等於
   放棄拆分、把問題完整丟給第二棒——確認在這個時間尺度（100秒間隔）內，這組測試
   案例附近根本不存在比原本單棒更好的二段式拆法。

腳本：`scratch_overnight/split_test1.py` ~ `split_test4_optimized.py`。**結論**：
不會做獨立的自動拆分機制——拆分只有在第二棒時機完全自由（不受
`MIN_MANEUVER_INTERVAL_SEC` 綁死在第一棒旁邊）才有意義，那已經是方向二「真正的
多棒搜尋」在做的事，不是單棒結果的廉價後製。Commit: `3b4c59c`。

### 方向二：多棒（多維）種子機制——分段貪婪接力，實測有效

**做法**：[`_generate_multiburn_seed_candidates`](src/optimizer.py:521) 不是對
高維度重做網格搜尋（維度詛咒：25點網格在 9 維會爆炸成 25⁹≈4萬億個點），而是
「分段貪婪接力」——遞迴呼叫單棒種子生成拿到 `(t_wait_final, final_leg_frac)`
這組已知有效的時機答案，前面 `num_burns-1` 棒全部塞「Δv=0 的空燒、卡最短間隔
滑行」，讓 `current_time` 精確疊加到 `t_wait_final`，最後一棒直接沿用單棒的答案。
重播出來的軌跡跟直接單棒解完全等價（前面幾棒等於沒發生過），是個保守但保證合法
的起點，L-SHADE 自己會在這個起點附近試探「前面幾棒到底該不該真的燒」。

過程中順便修了一個真的 bug：種子的獨立 NLP 精修（stage 4）原本的容忍度規則寫死
只認識單棒的 5 元素陣列（`i<2` 是嚴格容忍度、其餘寬鬆），多棒的陣列索引對不上，
精修會用錯規則。把 `refine_trajectory()` 裡本來就正確、通用的規則抽成共用方法
[`_narrow_tolerance_bounds`](src/optimizer.py:521)，兩處都改用它。

**A/B 測試**（`weird_test.json`，縮減預算 `MAXITER=300,POPSIZE=10` 方便快速迭代，
正式預算是 `MAXITER=1000,POPSIZE=20`）：

| 案例 | 分數 | 備註 |
|---|---|---|
| 1棒（有種子） | -76.9302 | 迴歸測試，跟先前記錄一致 |
| 2棒（無種子，baseline） | -64.3348 | 比1棒還差——純隨機在9維空間明顯吃虧 |
| 2棒（有種子） | -78.2005 | 贏過1棒也贏過2棒baseline |
| 3棒（無種子，baseline） | -64.3347 | 幾乎跟2棒baseline一模一樣——沒有種子時，維度再高也榨不出額外效益 |
| 3棒（有種子） | -78.7301 | 目前最好 |

清楚的模式：沒有種子時，不管給幾棒，DE 都卡在同一個分數（-64.33 左右）榨不出
額外維度的價值；有種子時，棒數越多分數越好。腳本：`scratch_overnight/mb_seeded_test.py`、
`mb_3burn_test.py`、`mb_3burn_baseline.py`。Commit: `dae3e3e`。

> **⚠️ 2026-08-15 更正**：上面這句「有種子時，棒數越多分數越好」**在正式預算下
> 不成立**，已被實測推翻——正式預算跑出來 2 棒 (-79.04) 反而贏過 3 棒 (-78.73)，
> 而且兩個多棒解的中間棒 Δv 都恰好是 0（退化成單棒），分數差異只是落在窄窗不同
> 位置的運氣。**但這不代表種子機制或多棒機制有問題**：真正的原因是這組情境本來
> 就存在合法單棒解，多燒一棒只會浪費 Δv。詳見下面「2026-08-15 白天」那一節，
> 那裡還有一組「單棒數學上不可能」的測資，多棒在那組確實找出了真實的兩棒解。
> 「沒有種子時卡在 -64.33」這半句則完全成立，正式預算下重測到小數點後四位相同。

**誠實的但書——這次贏的「多棒優勢」實際上不是教科書等級的策略**：把上面 2棒
解答案拆開看（`scratch_overnight/decode_2burn_solution.py`），第一棒 634.0 m/s、
第二棒 558.8 m/s，兩棒相隔只有 100 秒（最短間隔）、方向夾角只差 0.1 度——幾乎
是同一棒硬拆成兩份，跟「在遠地點用小燃燒轉平面、近地點再省油攔截」這種真正省油
的多棒策略完全不像。總 Δv (1192.8 m/s) 只比同時間點單棒直接打 (1197.9 m/s) 便宜
5.1 m/s，幾乎是雜訊等級。**分數差距 (-78.20 vs -76.93) 主要不是來自 Δv 省了多少，
更可能是瞄準點偏移 (offset_r/theta/phi) 這三個自由維度在多棒版本裡被重新找到了
一個命中精度/時間分數權衡更好的點**——種子機制確實讓多棒搜尋在同樣預算下比沒種子
好、也比單棒好，這個結論站得住腳，但「多棒省油的具體物理機制」這次的實測案例沒有
乾淨示範出來，見下面的白話說明。

#### 白話說明：多棒到底在幫什麼忙，該往哪個方向拆？（使用者要求解釋）

真正的軌道力學裡，多次推進通常在下面幾種情況比一次推進省油，都是有明確方向性的：

1. **在移動快的地方省 Δv 買到的軌道改變比較多（Oberth 效應）**：同樣的 Δv，在
   速度快的地方（例如近地點）燒，換到的軌道能量變化比在慢速的地方燒更大。所以
   「先在慢速的地方做小調整，再到快速的地方做主要的攔截燃燒」有機會比在同一個
   不上不下的地方硬燒一次省油。
2. **平面轉向要挑速度慢的地方做（這個最常見，也最直覺）**：改變軌道傾角（平面
   轉向）需要的 Δv 大致跟「當下速度 × 轉向角度」成正比——同樣要轉 77 度，在
   遠地點（慢）做遠比在近地點（快）做便宜。如果兩艘船傾角差很大（像
   `weird_test.json` 這組 77 度），理論上「先在慢速點做一次純轉平面的小燃燒，
   再到有利的時機做攔截燃燒」應該比「一次燃燒同時攔截+轉平面」省油——**這正是
   今晚方向一發現的傾角窄窗背後，教科書會建議的對策，但今晚實際測出來的 2 棒
   解沒有展示這個策略**（兩棒幾乎同時、同方向，見上面的但書），值得之後專門
   設計一個「大傾角差+有明顯遠地點」的案例去驗證這個策略是否真的會被搜尋到。
3. **拆開會讓「第二次機會」的幾何變得更好**（今晚窄窗研究的核心發現）：今天整晚
   在驗證的窄窗現象說明，有時候單一時間點的攔截幾何就是很差、沒有便宜的解——但
   如果先做一個小調整，哪怕只是把船提前挪到一個新的軌道上，過一段時間後跟目標
   的相對幾何可能整個改觀，變成一個便宜很多的攔截機會。這是「多棒」最貼近今晚
   研究主題的用途：不是省油技巧，是**製造一個原本不存在的機會**。
4. **軌道差距很大時，繞遠路反而省油（雙橢圓轉移）**：如果兩個軌道大小差很多，
   有時候「先飛到很遠的地方繞一圈、再回頭攔截」的總油耗會比「直接飛過去」少，
   雖然時間拉得比較長。這個案例裡沒有直接測到，但也是教科書上多棒策略的經典
   理由之一。

簡單說：**多棒不是「把同一個燃燒拆開來噴」，是「用額外的自由度，去換一個更好的
時機或更省油的角度」**——今晚的種子機制證明了「給定額外自由度，搜尋演算法真的
能找到比單棒更好的分數」，但還沒證明「找到的解真的展示了上面 1~4 哪一種教科書
策略」。下次如果要更有說服力地示範，該設計一個「傾角差極大+A軌道有明顯遠地點」
的案例，專門去看搜尋出來的多棒解是不是真的把轉平面的燃燒放在慢速點。

> **2026-08-15 後續**：上面這個「傾角差極大+有明顯遠地點」的案例已經做了
> (`configs/apoapsis_planechange_test.json`)，結果是**否定的，而且否定的理由
> 推翻了這一段的前提**——那個情境根本不需要轉平面，搜尋用 721 m/s 就攔截成功，
> 比「遠地點轉平面」的解析成本 1202 m/s 還便宜。原因是本節第 2 點把「平面轉向
> 成本」當成攔截成本，但任務只要求撞到位置、不要求共軌（正是第五階段那個
> 「他只是要撞到而已又沒有要共軌」的教訓）。**上面 1~4 點是通用軌道力學知識，
> 本身沒錯，但套用到「純位置攔截」這個問題時會系統性高估成本**，不能直接拿來
> 預測這個工具會找到什麼策略。真正逼出多棒的方式見下一節。

### 額外探索：其他窄窗成因？（時間有限，只做了初步、沒有定論的測試）

快速測了「軌道共振」（`T_A:T_B` 整數比：1:1、5:4、3:2、2:1，跟一個非共振對照組
`e:2≈1.359`）會不會也製造類似的窄窗效應，離心率跟傾角差都刻意壓低排除干擾：

| 週期比 | 1:1 | 5:4 | 3:2 | 2:1 | e:2 (對照) |
|---|---|---|---|---|---|
| 窗寬(s) | 396 | 1040 | 1040 | 1296 | 1180 |

只有 1:1（共軌，A、B 幾乎同軌道）明顯窄化跟 Δv 逼近上限，其餘幾個共振比
（5:4/3:2/2:1）沒有比非共振對照組窄——沒有看到乾淨的「共振→窄窗」規律，1:1 那組
比較可能是另一個機制（兩船軌道幾乎重合，彼此的相對漂移很慢，整個 `T_max` 時間
預算裡能碰頭的機會本來就少），不是傳統意義的軌道共振。腳本：
`scratch_overnight/bonus_resonance_test.py`。沒有再往下深挖，記錄成一個開放、
沒有定論的線索，不當成已確認的第三種窄窗成因。

### 這輪工作留下的檔案

這輪新增/修改的程式碼原本在 `overnight-generalization` 分支（4 個 commit：
`58bfaea` 種子機制基礎版、`672896e` 傾角節線候選來源、`3b4c59c` 自動拆分負面
結果、`dae3e3e` 多棒分段接力種子）。**該分支已經 merge 回 `Master` 並刪除
（2026-08-15 核對過 git log，四個 commit 都在 `Master` 上）。** 診斷用的腳本都在
repo 內的 `scratch_overnight/` 目錄（不是外部 scratchpad，因為這次是要留給使用者
早上一起檢視），命名大致對應上面每個小節提到的測試。

## 2026-08-15 白天：多棒機制的正式預算驗證，以及一次結論反轉

換到 WSL / 5800X 這台機器之後，補跑了夜間那輪留下的驗證 TODO。過程中先推翻了
夜間報告的一個結論，再用一組新設計的測資推翻了「推翻」本身——最後的理解跟兩邊
都不太一樣，這一節記錄完整的來龍去脈。

### 第一步：正式預算重跑夜間的 A/B 表，「棒數越多越好」不成立

夜間那張表是縮減預算 (`MAXITER=300, POPSIZE=10`) 跑的。用 `weird_test.json`
的正式預算 (`MAXITER=1000, POPSIZE=20`) 重跑同一組對照
（`scratch_overnight/full_budget_ab_validation.py`，總計 27.7 分鐘）：

| 案例 | 正式預算 (1000代) | 夜間縮減預算 (300代) |
|---|---|---|
| 1棒（有種子） | -76.9802 | -76.9302 |
| 2棒（無種子 baseline） | -64.3348 | -64.3348 |
| **2棒（有種子）** | **-79.0394** | -78.2005 |
| 3棒（無種子 baseline） | -64.3347 | -64.3347 |
| 3棒（有種子） | -78.7301 | -78.7301 |

**種子機制的價值完全確認，而且證據比夜間更強**：有種子 -77~-79 vs 無種子 -64.33，
差 13~15 分；兩個無種子 baseline 在預算加大 3.3 倍後**分數到小數點後四位都沒動**，
坐實「沒有梯度的平原」這個診斷——不是預算不夠，是搜尋根本沒有方向可循。

**但「棒數越多分數越好」不成立**：正式預算下 2 棒反而贏過 3 棒。把三個解拆開看
每一棒實際燒多少（`scratch_overnight/decode_full_budget_solutions.py`）：

| 案例 | 實際燃燒結構 | 總 Δv |
|---|---|---|
| 1棒 | 單棒 1196.79 m/s | 1196.79 |
| 2棒 | 中間棒 1190.01 + **最後棒 0.000** | 1190.01 |
| 3棒 | 中間棒 **0.000** + 中間棒 **0.000** + 最後棒 1191.05 | 1191.05 |

兩個多棒解都是**退化的單棒解**——多出來的棒 Δv 恰好是 0，就是分段貪婪種子放進去
的「空燒」結構，L-SHADE 從頭到尾沒離開過那個起點。三個案例的攔截時刻完全一樣
(19.9246 天)、瞄準偏移都頂在軟上限 3.5km、`t_wait` 全部落在第五階段那個 200 秒寬的
窄窗裡。分數差異完全由 Δv 差異解釋 (1196.8/1190.0/1191.1)，而 Δv 差異來自**每次剛好
落在窄窗的哪個位置**，是運氣不是策略。

（附帶一個有意思的觀察：夜間縮減預算下 2 棒還有兩把真實燃燒 634+558.8 m/s，正式
預算反而把它收斂回單棒了——因為單棒真的比較省 1190.0 < 1192.8，多給預算讓 L-SHADE
發現拆分是浪費。）

### 第二步：設計「傾角差 90 度 + 明顯遠地點」的案例，結果否定，而且否定了自己的前提

夜間報告留的 TODO 說要設計這種案例去逼出「遠地點轉平面」策略。做了
（`configs/apoapsis_planechange_test.json`，設計腳本
`scratch_overnight/design_apoapsis_planechange_case.py`）：A/B 都是大橢圓
(SMA 50000/48000, ECC 0.85/0.84，刻意留在已驗證安全的 ECC<=0.9 範圍內，
避免傳播誤差變成干擾變因)，傾角差整整 90 度。解析算出來 90 度平面轉向在 B 近地點
要 13,820 m/s（超標 9 倍）、在遠地點只要 1,202 m/s（合法），差 11.5 倍——看起來
有明確的教科書答案。

**實際跑出來完全不是那樣**：搜尋只花 **721.3 m/s** 就攔截成功（Score 95.53，0 違規），
比我算的「最佳策略」還便宜 40%，而且燃燒點在 r=42,180 km、v=3.25 km/s，**不在遠地點**。

**根因是我的設計前提錯了**：我用「90 度平面轉向」的成本當教科書答案，但這個任務
**只要求撞到 A 的位置、不要求共軌**。Lambert 只需要抵達位置，不需要匹配速度——在
4 萬公里處做一個小角度偏轉，到 8 萬公里外的目標處就放大成很大的橫向位移，根本
不需要昂貴的平面轉向。**這正是第五階段那句「他只是要撞到而已又沒有要共軌」推翻
MOID 分析的同一個錯誤，我又踩了一次。**

三個燃燒次數案例這次收斂到**完全相同**的目標值 -95.5295（小數點後四位），多棒退化
第二次確認，而且證據比第一次更強（連分數差異都沒有了）。GMAT 吻合度：預測
3500.002 m vs 實測 3487.139 m，差 **13 公尺**——順帶在一個全新情境上再驗證了
「ECC<=0.9 安全」這條結論。

### 第三步：設計「單棒數學上不可能」的案例——多棒機制確實會動，結論反轉

前兩組情境都有一個共同點：**存在合法的單棒解**。所以「多棒退化成單棒」可能根本
不是缺陷，而是正確行為。要分辨，得設計一組單棒在數學上不可能合法的情境。

`configs/perigee_kick_test.json`（設計 `design_perigee_kick_case.py`，可行性驗證
`verify_2burn_feasible.py`）：B 在 6,800 km 圓軌道，A 在 42,164 km（GEO 高度）
傾角 5 度。要把遠地點抬到 A 的半徑，**能量下限就是 2,391 m/s**——這不是某個策略的
成本，是物理下限，任何方向任何時機都不可能更便宜，而它超過每棒 1,500 m/s 的規則上限。

**這次記取上一步的教訓，兩層都用實掃驗證，不用解析推理**：
1. 單棒 Lambert 網格實掃 39,200 組 `(t_burn, flight_time, 方向)`：**合法解 0 個**，
   最好的也要 2,418.8 m/s（超標 919）。而且網格實測值跟理論下限 2,391.4 只差 27 m/s，
   這次理論跟實際對得上。
2. 2 棒近地點推進在含 5 度傾角的真實 3D 幾何下：粗網格找到 **174 組**兩棒都合法的
   組合，最省 2,558.2 m/s——先確認測試不會是空的（0 度時有 1,399 組太好找、10 度時
   只剩 1 組太刀鋒可能是網格假象，所以選 5 度）。

**結果——多棒機制確實會動，而且找到的解比我設計的更好**：

| 案例 | 分數 | 狀態 |
|---|---|---|
| 1 棒 | -79.2609 | 違規（數學上必然） |
| **2 棒** | **-88.2746** | ✅ 完全合法，勝出 |
| 3 棒 | -85.3943 | 合法但較差 |

```
[Burn 1]      t=16,456.1s  |  1,283.0 m/s  |  r=6,793.1 km, v=7.664 km/s
[Final Burn]  t=16,556.1s  |  1,151.4 m/s  |  r=6,793.2 km, v=7.664 km/s
總 ΔV_team = 2,434.4 m/s   |  違規 0 次    |  Score 88.22/100
```

兩把都是真實燃燒，不再是 Δv=0 的佔位。而且策略比我預想的聰明：**兩棒都精準落在
近地點**（r=6,793.1 vs 近地點 6,793.2 km，Oberth 效率最高處），**間隔剛好 100 秒**
（規則最短間隔）而不是我預想的「等一整圈 10,331s 回到近地點」——B 在 100 秒內幾乎
沒移動，拆分損失趨近於零，又省下等一圈的時間分數。總 Δv 只比物理下限高 **1.8%**。
（我手算預測 2 棒會拿 81.87 分，實際 88.22——最佳化器比手算聰明。）
GMAT 驗證：預測 3,420.375 m vs 實測 3,421.018 m，**差 0.6 公尺**。

**這也順帶驗證了第二階段那個判斷**：當初「同方向硬拆 8 棒反而更貴」的失敗，原因是
硬拆破壞了 Lambert 解的前提；而這裡第一棒是自由向量、最後一棒**重新解 Lambert**，
所以拆分是有效的——正是第二階段寫的「每一棒的方向/滑行時間該是各自獨立的自由變數」。

### 這輪的統一結論

**多棒退化成單棒是正確行為，不是缺陷。** 機制在單棒就夠用的情境下收斂回單棒
（多燒一棒只會浪費 Δv、拉低分數），在單棒不可能的情境下確實找出真實的多棒解，
而且接近最優。夜間報告「多棒種子機制有效」這個**結論是對的，只是它用來佐證的
測資（`weird_test.json`）剛好不需要多棒，所以那組證據本身無效**——結論對、論證錯。

### 順便修的：GMAT R2026a 顏色欄位棄用（`829afac`）

在 WSL 上開 GMAT GUI 時發現產生的腳本會噴 4 個 `field name is no longer in use`
警告：`OrbitView.OrbitColor` / `OrbitView.TargetColor` 在 R2026a 已經移除，設了會被
靜默忽略（所以 3D 視窗裡 ShipA/ShipB 其實一直沒有顏色區分）。改設在 Spacecraft
物件上（`ShipA.OrbitColor = Red` 等）。**這件事會影響繳交檔案**——主辦方拿
`output_submit.txt` 去跑也會看到那些警告。修完實測：GmatConsole 0 警告、完整流程
GMAT 驗證通過、0 個非 ASCII 字元、繳交版本仍然 0 個 `Target/Vary/Achieve`。

## 🔴 2026-08-15 傍晚：傳播器容忍度太鬆，高離心率軌道會安靜地算錯

**這節可能推翻先前用 `weird_test.json` 得到的部分結論，接手前先讀完。**

使用者在自己機器上跑一組自編情境 (`orbit_A` SMA=70,000 / ECC=0.9 / 近地點 7,000km)
回報：Python 預測命中 3,499.8m，**GMAT 實測 88,228m**。

### 根因：積分誤差集中在近地點通過，而預設容忍度攔不住

`GRAVITY_DEGREE=0` 代表兩邊都是純二體，而純二體**有解析解** (`farnocchia_rv`)，
所以可以直接量出誰錯。查證順序 (每一層都排除掉一個假設)：

- GMAT 的 `GravityField.Earth.Degree = 0` 是不是真的點質量？→ **是**，跟解析解差 1 公尺。
- 兩邊力學模型有沒有不一致 (第三體/阻力/SRP)？→ **沒有**，腳本裡都關掉了。
- Python 的積分誤差？→ **就是這個。**

用解析解當基準量 `propagate_dop853` (舊預設 `rtol=1e-9, atol=1e-6`)：

| 傳播到 | 近地點通過次數 | 誤差 |
|---|---|---|
| 0.5 週期 | 0 | 0.00 km |
| 1 週期 | 1 | 0.08 km |
| 2 週期 | 2 | **18.95 km** |
| 3 週期 | 3 | **38.35 km** |
| 4 週期 | 4 | **90.35 km** ← 對得上使用者看到的 88km |

**決定誤差的是近地點通過次數，不是總傳播時長**——前半圈還沒過近地點時是 0.00 km，
之後每過一次就跳一大階。這實證了 8/15 白天推論出來但一直沒機會測的那條。

### 修正：預設收緊到 `rtol=1e-12, atol=1e-9`

收緊後上述誤差**全部歸零** (跟解析解差 0.000 km)。使用者那組情境重跑：
GMAT 差距從 **88,228m 降到 0.154m**。

代價：純傳播器 benchmark 慢 1.8~2.9 倍。**端到端的成本跟情境高度相關，不能只量一組**：
`playground.json` 幾乎沒差 (50.59s vs 50.40s)，因為 Lambert/NLP 等固定開銷把差距稀釋
掉了；但 `weird_test.json` 的完整 A/B 表慢 **2.85 倍** (27.7 分 → 78.9 分，見下面
「正式預算 A/B 重驗」)。我一開始只拿 `playground` 就寫下「端到端幾乎沒差」，那是
**取樣不足**——差別在於有沒有大量高離心率傳播跟種子精修。
低偏心軌道在舊容忍度下本來就精確，等於白付成本，但「安靜地算錯」比「慢一點」
嚴重太多，所以預設選安全的那邊。

### 重驗結果 (`scratch_overnight/reverify_weird_test.py`)

發現這個 bug 的當下，我判斷它「可能推翻 weird_test 的整套結論」而且「第三層模型
分岔的主因根本不是缺 J3/J4，是容忍度」。**實際量過之後，這兩個判斷都是錯的。**
記在這裡當教訓：**抓到一個 bug 之後很容易高估它的解釋範圍，順手把原本正確的診斷
也推翻掉。要先量再說**——這正是這份文件反覆在講的那條原則，我自己又犯了一次。

**1. 窄窗結論完全成立，沒有被推翻。** 用 `rtol=1e-14` 當真值重掃：

| | 最小 Δv | 位置 | 窗寬 |
|---|---|---|---|
| 舊容忍度 | 1,191.7 m/s | t_wait=1,714,683s | 200s (0.0086%) |
| 新容忍度 | 1,191.7 m/s | t_wait=1,714,683s | 200s (0.0086%) |

完全一樣。A 在攔截時刻的位置誤差是 **12.9 km**（不是我先估的 223 km——那是拿完整
T_max 量的，而這個解只用到 74%），但 12.9 km 攤在 6,800 秒的轉移裡只需要約 1.9 m/s
修正，而且大多是沿軌道方向會互相抵銷，最後只影響 **0.04 m/s**。

**但實務上仍有重要差別**：Lambert 瞄準的是「你告訴它 A 在哪」。Δv 幾乎沒變，可是
如果 A 實際上在 12.9 km 外，就會**直接脫靶 12.9 公里**，遠超 5km 門檻。所以
「窗口在哪、要花多少 Δv」這些結論沒錯，錯的是**實際命中距離**。

**2. 第三層的歸因：原本的診斷是對的，缺 J3/J4 才是主因。** 同一條 A 軌道傳播到
攔截時刻，交叉比較重力階數 × 容忍度：

| 組合 | 跟真值的差距 |
|---|---|
| J2+J3+J4, 新容忍度 | 0.006 km |
| J2+J3+J4, 舊容忍度 | 12.933 km |
| 只有 J2, 新容忍度 | 49.093 km |
| 純點質量, 新容忍度 | 66,676 km |

容忍度的影響約 12.9 km，缺 J3/J4 的影響約 49.1 km——**後者是前者的 3.8 倍**。
所以「拆解 GMAT 對不上的真正原因」那節把第三層歸給重力模型是**正確的**，
補 J3/J4 是治本，不是我以為的副作用。

**3. 8/15 白天所有「GMAT 吻合到公尺等級」的結果不受影響**——那些情境的傳播時間短、
近地點通過次數少，本來就在誤差還沒累積的區間。

**4. 但容忍度 bug 本身仍然是真的、仍然該修**——使用者那組 ECC=0.9 情境的 88km
落差就是它造成的（那組 `GRAVITY_DEGREE=0`，兩邊重力模型完全一致，唯一變因就是
容忍度）。只是它的影響範圍比一開始以為的窄：要「高離心率 **且** 多次近地點通過」
同時成立，才會放大到公里等級。

### 同時修的：階梯種子在 `num_burns=2` 時產生 0 個

使用者另一組情境 (A 的 ECC 改 0.4，能量下限 2,386.7 m/s = 1.6 倍上限) 跑出退化解
(中間棒 Δv=0、最後一棒 2,813.2 m/s 違規、Score 67.86)。查出來階梯種子觸發了但
一個都沒生出來，兩個原因疊加：

- 最後一段 Lambert 只固定掃 40 點，但 `max_final` 可以長達 724,000s (每格 18,500s)，
  而 A 的週期只有 184,314s——每圈只取樣 10 個點，比可命中的窗口粗太多。
  改成**依「這段時間 A 會繞幾圈」動態決定點數** (每圈至少 60 點，夾在 60~600)。
- `n_climb==1` 時「平均分配」跟「每棒燒滿」退化成同一個方案，等於爬升高度沒得選。
  改成沿 `floor` 的比例掃 4 檔 (1.0/0.85/0.7/0.55) 再去重。

修完那組情境：**Score 67.86 → 77.76，違規 1 次 → 0 次**，中間棒變成真實的
1,486.9 m/s，GMAT 吻合 1.875m。`hard_mode` 的 3 棒種子也從 5 個增加到 7 個；
2 棒仍然是 0 個，那是**正確的**(已證明該情境無合法 2 棒解)。

### 正式預算 A/B 重驗：舊表的「棒數差異」有一半是積分誤差

容忍度改完之後，用**同樣的正式預算** (`MAXITER=1000, POPSIZE=20`) 把 `weird_test.json`
的五案例 A/B 表整個重跑一次 (`scratch_overnight/full_budget_ab_validation.py`)，
直接跟舊容忍度的那輪逐格對照：

| 案例 | 舊分數 | 新分數 | 舊耗時 | 新耗時 | 慢幾倍 |
|---|---|---|---|---|---|
| 1棒 (有種子) | 76.98 | **78.78** | 280.2s | 951.4s | 3.40x |
| 2棒 (無種子 baseline) | 64.3348 | 64.3348 | 58.8s | 104.1s | 1.77x |
| 2棒 (有種子) | 79.04 | **78.78** | 470.6s | 1464.5s | 3.11x |
| 3棒 (無種子 baseline) | 64.3347 | 64.3347 | 92.8s | 181.2s | 1.95x |
| 3棒 (有種子) | 78.73 | **78.78** | 754.9s | 2028.5s | 2.69x |
| **總計** | | | **27.7 分** | **78.9 分** | **2.85x** |

> ⚠️ 讀這張表前先注意**符號**：腳本印的「分數」是 mealpy 的適應度，
> [`optimizer.py`](src/optimizer.py:196) 是 `return -score` 且 `minmax="min"`，
> 所以原始 log 裡是負數、**負得越多越好**。上表已經還原成正的實際分數。
> (我自己在報告過程中把方向講反過一次，特此標明。)

**1. 三個有種子案例現在收斂到同一個數字 (78.7832)。** 舊容忍度下它們是
76.98 / 79.04 / 78.73，散布 2.06 分。那個散布**整個是積分誤差造成的**，不是真的
最佳化差異。收緊之後 1/2/3 棒找到的是**同一個解**——多出來的棒次正確退化成 Δv=0
空燒，這正是「單棒就夠用的情境」該有的行為。

順帶，舊版 2 棒的 79.04 比現在的 78.78 高，是**灌水**：傳播器算錯讓它以為打得比
實際準。三個案例現在互相對得上，是可信度提高而不是變差。這也補強了上面那節
「棒數越多分數越高」不成立的結論——連那 2 分的差距都不是真的。

**2. 兩個無種子 baseline 逐位元不變** (64.3348 / 64.3347，小數點後四位完全相同)。
把積分誤差從公里級壓到公尺級，一位數都沒動。這把「無種子搜尋卡在計分函式的**無梯度
平台**上」從推測變成確認：如果它真的在跟隨地形，不可能對傳播精度完全免疫。
種子機制的 **+14.45 分**優勢因此完全成立，而且跟傳播精度無關。

**3. 成本集中在種子的獨立 L-BFGS-B 精修**：有種子的案例慢 2.7~3.4 倍，無種子只有
1.8~2.0 倍。原因是每次有限差分 Jacobian 要對每個維度各跑一次完整傳播，容忍度收緊
直接乘上去，再乘上種子數。耗時也大致跟種子數成正比 (1棒 5 個種子 951s、2棒 9 個
1465s)，3 棒同樣 9 個種子但決策向量 9 維→13 維，所以再貴一截。

## 2026-08-25：規則第 6 節「平手判定」實作（初賽延到 9/5）

**背景**：官方 8/25 來信，因沙德爾颱風把初賽從 8/29 延後一週到 **9/5（六）**。多出來的
一週不太可能等到題目公布（連延賽通知都只說「時間地點近日另行寄發」），所以挑了唯一
一件「規則 PDF 已經白紙黑字寫死、不用等公告」的事來做：第 6 節的平手判定。

規則原文（`rules/Regulations_PrelimRound-20260605.pdf` §6，這次用 `uv run --with pypdf`
抽文字才第一次真的讀到原文，之前是靠筆記轉述）：

| 優先序 | 判準 | 排序 |
|---|---|---|
| 1 | Minimum Relative Distance `d_min,team` | 小者前 |
| 2 | Total Velocity Increment `ΔV_team` | 少者前 |
| 3 | Mission Completion Time `T_team` | 短者前 |
| 4 | Design Theory | 同分隊伍上台講 5 分鐘方法論 |

### 修掉的真問題：贏家是「誰先跑完誰贏」

`run_study()` 舊版在 `as_completed` 迴圈裡用 `best_score < best_overall_score`（**嚴格**
小於）即時挑贏家。分數打平時留下的是先回來的那個，而完成順序由作業系統的行程排程
決定——**同一份設定重跑兩次可能交出不同方案**。而且規則裡根本沒有「誰先跑完」這條。

分數打平在這個工具裡是常態不是特例：計分函式在 `Δr ≤ 5km` 內完全平坦、燃料/時間
sigmoid 飽和時也平坦、多棒解又常退化成跟少棒解同一個解。

現在改成所有案例跑完後統一挑，挑選邏輯抽成 `MissionOptimizer._pick_best_case()`
（獨立成方法是為了可測——平手那條路在真實搜尋裡不保證跑得到）。

### 新增：收尾微調 `_tiebreak_polish()`

計分把 `Δr_min` 地板在 5km，所以 5km 內壓小 Δr **有時候**不用付分數的代價，那就是
白拿的優先序 1 優勢。這一步在「分數一分都不能少」的前提下把瞄準偏移壓到最小，
做不到就原封不動退回（分數永遠優先於平手判定）。

實測有效的是**燃料 sigmoid 飽和**的情境：飽和時 `fast_fitness_evaluator` 對 `offset_r`
的梯度**恰好是零**（實測 offset_r 0 跟 3.0 km 給出位元相同的 fitness），搜尋會把它
留在任意位置。測試裡把偏移硬設成 3,000 m，微調壓到 **0.035 m**，分數完全不變。

### 🔴 這次量出來最值得記住的數字：3.5 公里只值 0.001 分

實測 `playground`：把瞄準偏移從 0 拉到規則極限 3.5 km（其餘維度重新最佳化），
分數變化只有 **0.001 分**——比搜尋本身的重跑變異（~0.02 分）還小一個數量級。

意思是預設行為會為了 0.001 分白白讓出 3.5 km 的距離優勢，而**優先序 1 是硬性的
字典序比較，3.5km 輸給 0km 沒得商量**。如果官方驗證程式把分數四捨五入到小數點後
兩位（**我們不知道**），那 0.001 分根本不存在，這筆交易就是純虧。

所以加了 `strategy.TIEBREAK_SCORE_EPS`（預設 `1e-9` = 只認浮點數等級的完全相同）。
設成 `0.005` 就是賭「官方比到小數點後兩位」：實測 `playground` 開下去，
`Δr_min` 3,499.9 m → **0.1 m**，分數 90.6877 → 90.6862（掉 0.0015 分，兩者都是 90.67）。
工具會把實際付出的分數代價印出來，不假裝沒成本。**這是個賭注，工具不替使用者決定。**

### 規則本身的歧義：優先序 1 到底是哪個距離

第 6 節用的符號是 `d_min,team`，**全文沒有定義**；第 4/5 節計分用的是
`Δr_min = max(Δr(T_team), 5)`。兩種讀法會選出不同贏家：

- 用**原始**最近距離（工具預設）：優先序 1 對成功攔截的隊伍有效。
- 用第 4 節的地板值：所有成功的隊伍這一項全都是 5，優先序 1 直接失效，落到優先序 2。

工具不替使用者決定，但**兩種讀法選出不同方案時會明講**並把數字擺出來。這條在
`_pick_best_case()` 跟 `tests/test_tiebreak.py` 都有對應的實作與測試。

### 兩個「差點做錯」的地方（都是實測擋下來的）

1. **窄邊界讓微調白做**：`_tiebreak_polish` 一開始沿用 NLP 微調的窄邊界，結果
   3,000 m 只降到 2,475 m 就卡住——窄邊界是繞著微調前的解切的。改成只把 `offset_r`
   那一維放開到 `[0, MISS_TOLERANCE_SOFT]`，其餘維度維持窄邊界，才降得到 0。
2. **1 公分的差距選了退化的多棒方案**：`playground` 曾出現 1 棒 3,499.9 m / 2 棒
   3,498.3 m，工具為了 1.6 公尺選了 2 棒——而那多出來的一棒是 `Δv≈0` 的空燒。
   兩個修正：(a) 打平的候選**先各自跑一次收尾微調再比**（比「會交出去的那一版」，
   不是中間值）；(b) 比 `Δr` 時量化到 `TIEBREAK_MISS_RESOLUTION_M = 10 m`——GMAT
   DifferentialCorrector 的 Achieve Tolerance 就是每軸 0.01 km，Python 端 1 公分的
   差距根本傳不到交出去的腳本裡。只量化 `Δr`，不量化 `ΔV/T`（後兩者是計分直接在
   最佳化的量，數字有意義；只有 `Δr` 會出現「有差但沒意義」）。決勝尾鍵也改用
   `effective_burns()` 而不是名目棒數。

### 順帶修的 / 新增的

- `sweep_burns.py` 的平手警告從「請自己回頭翻 Mission Plan」改成**直接列出三個數字的
  表**，而且會分清楚「規則意義上真的同分」跟「只是在 `--plateau-tol` 以內」——
  `--plateau-tol` 是工程判斷（值不值得多開棒數），跟規則的同分不是同一回事。
- `strategy.TIEBREAK_POLISH`（預設 `true`）可以關掉收尾微調省時間；
  `strategy.TIEBREAK_SCORE_EPS` 上限 1.0，超過會被 `config_validator` 擋下並解釋原因。
- **新增 `tests/test_tiebreak.py`**（專案第一份測試檔，17 項，不需要 pytest，
  `uv run python tests/test_tiebreak.py` 直接跑）。

回歸測試：`smoke_test`、`playground`、`hyperbolic_smoke` 都跑過，結果與改動前一致
（`hyperbolic_smoke` 這次跑到 639.4 m/s 的兩棒解，比先前記錄的 851.2 m/s 更好，
是搜尋變異不是這次改動造成的）。


## 🔴 2026-08-28：官方公布範例題目，揪出兩個「一直在漏解」的問題

官方今天發《參賽選手注意事項》，第八節首次給了**範例題目 + 參考機動法**。這是至今
唯一一組不是自己編的測資，而它一天之內就打出兩個自製情境七組全部沒測到的問題。

賽程也確定了：**9/5（六）13:00 報到、14:00–15:30 正式競賽（固定 90 分鐘）**，
淡江新工學大樓 E213/E214，**共 57 隊、前 8 名晉級**。當天的作業手冊寫在
[CONTEST_DAY.md](CONTEST_DAY.md)。官方明文**允許使用生成式 AI**，但禁止用網路搜文獻／
開源程式碼／找場外隊友——「事先寫好帶進場的自有程式」照字面讀沒問題，但那是我們的命門，
**報到時要當面問清楚**。

### 第一件事：拿官方參考解驗自己的物理

官方參考解是 2 棒（t=0 燒 1,500 m/s、t=100 燒 741.427 m/s），總 2,241.427 m/s、
攔截 3,211.737 s。把那兩個向量當慣性座標（EarthMJ2000Eq）餵進我們的傳播器：

**t=3,211.737s 時 Δr = 2.7 公尺。**

一次確認了六根數→狀態向量的轉換、傳播器、燃燒向量的座標慣例、μ 值全部跟主辦方對得上。
**這種等級的交叉驗證是自製測資永遠給不了的。**

### 🔴 問題一：拆棒解整個家族被誤判成「會撞地球」

`check_constraints()` 比的是**密切軌道的近地點半徑**，跟太空船會不會真的飛到那裡無關。

官方參考解燒完第一棒後，中間那個暫態軌道的近地點是 **5,517 km——在地表以下 860 km**。
但太空船只在上面待 100 秒（機動間隔下限），100 秒後半徑是 7,024 km，實際飛過的高度
完全安全。舊版直接判 0 分。

而「把一發超過每棒上限的大燒拆成兩段幾乎同向的燒」正是繞過 ΔV_lim 的標準手法——
官方參考解的 B1 跟 B2 只差 **4.9°**，向量和 2,239.6 vs 純量和 2,241.4，只損失 0.08%。
**我們把整個這種家族判死。**

修正（`core_math.reaches_perigee()`）：用 Kepler 關係算出「接下來 dt 秒內會不會真的
經過近地點」。會經過才比近地點半徑；不會經過的話，這段弧的最小半徑就是**兩端取小者**
（中間就算越過遠地點也只會更高），檢查兩端即可。兩種情況合起來是**精確**的，不是放寬。

實測影響：

| | 修正前 | 修正後 |
|---|---|---|
| 官方範例題目，抵達 ≤3,300s | **找不到任何合法解**（≤5,600s 全都沒有）| **2,224.7 m/s / 3,185.4s**——比官方參考解省 0.7%、快 0.8% |
| `perigee_kick_test` | 2,434.4 m/s、Score 88.2 | **2,392.4 m/s、Score 89.28**，離能量下限 2,391 只差 **0.06%**（原本 1.8%）|

`perigee_kick` 那條是意外收穫：這不是只影響官方那一題的邊角修正。

### 🔴 問題二：Lambert 寫死 `M=0`，看不到多圈轉移

`izzo()` 的呼叫全部寫死 `M=0, lowpath=True`，等於只考慮「不繞滿一圈就直接過去」的轉移。
但 T_max 是 A 的**4 個週期**，多圈轉移本來就在規則允許範圍內。

官方範例題目實測（單棒 Lambert 網格掃描，最小合法 Δv）：

| 設定 | 最小合法 Δv | 抵達時刻 |
|---|---|---|
| `M=0, lowpath=True`（原本唯一的行為）| 430.1 m/s | 6,145 s |
| `M=1, lowpath=False` | **267.3 m/s** | 11,928 s |
| `M=2, lowpath=True` | 284.1 m/s | 17,741 s |

**省 38%。** `lowpath` 在 M=0 時沒有意義（只有一組解），但 M≥1 時同一個飛行時間有兩組
解而且差很多——上面那組 267.3 就是 `lowpath=False` 才找得到的。

改成掃過 `M ∈ [0, LAMBERT_MAX_REVS] × lowpath × 順/逆向` 取最省。實測單次評估成本
幾乎沒變（0.262 → 0.246 ms），因為飛行時間不夠繞圈時 izzo 會直接失敗、退出得很快。
`strategy.LAMBERT_MAX_REVS` 預設 **0（維持舊行為）**，要開才開。

**差點犯的錯**：一開始只改了搜尋端 `fast_fitness_evaluator`，沒改重播端
`reconstruct_mission_logs`。那會變成「搜尋照多圈解評分，但重播跟產生出來的 GMAT 腳本
是不繞圈的解」——回報的數字跟實際交出去的東西對不起來。是靠「fitness 變好但
`mission_metrics` 回報的 ΔV 一模一樣」這個矛盾抓到的。**加分支就一定要兩邊一起加。**

### 這兩個問題的共同型態

都是**「保守的防呆守則把合法解擋在外面」**。同一類的還檢查了三個：

- `MISS_TOLERANCE_SOFT`（少用 1.5 km 的命中容許範圍）：實測整個 3.5 km 範圍只值
  **0.001 分**，而且瞄得準對平手判定優先序 1 更有利 → **不動，測完排除**。
- `MAX_DV_SOFT`（每棒少燒 10 m/s）：拆棒解會有棒數頂到上限，這 10 m/s 是白丟的。
  約值 0.03 分，改成可設定 `strategy.MAX_DV_MARGIN_MPS`（預設仍 10）。
- 階梯種子裡的 `check_constraints`：那裡滑行整整一圈，**一定**會經過近地點，
  原本的嚴格判定是對的 → 不動。

### 對 `hard_mode_test` 的影響（不是 bug，但要知道）

修正後違規分支變好了 143 m/s（2,924.8 → 2,781.8），照 hard_mode 自己的計分參數，
違規解（84.01）現在**贏過**已知的合法 3 棒解（83.15）0.86 分。SCENARIOS.md 記載原本
合法解只贏 0.35 分，所以這是預期內的翻轉，搜尋照規則算是對的。

**但這表示 `hard_mode` 原始參數已經不再是「能不能找到合法解」的有效測試**——
要驗那件事得用 SCENARIOS.md 記載的診斷變體（`k_t` 壓平到 5e-7）。

#### 診斷變體的「退步」是誤判——真相更重要

一開始看到診斷變體（`k_t` 壓平）交出 1 棒違規解 Score 74.75，而 SCENARIOS.md 記載
階梯種子的驗收是 -83.86，判定成回歸。**那個比較是錯的：-83.86 是 fitness（搜尋用的
代理值），74.75 是重播算出來的真實分數，兩者不是同一個量。**

查證過程排除了幾個可能：階梯種子修正前後**位元級相同**（3 棒都是 7 個、最好 fitness
都是 -78.463、0 違規、真的用 3 棒，用 `git worktree` 開 HEAD 的乾淨副本對照過）；
重播也沒有失敗（7 個精修後的種子全部重建成功）。

真正的發現是：**代理值跟真實分數會嚴重脫節。** 把 7 個階梯種子各自做一次 L-BFGS-B
精修後對照：

| 種子 | 代理 fitness | 重播真實 Score | 落差 |
|---|---|---|---|
| 0 | -79.167 | 79.164 | 0.00 |
| 2 | -80.549 | 70.038 | **10.5** |
| 4 | -80.719 | 72.334 | **8.4** |
| 6 | -78.347 | **1.106**（ΔV 13,661 m/s）| **77** |

推測的機制：`fast_fitness_evaluator` 的最後一棒直接用 `izzo` 的**純二體** Lambert 解
算 Δv，而重播會用 `refine_lambert_burn` 修正成「含 J2/J3/J4 之後仍然命中」的 Δv。
飛行時間越長，二體解偏得越多、要補的修正量越大——hard_mode 的最後一段長達 7 天。
量出來了（`scratch_overnight/proxy_gap.py`，每個種子先做一次 L-BFGS-B 精修再比）：

| 情境 | T_max | 代理落差（最大 / 中位數）|
|---|---|---|
| `hard_mode` 變體 | 1,258,841 s（14.6 天）| **77.24 分 / 8.26 分** |
| **官方範例題目** | 23,204 s（6.4 小時）| **0.07 分 / 0.02 分** |

**機制成立，而且影響完全被飛行時間的尺度決定。** 初賽是 LEO、`T_max = 4×T_A` 只有
6.4 小時，代理跟真實分數幾乎完全一致（中位數差 0.02 分），所以**這件事不影響初賽**。
會出事的是自己編的那些多日 T_max 極限情境。

要不要修：修的方式是把 J2 修正搬進適應度函式，但那等於每次評估都跑一次牛頓法差分修正，
成本會爆炸（現在一次評估 0.25 ms）。既然初賽尺度上誤差是 0.02 分，**不值得修**——
但要把「代理在長飛行時間會失準」寫清楚，未來設計長 T_max 情境時不要再拿 fitness 當真實成績比。

**這是既有問題，不是這次改動造成的。** 但這次改動讓它浮出來，因為
`_pick_best_case()` 現在是用 `mission_metrics()` 算出來的**真實分數**排名，
舊版是用 fitness 排。所以 hard_mode 不是退步，是**改成用真實分數挑之後，
那個 fitness 看起來 -80.7、真實只有 72.3 的 3 棒解，確實輸給真實 74.75 的 1 棒解**。

用真實分數挑顯然比用代理值挑正確（官方是用真實成績計分的），所以這個行為改變要留著。

**再跑一次又推翻了一半：** `SEED: null`，同一組設定重跑，這次 3 棒收斂到
**目標值 -83.574 / 真實 Score 83.66 / 0 違規 / ΔV 2,842.9 m/s**——真正的合法解，
而且代理跟真實只差 0.09 分，於是 3 棒正確地贏了。所以「交出 1 棒違規解」是**單次的
搜尋運氣**，不是系統性行為，兩次的**挑選都是對的**。

這給出更精確的結論：**代理誤差製造的是假的吸引子。** 搜尋有時候會收斂到一個
「代理看起來很好、真實很爛」的點（-80.72 / 72.3）；真正的好解上代理是準的
（-83.57 / 83.66）。用真實分數挑，正好擋掉那些看起來漂亮但交不出去的解——
這比原本以為的「只是計分比較準」更有價值。

**新的驗收基準（`hard_mode` 診斷變體）請用真實分數：3 棒、0 違規、
真實 Score ≈ 83.7、ΔV ≈ 2,843 m/s。** 不要再記 fitness。

### 還沒解決的：省油解跟快解是兩個完全不同的方案

官方範例題目在完整 T_max 下的最佳解是 **1 棒 412.1 m/s 但要 6,120.7 s**
（GMAT 驗證通過，Δr 3,498.75 m，跟 Python 差 **1.13 公尺**，固定燃燒版也乾淨過關）。
跟官方參考解比是省 5.4 倍油、慢 1.9 倍。而**單棒在 5,000 秒以前沒有合法解**。

哪一邊分數高**完全取決於當天公告的 `k_t/C_t`**。取捨曲線量在
`scratch_overnight/sample_pareto_frontier.py`（用 `rules.T_MAX_SEC` 當硬性時間上限掃）。
當天拿到參數就能直接在曲線上判斷該押哪邊。


## 🔴 2026-08-28 晚間：官方給出真實計分參數，策略整個反轉

主辦單位 18:55 來信給了範例題目的計分參數（回覆隊伍提問）：

```
kt = 0.003982 1/s        Ct = 3763.526 s
kv = 1.1862 (km/s)^-1    Cv = 2.955723 km/s
```

### 🔴 單位陷阱

**官方的 `kv` 是 `(km/s)^-1`、`Cv` 是 `km/s`，而本工具的 Δv 一律用 m/s。**
照抄會讓燃料項整個算錯。換算：`k_v = 1.1862/1000 = 0.0011862`、
`C_v = 2.955723*1000 = 2955.723`。`kt`/`Ct` 本來就是 1/s 跟 s。

**驗算方法**：官方參考解（2,241.427 m/s @ 3,211.737 s）用換算後的參數算出來
**剛好 90.00 分**——主辦方是照參考解校準參數的。以後拿到任何新公告的參數，
先用「參考解該得 ~90 分」驗一次單位。這條寫進 `CONTEST_DAY.md` 跟長期記憶了。

### 策略反轉：快 > 省油

| 方案 | ΔV (m/s) | T (s) | 時間分 | 燃料分 | 總分 |
|---|---|---|---|---|---|
| 官方參考解（2 棒）| 2,241.4 | 3,212 | 22.50 | 17.50 | **90.00** |
| 我們的快解（2 棒）| 2,224.7 | 3,185 | 22.73 | 17.60 | **90.33** |
| 我們的省油解（1 棒）| 412.1 | 6,121 | **0.00** | 23.83 | 73.84 |
| 多圈省油解（1 棒）| 250.7 | 11,916 | **0.00** | 24.03 | 74.03 |

`C_t = 3,763.5 s` 配上很陡的 `k_t`，讓 6,121 秒抵達的方案**時間分歸零**。
省 5.4 倍燃料只多拿 6.4 分，卻賠掉 22.7 分。**跟直覺完全相反。**

附帶結論：**這種參數形狀下 `LAMBERT_MAX_REVS` 要維持 0**——多圈轉移是拿時間換燃料，
方向剛好相反。今天實作它是對的（不知道參數之前必須兩條都能打），但這一題不該開。

### 🔴 這量出了今天那個 bug 的實際代價：16.5 分

修正 `check_constraints` 之前，我們在這題**找不到任何快解**（抵達 ≤5,600s 全部無解），
最好只有 **73.84 分，輸給官方參考解 16 分**。修正後 90.33，贏 0.33。

### 實跑結果（都經過 GMAT 端到端驗證）

| 設定 | Score | ΔV | T | Δr | GMAT 落差 | 耗時 |
|---|---|---|---|---|---|---|
| 預設 | 90.21 | 2,245.9 | 3,184.6 s | 3,500 m | 0.980 m | 76 s |
| 調校後 | **90.44** | 2,242.0 | 3,158.4 s | **2,313 m** | 0.755 m | 246 s |

調校的三項各自都看得到效果：

- `MAX_DV_MARGIN_MPS: 2.0` → 第一棒從 1,490.0 變成 **1,498.0 m/s**（拆棒解會頂到上限，
  那 8 m/s 本來是白丟的）
- `MAXITER: 2000` → 找到更好的點（快解那片區域很窄，`coast_frac` 要壓到 ~0.004）
- `TIEBREAK_SCORE_EPS: 0.005` → 平手微調把 Δr 從 3,500 m 壓到 **2,314 m**，
  代價 0.004992 分（規則第 6 節優先序 1 是硬性字典序，Δr 小的排前面）

**注意**：調校後的贏家是「3 棒但實際只用 2 棒」的退化解。分數是真的比較好（多一個維度
幫到搜尋，不是多一棒幫到物理），但繳一份含 `Δv = 0.0 m/s` 空燒的腳本不好看，
正在測純 2 棒能不能達到同樣分數。


## 2026-08-28 深夜：多圈 Lambert 拿 ESA pykep 交叉驗證，完全等價

今天把 `izzo()` 從寫死 `M=0` 改成掃 `M x lowpath x 順/逆行`，是這天最有風險的改動——
分支挑錯會直接汙染繳交的答案，而且**沒有任何外部檢查**。

拿 [pykep](https://www.esa.int/gsp/ACT/open_source/pykep/)（ESA Advanced Concepts Team
維護）的 `lambert_problem` 做集合比對，200 組隨機幾何 x 順/逆行，`max_revs=3`：

| | 結果 |
|---|---|
| 比對解數 | 1,552 |
| 最大偏差 | **5.169e-14 km/s**（機器精度）|
| 假解（我們有、pykep 沒有）| **0** |
| 漏解（pykep 有、我們沒有）| **0** |
| pykep 自身解數符合 2N+1 | 全部相符 |

**結論：我們的分支政策跟 ESA 的獨立實作逐點等價。** 多圈的結果可信。

腳本在 `scratch_overnight/xcheck_lambert_pykep.py`。兩個踩到的環境坑記在那裡：

1. **pykep 3.0.1 的 wheel 漏打包 `trajopt/gym/tops/*.json`**，`import pykep` 直接
   FileNotFoundError。腳本開頭的 `_patch_pykep()` 會補四個空 json（uv 快取目錄，可重建）。
2. **`uv run --with pykep` 會把 numpy 拉到 2.5，numba 不吃**（需要 <= 2.4）。
   要加 `--with 'numpy<2.5'`。完整指令：
   `uv run --with pykep --with 'numpy<2.5' python scratch_overnight/xcheck_lambert_pykep.py`


## 2026-08-29 深夜：GTOC-9 真實軌道壓力測試 + 多圈預設改為 4

### 壓力測試：40 組 ESA 公開的真實 LEO 碎片軌道，0 崩潰

軌道來源是 ESA Kelvins 平台上 GTOC-9「The Kessler Run」的 `debris.csv`（123 筆真實碎片）。
用途是**打破「編測資的人 = 寫程式的人」這個相關性**——自製情境的盲點跟程式的假設是同一套。
依兩軌道平面夾角分層抽 40 組配對，套官方真實計分參數。
（限制寫在 `scratch_overnight/gtoc9_orbits.py`：曆元沒歸算，所以相對相位是任意的；
GTOC-9 要的是交會不是攔截，他們的分數跟我們**不能比**。這裡只借軌道幾何。）

| | |
|---|---|
| 完成 / 崩潰 / 無解 | **40 / 0 / 0** |
| Score | min 73.68、中位 74.09、max 98.42 |
| 有違規的 | 6/40 |
| 退化解（實用棒數 < 名目）| 15/40 |
| 每組耗時 | 60~107 秒 |

**對 40 組我沒設計過的幾何，整條流程沒有任何一次崩潰。**

### 🔴 那些「74 分」是對的，不是沒找到

分數呈雙峰：多數約 74 分（Δv 極省 45~520 m/s，但抵達 21,000~24,000s），少數 93~96 分
（抵達 1,600~3,100s）。用官方參數算這個取捨，Δv 從 45 拉到 1,500 只掉 3 分燃料分，
但抵達從 21,500s 縮到 3,000s 可以拿回 22 分時間分——**淨賺約 19 分**。
所以 74 分要嘛是快解不存在，要嘛是沒找到，兩者處理方式完全不同。

用 `rules.T_MAX_SEC` 硬壓抵達上限逼搜尋只能找快解（`gtoc9_fastbranch_probe.py`，5 組 x 4 個上限）：

| 上限 | 結果 |
|---|---|
| 3,000s | 3/5 找不到解，其餘交出 11,000~57,800 m/s 的違規解 |
| 4,000s | 全部違規（10,000~57,800 m/s，上限是 1,500）|
| 6,000s | 同上 |
| 9,000s | 有合法解（812~2,128 m/s）但**分數反而低 0.78~5.95** |

**結論：快解真的不存在。** 近共平面、同高度的 LEO 配對要快就得徹底改變軌道，
1,500 m/s 的每棒上限做不到。官方範例題目之所以有快解，是因為 A/B 的 SMA 差 100 km
且平面夾角 93.84°，幾何允許。**工具的判斷是對的。**

### `LAMBERT_MAX_REVS` 預設 0 -> 4

**我先前寫的「時間項陡時要關掉多圈」是錯的**，理由錯在我比的是不同的決策向量。
正確的結構論證：分支選擇是在**固定的 `t_final_leg`** 下取需求 Δv 最小的那條，
所以同一個決策向量的抵達時間不變、時間分不變，只有燃料可能更便宜——
**嚴格更大的搜尋空間，不可能變差。**

實測（官方範例題目，同 `SEED=42`，只差這個開關）：

| | Score | ΔV | T_team |
|---|---|---|---|
| `REVS=0` | 90.21 | 2,245.9 m/s | 3,184.6 s |
| `REVS=4` | **90.43** | 2,241.2 m/s | 3,160.1 s |

其他證據：`known_planechange` 84.7 -> 62.4 m/s（省 26%，精準命中閉合構造）；
`known_phasing`（`MAX_BURNS=[1]`）1,490.0 -> 70.6 m/s（省 21 倍）。
成本：每次評估 REVS=4 對 REVS=0 是 **1.01 倍**，端到端量不出差異。
正確性：ESA pykep 1,552 條解、最大偏差 5.2e-14 km/s。

取 4 是因為規則的 `T_max = 4 x T_A`，最多也就塞得下約 4 圈。設 0 可退回舊行為。

### 改預設後的完整回歸（全部 `--no-gmat`，官方範例題目另跑 GMAT）

| 情境 | 舊基準 | 新結果 | 判定 |
|---|---|---|---|
| `smoke_test` | 90.23 | 90.24 | 一致 |
| `playground` | 90.67 | 90.67 | 一致 |
| `hyperbolic_smoke` | 85.22 / 851.2 m/s | 85.22 / 851.2 m/s | 一致 |
| `perigee_kick_test` | 89.28 / 2,392.4 m/s | 89.28 / 2,392.3 m/s | 一致 |
| **`hard_mode_test`** | **84.01，1 次違規** | **92.54，0 違規，2,733.1 m/s @ 3.3 天** | 🔴 **大幅變好** |
| `weird_test` | 78.85 / 1,190.6 m/s | 78.85 / 1,190.6 m/s | 一致 |
| `known_hohmann` | 586.1 m/s | 586.1 m/s | 一致（下限 586.121）|
| `known_phasing` | 70.6 m/s | 70.6 m/s | 一致（構造 70.87）|
| `known_planechange` | 84.7 m/s（REVS=0）| **62.4 m/s** | 大幅變好（構造 62.66）|
| 官方範例題目 | — | 90.21，GMAT 差 **1.042 m**，固定燃燒版可繳交 | ✅ |

`tests/test_tiebreak.py`、`tests/test_arc_safety.py` 全過。

### 🔴 多圈解掉了 `hard_mode` 長期買違規的問題

`hard_mode` 是目前最難的情境，歷史上一直交出**違規解**；已知最好的合法解是
3,185.4 m/s / 6.93 天（約 83.15 分）。開多圈之後：

**2,733.1 m/s / 284,004 s（3.3 天）/ 3 棒 / 0 違規 / 92.54 分**

比已知最好的合法解**又便宜（-14%）又快（-53%）**，而且完全不需要買違規。
先前記載的「這組是唯一有已知正確答案可以衡量搜尋能力的基準」要更新——
**新的基準是 2,733.1 m/s / 92.54 分，而且要記真實分數不要記 fitness。**

### 🔴 方法論：沒有固定 seed 的 A/B 比較不可信

改完 `LAMBERT_MAX_REVS` 預設後，拿同一批 40 組真實軌道配對重跑做對照，結果是
**REVS=4 淨負 7.55 分**（4 好 / 3 壞 / 33 持平，最差一組 -9.94）。差點就據此把預設改回去。

但兩批都是 `SEED: null` 各自獨立跑的，分不出「多圈害的」還是「重跑變異」。
而且有一個**真實存在的機制**可以讓多圈間接害事：多圈讓慢解變便宜
（12,000s 上限：404.5 -> 251.3 m/s），而陡峭的時間項讓慢解是陷阱——
所以「把錯的盆地變得更有吸引力」不是無稽之談，不能用結構論證打發。

（我原本的結構論證「同一個決策向量下多圈不會更差」**是對的**，但它
**沒有涵蓋搜尋會落到哪個盆地**。論證的適用範圍要自己講清楚。）

受控 A/B（同 8 組配對、同 `SEED=12345`、只差開關，`scratch_overnight/gtoc9_revs_ab.py`）：

| | |
|---|---|
| 總計 | **+0.33 分** |
| 個別差異 | -0.03 ~ +0.29，多數 ±0.02 |
| `T_team` | 兩邊幾乎完全相同（21,446 vs 21,442、23,617 vs 23,626…）|

`T_team` 一致正好印證結構論證：同 seed -> 同搜尋軌跡 -> 同抵達時間，只有燃料會變。
所以那 -7.55 是**重跑變異**，預設改成 4 站得住。

**教訓：這個工具的重跑變異在個別情境上可以到 ±10 分。**
任何「改了設定之後變好/變差」的判斷，**沒有固定 seed 就不算數**。
（注意設了 `SEED` 會自動退回單執行緒換取重現性，會比較慢，見 `_optimize_burn_case`。）

### 已知答案測資第三組：`known_planechange`

同半徑圓軌道的兩個平面**必定相交**，B 根本不用轉平面。

| | Δv |
|---|---|
| 課本純平面變更 `2v sin(θ/2)` | 7,546 m/s（超過每棒上限 5 倍）|
| 相位構造（在共用節線會合）| 62.66 m/s |
| 工具（多圈）| **62.4 m/s @ t=22,747.5s**（構造是 62.66 @ 22,747.4s）|

**課本答案貴 120 倍。**


## 還沒做 / 值得考慮的

優先順序由高到低：

1. ✅ **已解決：固定 60 秒 RK4 步長**——先換 RK45，後來又換成更好的 DOP853 (8 階 Dormand-Prince)，目前是主線積分器。3 天/44 圈測試案例誤差從舊 RK4 的 17.2 公里壓到 DOP853 的 222 公尺，速度也持平或優於 RK45 (不像 RK45 相對舊 RK4 有短弧線變慢的取捨，DOP853 幾乎沒有這個代價)。細節見「DOP853」那一節。
2. ✅ **已解決：找出「能信任」跟「不能信任」的軌道規模分界線**——系統性掃描 SMA (6,578km~5,000,000km) 跟 ECC (0~0.99，含雙曲線 1.01~10) 後確認：**SMA 完全不是限制因素** (再大再小都準)，**真正的邊界是離心率**：ECC≤0.9 安全 (公尺~2公里等級)，0.9<ECC≤0.95 開始接近 5km 門檻，ECC>0.95 不可信 (可以到 10~120km 誤差)。雙曲線軌道 (排位賽相關) 完全沒問題。這個 session 一直在用的測試軌道 (ECC=0.87) 全程都在安全範圍內。細節見「系統性測試軌道參數極限」那一節。
3. **正式測資公布後，`configs/config.json` 整組要換成官方數字**（軌道六根數 + `k_t/C_t/k_v/C_v`）。現在的數字除了 `orbit_A.ECC=0` 是照簡報修正過的，其他都是我們自己編的，不能直接拿去用。
4. **排位賽的官方細節公布後要接上**：目前只做了「不會直接壞掉」的輸入端準備 (`config_validator.py`/`optimizer.py` 接受雙曲線 A + `rules.T_MAX_SEC` 覆寫，見上面「排位賽輸入端準備」一節)，T_max 實際怎麼算、計分公式是否跟初賽一樣，都要等公告後才能真正接上。
5. **如果晉級賽出現 ECC>0.95 的高離心率橢圓軌道，現在的工具還沒有應對機制**——目前只確認了「這個範圍不可信」，還沒做「偵測到這個範圍就發警告/自動切換到更高精度處理」。優先順序低，因為目前完全沒有跡象顯示比賽會用到這麼極端的離心率橢圓軌道 (初賽是圓、排位賽是雙曲線，兩者都已經確認安全)。
6. **Encke's method 留著沒接上**——已驗證正確、比 DOP853 之前的比較基準 RK45 準，但比 RK45/DOP853 都慢，`propagate_encke` 留在 `core_math.py` 但主線沒有呼叫。DOP853 換上主線後，這個選項的吸引力更低了 (DOP853 已經同時達到「快」跟「準」)，除非之後有新的理由，否則不建議再花力氣。
7. ✅ **已解決：物理直覺的初始種子**——單棒 (`num_burns==1`) 版本 2026-08-14 晚上實作完成 (見「第六階段」)，2026-08-15 凌晨推廣到高傾角差 (節線鄰近度候選來源) 跟多棒 (`num_burns>1`，分段貪婪接力) (見「夜間自主開發」)。**2026-08-15 白天用正式預算 (`MAXITER=1000,POPSIZE=20`) 驗證過，種子機制的效果確認且比夜間報告的更強** (有種子 77~79 分 vs 無種子 64.33 分，且無種子 baseline 在 300 代跟 1000 代分數到小數點後四位相同)。傍晚修完容忍度後**又用同樣預算整組重跑一次**，優勢維持 +14.45 分，且無種子 baseline 逐位元不變 (見「正式預算 A/B 重驗」一節)。仍然只在自編的極端壓力測試上驗證過，正式測資公布後數字可能完全不適用，但機制本身 (粗掃候選窗口→細網格精修→獨立精修) 應該還是通用的。
8. 四強賽（即時追逐戰，電競直播模式）**完全還沒碰**——這是離線最佳化+腳本產生架構完全不適用的另一種問題，等晉級再說。
9. ✅ **已解決：進度條粒度太粗，看不出真實進度**（使用者 2026-08-14 下午發現，同一天稍晚修復）。根因是 `run_study()` 原本的 `tqdm(concurrent.futures.as_completed(futures), total=num_cases)` 用「燃燒次數案例數」當總量，整個案例（可能要跑幾秒到幾分鐘）全部跑完才跳一格。改法：`src/optimizer.py` 的 `_optimize_burn_case` 新增 `progress_queue` 參數，用 `_attach_progress_reporting()` monkeypatch mealpy 模型 instance 的 `track_optimize_step`（mealpy 每代結束都會呼叫這個方法，不管 `log_to` 是不是 `None`），讓子行程每跑完一代就把 `(current_burns, epoch)` 塞進一個 `multiprocessing.Manager().Queue()`（不是普通 `multiprocessing.Queue()`——那個只有在子行程「建立當下」當參數傳入才能正確共享，事後才傳給已經活著的 `ProcessPoolExecutor` worker 會直接丟 `RuntimeError`；`Manager().Queue()` 的 proxy 就是設計成能在任意時間點 pickle 送給已經在跑的行程，spawn 下也驗證過沒問題）。主行程開一條背景 thread 持續清空這個佇列、更新一個 `total=num_cases*self.maxiter`（世代數加總）的 tqdm bar，跟原本負責印「案例完成」訊息/挑最佳解的 `as_completed` 迴圈用一個 `threading.Lock` 共用進度資料，避免兩邊同時 `update()` 打架；每個案例 (不管正常完成/提早停止/崩潰) 收尾時都會在 `finally` 補滿進度到 `self.maxiter`，確保 100% 精準收在總量上，不會卡在 99% 或某個案例的份額空白。實測：`practice_scenario.json` 規模 (3 案例×150代=450) 進度條平滑從 0%→2%→4%→...→100%（不是舊版的一次跳 33%），大規模 (2 案例×1000代=2000) 一樣精準跑到 2000/2000，跑完後用 `ps` 確認沒有殘留的子行程/Manager 行程，其餘輸出 (Mission Plan/驗證/分數) 都不受影響。
10. **`sweep_burns.py` 粗掃階段依維度公平分配世代預算**——已實作、驗證過邏輯正確 (見「sweep_burns.py 粗掃階段依維度公平分配世代預算」那節)，但**建議的加速參數組合 (`--coarse-iters 25 --coarse-iters-cap 100 --coarse-popsize 6`) 還沒讓使用者實際在 `weird_test.json` 上跑過**，估計的 7~8 分鐘只是算帳算出來的，不是實測數字，下次接手可以補這個驗證。
11. **`_optimize_burn_case` 的 `n_workers` 不會隨著其他案例跑完釋放核心而增加**——已知的 CPU 沒完全榨乾的地方。**這條當初「先不做」的理由（MacBook Air 會熱降頻，多開 thread 不保證淨賺）在換到 WSL/5800X 之後已經不適用了**（塔扇散熱、16 threads），值得重新評估——現在做這個修法是淨賺的機率高很多。
12. **拿 5800X 實測比對 MacBook Air (熱降頻)**——**這條的前提已經改變**：2026-08-15 起主要開發機就是 5800X 那台，不再是 Air。原本想做的「兩台對照、量出差距」如果還想做，得回頭到 Air 上跑一次；但如果之後都在 5800X 上開發，這個比較的實用價值不高，可以考慮直接關掉。參考數據：5800X 上 `weird_test.json` 正式預算 5 個案例（1/2/3 棒，含有無種子對照）總計 27.7 分鐘。
13. **`weird_test.json` 找到的合法單棒解 (1189.73 m/s) 還沒拿真的 GMAT 驗證過**——目前只有 Python/Lambert 預測，細節見「weird_test.json 深度診斷」那節。2026-08-15 白天在 WSL 這台重建了 `weird_test.json` 並重跑搜尋，但**沒有專門把這個特定解餵進 GMAT 驗證**，這條仍然待辦。（不過同一天在另外兩組測資上，GMAT 跟 Python 只差 0.6~13 公尺，是個旁證。）
14. ✅ **已解決：`script_generator.py` 的 `View_ShipBChase` 座標系修復**——2026-08-14 傍晚修的 (原本用固定慣性系偏移量，B 軌道半徑跟地球半徑餘裕不夠時鏡頭會跑進地球裡面；改成綁在 ShipB 身上、永遠指向遠離地球方向的 `ObjectReferenced` 座標系)，已經用真實 GMAT 跑過 `practice_scenario.json` 確認腳本正常解析、InterceptSuccess/MissDistance 不受影響。commit `e30f96a`——這條 TODO 原本寫「還沒 commit」，2026-08-15 凌晨核對 git log 發現其實已經進去了，是這份文件沒同步更新，順手修正。
15. **夜間開發 (2026-08-15) 留下的後續工作**，優先順序由高到低：
    - ✅ **已完成 (2026-08-15)**：`overnight-generalization` 分支已 merge 回 `Master` 並刪除（四個 commit 都在 `Master` 上，核對過 git log）。`scratch_overnight/` 決定留著當文件參考，沒有清掉。
    - ✅ **已完成 (2026-08-15)**：多棒種子機制的正式預算驗證做了，結論部分反轉——種子機制的價值確認且更強，但「棒數越多分數越好」不成立。詳見「2026-08-15 白天」那一節。
    - ✅ **已完成 (2026-08-15)**：「傾角差極大 + A軌道有明顯遠地點」的專門案例做了（`configs/apoapsis_planechange_test.json`），結果是否定的，而且否定的理由推翻了提案本身的前提（純位置攔截不需要轉平面）。真正逼出多棒的是另一組「單棒數學上不可能」的測資（`configs/perigee_kick_test.json`）。同一節有完整記錄。
    - 座標下降精修 (輪流鎖住其他維度、只精修一個子集) 跟 CMA-ES 打野測試 (取代 stage-4 的 L-BFGS-B) 原本規劃在方向二的範圍內，時間不夠沒做，還沒實作也沒測試。
    - 「額外探索」那節的軌道共振測試沒有定論 (只有 1:1 共軌明顯窄化，其餘共振比沒有規律)，如果之後想深挖，1:1 那組窄化更可能是「兩船軌道幾乎重合、相對漂移慢」而不是傳統共振，值得先確認這個猜測對不對。
    - 夜間那批 Δv 數字 (單棒 1183~1198 m/s、2棒 1192.8 m/s 等) 仍然只有 Python/Lambert 預測，**還是沒有拿真的 GMAT 驗證過**（2026-08-15 的驗證是重跑搜尋，沒有把夜間那些特定解餵進 GMAT）。不過同一天在兩組新測資上做了等價的交叉驗證，GMAT 跟 Python 只差 0.6~13 公尺，對「這個尺度的解可信」有相當的旁證。
    - 開工前偶然發現 `configs/weird_test.json` 被使用者互動測試時的暫時性編輯 (`SMA=35000,ECC=0.8`) 覆蓋掉了，已經還原成正式版本 (`SMA=150000,ECC=0.93`)——以後如果要用 `weird_test.json` 做一次性實驗，建議另存新檔，不要直接編輯這個當成長期基準的檔案。
16. **`configs/` 在新機器上要自己重建**——整個目錄被 gitignore，換機器不會帶過來。2026-08-15 在 WSL 這台上重建了 `weird_test.json`（軌道六根數是使用者提供的，STATUS.md 先前只記了部分參數，`orbit_B` 的 RAAN/AOP/INC 沒寫下來，差點無法重現）。**教訓：如果某個 config 是長期基準、實驗結論要靠它重現，六根數應該完整寫進 STATUS.md，不能只寫「A 是 SMA=150000/ECC=0.93、B 是一般 LEO」。** 目前這台機器上的 configs：`config.json`、`weird_test.json`、`smoke_test.json`、`apoapsis_planechange_test.json`、`perigee_kick_test.json`（後兩者的完整參數在對應的 `scratch_overnight/design_*.py` 裡有記錄，可以重建）。

## 跑法提醒

```bash
uv run main.py                          # 用 configs/config.json
uv run main.py --config configs/x.json  # 換設定檔
uv run main.py --no-gmat                # 跳過自動 GMAT 驗證
```

WSL 這台機器上開 GMAT GUI（看 3D 軌跡用，一般驗證流程不需要，`main.py` 走的是
無頭的 `GmatConsole`）：

```bash
~/software/gmat-gui.sh ~/ODC_GMAT_Planner/outputs/output_submit.txt
```

正式提交前：確認 `run_history.jsonl` 最新那筆 `gmat_verified.intercept_success` 和 `final_burn_legal` 都是 `true`。README.md 有更完整的欄位說明。

## 這個 session 的工作方式（給下次的自己參考）

- 使用者很重視「實測驗證，不要用猜的」——這個 session 好幾次理論上「應該沒問題」的東西實測後發現是錯的（seed 沒生效、GMAT 打靶蓋掉省油設計、安全邊界不夠寬、`local` dt 自適應改動），都是靠寫小驗證腳本、跑 `main.py`、對照 GMAT 實際輸出抓出來的，不是光看程式碼推理出來的。改動後最好都找方法實測，不要只憑理論保證。
- 傾向小步快跑：改一項、驗證一項、確認沒問題再繼續，不要一次改一大包才測。
- **新教訓（RK4 自適應步長那次）**：只在「一般規模」情境上跑過回歸測試就以為沒事是不夠的——那次改動在 `practice_scenario.json` 上完全正常，卻在極端規模的情境上讓數值直接爆炸。**改動核心物理引擎/演算法時，光靠一種規模的情境驗證不夠，至少要用差異夠大的多種規模交叉測過，才能真的說「沒問題」。**
- **新教訓（依賴清理那次）**：改完 `pyproject.toml`/依賴之後，只在本機既有的 `.venv` 測不出「全新環境裝不裝得起來」這種問題（本機的 venv 可能還留著清理前的殘留套件，掩蓋掉問題）。**改完依賴後，要嘛自己刪掉 `.venv` 重來一次真的模擬全新環境，要嘛請別人在真正全新的機器上測。** 這個 session 至少踩到兩次同一類的坑（`tqdm` 遺漏、`outputs/` 資料夾不存在）。
- 這個 session 後半段有隊友在幫忙用全新 Windows 環境實測，抓到好幾個只有在「完全乾淨的環境」才會現形的問題（tqdm 依賴、`outputs/` 資料夾、GMAT 路徑寫死）——**這種交叉環境測試很有價值，比自己一個人在同一台開發機上測更容易抓到部署層級的問題**，值得繼續維持這個習慣。
- 背景執行長時間指令（例如極端情境的完整流程，可能要跑 20~30 分鐘）時，**不要同時開兩個一起跑**——這個 session 曾經讓兩組極端情境的測試同時搶 10 個核心，互相拖慢，比序列執行還慢。一次一組，或至少留意資源競爭。
- **新教訓 (2026-08-14，「先讀規則再下結論」)**：一開始把 `scorer.py` 的固定 -10 分違規懲罰講成「懲罰太輕、像 bug」，提案要改成硬性淘汰——後來翻官方規則 PDF 才發現這就是主辦方明文規定的計分方式 (第 5 節)，程式碼完全沒問題，是自己想當然爾猜錯了。**遇到「這個公式/這個邏輯看起來不合理」的直覺時，先去翻官方規則原文或既有文件確認過再下判斷，不要憑經驗猜——尤其是計分/規則相關的地方，程式碼很可能是照著白紙黑字的規定寫的，用一般工程直覺去評判反而會判斷錯。** 這次還好在動手改之前先問過使用者，使用者一句「超了也沒辦法，只能以分數去讓他判斷」就點出了正確的方向。
- **新教訓 (2026-08-14，拆解「表面現象」跟「根本原因」要分層验证)**：極端軌道 GMAT 對不上這個現象，一開始很容易當成單一原因 (「模型在這個尺度失準」) 就結案，但實際上疊了三層 (計分規則本身允許違規解、DC 的 Vary 邊界結構性搆不到超標的目標、以及長時間傳播下真正的模型落差)，只有全部拆開個別驗證 (每層都用受控實驗單獨測，不是看到一個解釋就停) 才發現真正還沒解決的只剩最後一層，而且範圍比原本以為的小很多 (不是「這個 SMA/ECC 量級」，是「這個傳播時長」)。**看到一個異常現象、腦中冒出第一個「應該是因為 X」的解釋時，先假設它只是部分原因，找方法把其他可能疊加的原因也分別隔離測過，不要驗證到第一個解釋說得通就停手。**
- **新教訓 (2026-08-15，「軌道匹配」的直覺會系統性高估「位置攔截」的成本)**：設計 `apoapsis_planechange_test` 時，我用「90 度平面轉向要 2·v·sin(θ/2)」算出遠地點 1202 m/s、近地點 13820 m/s，據此宣稱這組情境有明確的教科書答案。**實際跑出來只要 721 m/s，比我算的「最佳策略」還便宜 40%，而且燃燒點根本不在遠地點。** 因為規則只要求 Δr≤5km（撞到位置），不要求共軌——Lambert 只需要抵達位置，不需要匹配速度，遠距離目標更是讓小角度偏轉被放大成大橫向位移。**這跟第五階段 MOID 分析被推翻是完全同一個錯誤，隔一天又犯一次。** 教訓：凡是要宣稱「這個情境有多難 / 最佳策略是什麼」，都必須用實際的 Lambert 網格掃過才算數，不能用軌道力學的解析公式推——那些公式算的是「進入並停留在某個軌道」的成本，跟這個任務要的東西不是同一件事。（下一組 `perigee_kick_test` 就改成先實掃 39,200 組確認「單棒處處超標」，理論下限 2391 跟網格實測 2419 只差 27 m/s，這次才對得上。）
- **新教訓 (2026-08-15，「證據不成立」不等於「結論錯誤」)**：正式預算重跑後發現夜間報告的多棒 A/B 表證據無效（多棒解全是 Δv=0 的退化單棒），當下很容易直接下「多棒機制沒用」的結論。但再設計一組「單棒數學上不可能」的測資後，多棒立刻找出真實的兩棒合法解——**原結論是對的，錯的只是論證用的測資剛好不需要多棒**。教訓：推翻一份證據時，要分清楚推翻的是「證據」還是「結論」，並且主動去找一個能真正檢驗該結論的情境，不要用「證據不成立」直接反推「結論不成立」。
